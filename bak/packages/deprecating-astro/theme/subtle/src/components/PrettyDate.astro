---
const { date, } = Astro.props;
const dateDate = new Date(date,);
const readable = dateDate.toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: '2-digit', },);
---
<time datetime={dateDate.toString()}>
  {readable}
</time>
