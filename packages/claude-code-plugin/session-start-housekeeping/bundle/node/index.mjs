#!/usr/bin/env node
import{glob,lstat,mkdir,rm}from"node:fs/promises";import{text}from"node:stream/consumers";const NO_STDOUT=Symbol(`claude-code-plugin-runtime/hook-intentionally-emits-no-stdout-bytes`);async function runHookPlugin({parser,handler,writer}){let serialized=writer(await handler(parser(await text(process.stdin))));serialized!==NO_STDOUT&&process.stdout.write(serialized)}const STALE_DIST_ARTIFACTS=[`HEAD`,`config`,`hooks`,`objects`,`refs`,`.claude`],ROOT_SENTINEL_ARTIFACTS=[`HEAD`,`config`,`hooks`,`objects`,`refs`];async function ensureDir(dirPath){await mkdir(dirPath,{recursive:!0})}async function cleanDistArtifacts(workspaceRoot){let removals=[];for await(let finalDir of glob([`packages/*/*/dist/final`,`packages/*/*/bundle`],{cwd:workspaceRoot}))for(let artifact of STALE_DIST_ARTIFACTS)removals.push(rm(`${workspaceRoot}/${finalDir}/${artifact}`,{recursive:!0,force:!0}));await Promise.all(removals)}function isMissingPathError(error){return typeof error!=`object`||!error||!(`code`in error)?!1:error.code===`ENOENT`}async function cleanRootSentinelArtifact(artifactPath){try{let stats=await lstat(artifactPath);if(!stats.isFile()||stats.size>0)return;await rm(artifactPath,{force:!0})}catch(error){if(isMissingPathError(error))return;throw error}}async function cleanRootSentinelArtifacts(workspaceRoot){await Promise.all(ROOT_SENTINEL_ARTIFACTS.map(function(artifact){return cleanRootSentinelArtifact(`${workspaceRoot}/${artifact}`)}))}async function removeMcpJson(workspaceRoot){await rm(`${workspaceRoot}/.mcp.json`,{force:!0})}async function sessionStartHousekeepingHandler(event){let workspaceRoot=event.cwd;await Promise.all([ensureDir(`/tmp/claude`),ensureDir(`/tmp/claude-1000`),cleanDistArtifacts(workspaceRoot),cleanRootSentinelArtifacts(workspaceRoot),removeMcpJson(workspaceRoot)])}function sessionStartHousekeepingParser(raw){return JSON.parse(raw)}function sessionStartHousekeepingWriter(_output){return NO_STDOUT}await runHookPlugin({parser:sessionStartHousekeepingParser,handler:sessionStartHousekeepingHandler,writer:sessionStartHousekeepingWriter});export{};