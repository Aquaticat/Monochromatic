---
interface Props {
  date: Date;
}
const { date } = Astro.props;
---
<time datetime={String(date)}>
  {
    date.toLocaleDateString('en', {
      year: 'numeric',
      month: 'short',
      day: '2-digit',
    })
  }
</time>
