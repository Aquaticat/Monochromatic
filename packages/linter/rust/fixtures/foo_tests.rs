// This fixture is linter test input, not part of the crate build.
// Its name ends in `_tests.rs`, so the max-lines rule must exempt it even
// when the budget is tiny. It has three code lines.

fn exempt_sample() {
    let value = 1;
}
