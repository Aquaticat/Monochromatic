/**
 * Shared constants for terminal title formatting.
 *
 * @module
 */

/**
 * Marker appended when title text is truncated.
 *
 * @example
 * ```ts
 * `${'prefix'}${TITLE_TRUNCATION_MARKER}`;
 * // 'prefix…'
 * ```
 */
const TITLE_TRUNCATION_MARKER: string = '…';

export { TITLE_TRUNCATION_MARKER, };
