/**
 * Default export that is a function returning the number 1, used for testing callable default exports.
 *
 * @returns Always 1
 *
 * @example
 * ```ts
 * return1() // → 1
 * ```
 */
export default function return1(): number {
  return 1;
}
