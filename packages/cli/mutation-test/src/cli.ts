#!/usr/bin/env node

/**
 * Host CLI entrypoint for the oxc-based mutation tester.
 *
 * Parsing and orchestration land with the host-orchestrator milestone;
 * until then this entrypoint fails loudly instead of pretending to run.
 *
 * @example
 * ```bash
 * mutation-test --package packages/module/fs-path
 * ```
 */

export {};

throw new Error('mutation-test: not implemented yet; see packages/cli/mutation-test/README.md',);
