# Gradle / Android build outputs and machine-local config.
/build
/app/build
/.gradle
/.kotlin
/local.properties
*.iml
.idea/
captures/

# Rust native engine: cargo build dir, and the cargo-ndk-produced .so packaged
# into the app (a build artifact; the crate source under /rust is tracked,
# Cargo.lock included for reproducible app builds).
/rust/target
/app/src/main/jniLibs
