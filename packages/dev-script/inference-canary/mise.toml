[tasks.run]
description = "Run the inference canary against all configured models"
run = "bun src/index.ts"

[tasks.'lint:types']
extends = "lint:types"

[tasks."lint:oxlint"]
extends = "lint:oxlint"
