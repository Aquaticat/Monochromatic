[tasks.build]
extends = "build"

[tasks."build:js"]
extends = "build:js"

[tasks."build:js:node"]
extends = "build:js:node"

[tasks."watch:build"]
extends = "watch:build"

[tasks."watch:build:js"]
extends = "watch:build:js"

[tasks."watch:build:js:node"]
extends = "watch:build:js:node"

[tasks.lint]
extends = "lint"

[tasks.'lint:types']
extends = "lint:types"

[tasks.'test:unit']
extends = "test:unit"

[tasks."lint:oxlint"]
extends = "lint:oxlint"

[tasks."test:container"]
description = "Run ensurePackage container tests across OS/user matrix"
run = "bun src/package/mise.container-test.ts"

[tasks."generate:index"]
description = "Generate packages.generated.ts from Repology data"
run = "bun src/package/mise.generate-index.ts"
