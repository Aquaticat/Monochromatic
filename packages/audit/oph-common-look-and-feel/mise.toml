# Static audit document.
# Open src/index.html directly in a browser (all assets are embedded).

[tasks.lint]
extends = "lint"

[tasks."lint:oxlint"]
extends = "lint:oxlint"

# No lint:types -- this package has no TypeScript source files.
