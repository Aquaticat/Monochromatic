# Static audit document.
# Open src/index.html directly in a browser (all assets are embedded).

[tasks.lint]
run = "echo No lintable TypeScript sources."

# No lint:oxlint or lint:types -- this package has no TypeScript source files.
