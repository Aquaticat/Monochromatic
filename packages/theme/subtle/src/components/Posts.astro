---
import Post from './Post.astro';
import {langsPosts} from './_globs.astro';
import consts from '../../consts';
const lang = Astro.currentLocale!;
const posts = Astro.props.posts || langsPosts.get(lang === consts.langs.defaultLang ? '' : lang);
---

<ul class="Posts">
  {
    posts.map((post) => (
      <Post post={post}></Post>
    ))
  }
</ul>
