---
import consts from "../../consts";
const linksArr = Array.from(consts.links) as string[];
---

<ul class="Links">
    { linksArr.map((link) => (
    <li>
      <a href={link[1]}>
        <span>{link[0]}</span>
      </a>
    </li>
    ))
  }
</ul>
