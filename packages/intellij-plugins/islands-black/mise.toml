[tasks.build]
description = "Build islands-black.jar for installing the plugin from disk."
run = "node src/build.ts"

[tasks.lint]
extends = "lint"

[tasks."lint:types"]
extends = "lint:types"

[tasks."lint:oxlint"]
extends = "lint:oxlint"
[tasks."format:oxlint"]
extends = "format:oxlint"
