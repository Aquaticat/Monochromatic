[tasks.lint]
extends = "lint"

[tasks."lint:oxlint"]
extends = "lint:oxlint"

[tasks."format:oxlint"]
extends = "format:oxlint"

[tasks."lint:types"]
extends = "lint:types"

[tasks.bench]
description = "Parse benchmark: jsonc-edit vs jsonc-eslint-parser and microsoft jsonc-parser"
run = "node src/parse.bench.ts"
