[task_config]
includes = [
  "../../../mise-shared/browser.toml",
  "../../../mise-shared/build-js.toml",
  "../../../mise-shared/build.toml",
  "../../../mise-shared/lint-types.toml",
  "../../../mise-shared/lint.toml",
  "../../../mise-shared/oxlint.toml",
  "../../../mise-shared/watch-browser.toml",
  "../../../mise-shared/watch-build.toml",
]

[tasks.test]
description = "Run type-level and runtime unit tests for every LLM type"
run = "node src/self.unit.test.ts"

[tasks.buildAndTest]
description = "Build the neutral bundle, then run unit tests against the built dist"
run = ["mise run build", "mise run test"]
