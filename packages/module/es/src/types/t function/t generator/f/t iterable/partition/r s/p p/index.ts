/**
 * Re-export of the named-parameter sync variant. The `p p` (positional) form
 * is retained as a directory shape but uses destructured parameters per
 * workspace lint policy.
 */
export { $, } from '../p n/index.ts';
