// Fixture: array elements on the same line.
// Expected violations: stylistic(array-element-per-line)

const numbers = [1, 2, 3];

const strings = ['alpha', 'beta'];

const nested = [[1, 2], [3, 4]];

export { numbers, strings, nested };
