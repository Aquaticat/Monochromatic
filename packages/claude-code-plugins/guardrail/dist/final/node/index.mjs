#!/usr/bin/env bun
import{text}from"node:stream/consumers";async function readStdin(){return text(process.stdin)}function writeOutput(output){process.stdout.write(JSON.stringify(output))}function isGeneralPurpose(subagentType){return subagentType===void 0||subagentType===`general-purpose`}const raw=await readStdin(),event=JSON.parse(raw);if(event.tool_name!==`Agent`)writeOutput({});else if(isGeneralPurpose(`subagent_type`in event.tool_input?event.tool_input.subagent_type:void 0))writeOutput({hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:[`Blocked: general-purpose Agent calls are banned due to bugs.`,`Use spawn-claude outside sandbox to launch steerable child Claude Code sessions instead.`,`Example: spawn-claude "your task description here"`,`Specialized agent types (Explore, Plan, etc.) are allowed -- set subagent_type explicitly.`].join(` `)}});else{let resume=`resume`in event.tool_input?event.tool_input.resume:void 0;writeOutput(typeof resume==`string`?{hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:[`Blocked: Agent resume call (agent ID: ${resume}).`,`Background agents notify automatically on completion.`,`Do not poll or resume running agents -- wait for the notification.`,`If you need the result now, use TaskOutput to check the agent's status.`].join(` `)}}:{})}export{};