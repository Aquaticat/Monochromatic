#!/usr/bin/env bun
import{text}from"node:stream/consumers";function isGeneralPurpose(subagentType){return subagentType===void 0||subagentType===`general-purpose`}function guardrailHandler(event){if(event.tool_name!==`Agent`)return{};if(isGeneralPurpose(`subagent_type`in event.tool_input?event.tool_input.subagent_type:void 0))return{hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:[`Blocked: general-purpose Agent calls are banned due to bugs.`,`Use spawn-claude outside sandbox to launch steerable child Claude Code sessions instead.`,`Example: spawn-claude "your task description here"`,`Specialized agent types (Explore, Plan, etc.) are allowed; set subagent_type explicitly.`].join(` `)}};let resume=`resume`in event.tool_input?event.tool_input.resume:void 0;return typeof resume==`string`?{hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:[`Blocked: Agent resume call (agent ID: ${resume}).`,`Background agents notify automatically on completion.`,`Do not poll or resume running agents; wait for the notification.`,`If you need the result now, use TaskOutput to check the agent's status.`].join(` `)}}:{}}function guardrailParser(raw){return JSON.parse(raw)}function guardrailWriter(output){return JSON.stringify(output)}async function runHookPlugin({parser,handler,writer}){let output=await handler(parser(await text(process.stdin)));process.stdout.write(writer(output))}await runHookPlugin({parser:guardrailParser,handler:guardrailHandler,writer:guardrailWriter});export{};