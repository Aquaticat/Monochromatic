#!/usr/bin/env bun
import{text}from"node:stream/consumers";function parseHookJson(raw){return JSON.parse(raw)}async function runHookPlugin({parser,handler,writer}){let output=await handler(parser(await text(process.stdin)));process.stdout.write(writer(output))}function isGeneralPurpose(subagentType){return subagentType===void 0||subagentType===`general-purpose`}function invokesBunTest(command){return/(?:^|[\n;|&(])\s*bun\s+test\b/.test(command)}function guardrailHandler(event){if(event.tool_name===`Bash`){let command=`command`in event.tool_input?event.tool_input.command:void 0;return typeof command==`string`&&invokesBunTest(command)?{hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:["Blocked: `bun test` invocations are banned in this repo.","The custom `@monochromatic-dev/module-test` harness runs tests as a side effect of module import,","so `bun test <file>` reports `0 pass / 0 fail` even when every test passed (the harness's `PASS`",`log lines are not measured by bun's test runner).`,"Use `mise run //packages/<path>:test:unit` instead. When no such task exists, add one to the","target package's `mise.toml` first. For ad-hoc single-file runs use `bun <file>` directly","(no `test` subcommand)."].join(` `)}}:{}}if(event.tool_name!==`Agent`)return{};if(isGeneralPurpose(`subagent_type`in event.tool_input?event.tool_input.subagent_type:void 0))return{hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:[`Blocked: general-purpose Agent calls are banned due to bugs.`,`Use spawn-claude outside sandbox to launch steerable child Claude Code sessions instead.`,`Example: spawn-claude "your task description here"`,`Specialized agent types (Explore, Plan, etc.) are allowed; set subagent_type explicitly.`].join(` `)}};let resume=`resume`in event.tool_input?event.tool_input.resume:void 0;return typeof resume==`string`?{hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:[`Blocked: Agent resume call (agent ID: ${resume}).`,`Background agents notify automatically on completion.`,`Do not poll or resume running agents; wait for the notification.`,`If you need the result now, use TaskOutput to check the agent's status.`].join(` `)}}:{}}function guardrailParser(raw){return parseHookJson(raw)}function guardrailWriter(output){return JSON.stringify(output)}await runHookPlugin({parser:guardrailParser,handler:guardrailHandler,writer:guardrailWriter});export{};