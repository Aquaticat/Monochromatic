#!/usr/bin/env bun
import{text}from"node:stream/consumers";function isDigit(c){return c>=`0`&&c<=`9`}function isLowerAlpha(c){return c>=`a`&&c<=`z`}function isUpperAlpha(c){return c>=`A`&&c<=`Z`}function isAlphaNum(c){return isLowerAlpha(c)||isUpperAlpha(c)||isDigit(c)}function isWordChar(c){return isAlphaNum(c)||c===`_`}function isWhitespace(c){return c===` `||c===`	`||c===`
`||c===`\r`||c===`\f`||c===`\v`}function isGeneralPurpose(subagentType){return subagentType===void 0||subagentType===`general-purpose`}function isCommandBoundary(c){return c===`
`||c===`;`||c===`|`||c===`&`||c===`(`}function invokesBunTest(command){function skipWhitespace(idx){let at=idx;for(;at<command.length&&isWhitespace(command.charAt(at));)at+=1;return at}function matchesAt(pos){let bunStart=skipWhitespace(pos);if(!command.startsWith(`bun`,bunStart))return!1;let afterBun=bunStart+3;if(afterBun>=command.length||!isWhitespace(command.charAt(afterBun)))return!1;let testStart=skipWhitespace(afterBun);if(!command.startsWith(`test`,testStart))return!1;let afterTest=testStart+4;return afterTest>=command.length||!isWordChar(command.charAt(afterTest))}function scanForBoundary(fromIdx){for(let cursorIndex=fromIdx;cursorIndex<command.length;cursorIndex+=1)if(isCommandBoundary(command.charAt(cursorIndex))&&matchesAt(cursorIndex+1))return!0;return!1}return matchesAt(0)||scanForBoundary(0)}function guardrailHandler(event){if(event.tool_name===`Bash`){let command=`command`in event.tool_input?event.tool_input.command:void 0;return typeof command==`string`&&invokesBunTest(command)?{hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:["Blocked: `bun test` invocations are banned in this repo.","The custom `@monochromatic-dev/module-test` harness runs tests as a side effect of module import,","so `bun test <file>` reports `0 pass / 0 fail` even when every test passed (the harness's `PASS`",`log lines are not measured by bun's test runner).`,"Use `mise run //packages/<path>:test:unit` instead. When no such task exists, add one to the","target package's `mise.toml` first. For ad-hoc single-file runs use `bun <file>` directly","(no `test` subcommand)."].join(` `)}}:{}}if(event.tool_name!==`Agent`)return{};if(isGeneralPurpose(`subagent_type`in event.tool_input?event.tool_input.subagent_type:void 0))return{hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:[`Blocked: general-purpose Agent calls are banned due to bugs.`,`Use spawn-claude outside sandbox to launch steerable child Claude Code sessions instead.`,`Example: spawn-claude "your task description here"`,`Specialized agent types (Explore, Plan, etc.) are allowed; set subagent_type explicitly.`].join(` `)}};let resume=`resume`in event.tool_input?event.tool_input.resume:void 0;return typeof resume==`string`?{hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:[`Blocked: Agent resume call (agent ID: ${resume}).`,`Background agents notify automatically on completion.`,`Do not poll or resume running agents; wait for the notification.`,`If you need the result now, use TaskOutput to check the agent's status.`].join(` `)}}:{}}function guardrailParser(raw){return JSON.parse(raw)}function guardrailWriter(output){return JSON.stringify(output)}async function runHookPlugin({parser,handler,writer}){let output=await handler(parser(await text(process.stdin)));process.stdout.write(writer(output))}await runHookPlugin({parser:guardrailParser,handler:guardrailHandler,writer:guardrailWriter});export{};