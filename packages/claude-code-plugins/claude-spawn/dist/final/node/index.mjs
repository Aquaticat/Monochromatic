#!/usr/bin/env bun
import{chmodSync,mkdirSync,readFileSync,readdirSync,renameSync,symlinkSync,unlinkSync,writeFileSync}from"node:fs";import{join,resolve}from"node:path";import{text}from"node:stream/consumers";import{execFileSync}from"node:child_process";function parseHookJson(raw){return JSON.parse(raw)}async function runHookPlugin({parser,handler,writer}){let output=await handler(parser(await text(process.stdin)));process.stdout.write(writer(output))}const SPAWN_RESULTS_DIR=join(process.env.HOME??`/tmp`,`.claude`,`spawn-results`),BY_PID_DIR=join(SPAWN_RESULTS_DIR,`.by-pid`),SPAWNS_DIR=join(SPAWN_RESULTS_DIR,`spawns`);function handleSessionStart({sessionId,transcriptPath,hookDir}){mkdirSync(BY_PID_DIR,{recursive:!0});let mapping={sessionId,transcriptPath};writeFileSync(join(BY_PID_DIR,String(process.ppid)),JSON.stringify(mapping));let spawnId=process.env.CLAUDE_SPAWN_ID;if(spawnId!==void 0){let jsonPath=join(SPAWNS_DIR,`${spawnId}.json`);try{let state=parseHookJson(readFileSync(jsonPath,`utf8`));if(state.sessionId===``){let updated={...state,sessionId,transcriptPath};writeFileSync(jsonPath,JSON.stringify(updated))}}catch{}}let cliWarning=autoSetupCli(hookDir);return cliWarning===null?JSON.stringify({}):cliWarning}function cliIsOnPath(){try{return execFileSync(`which`,[`spawn-claude`],{stdio:`ignore`}),!0}catch{return!1}}function autoSetupCli(hookDir){if(cliIsOnPath())return null;let cliSource=join(resolve(hookDir,`..`,`..`,`..`),`src`,`cli.ts`),localBin=join(process.env.HOME??`/tmp`,`.local`,`bin`),symlinkPath=join(localBin,`spawn-claude`);try{mkdirSync(localBin,{recursive:!0}),chmodSync(cliSource,493);try{unlinkSync(symlinkPath)}catch{}return symlinkSync(cliSource,symlinkPath),(process.env.PATH??``).split(`:`).includes(localBin)?null:[`[claude-spawn] Symlinked spawn-claude to ~/.local/bin/spawn-claude,`,`but ~/.local/bin is not on PATH. Add it to your shell profile:`,`  export PATH="$HOME/.local/bin:$PATH"`].join(`
`)}catch{return[`[claude-spawn] Could not auto-setup spawn-claude CLI.`,`Symlink target: ${cliSource}`,`Symlink path: ${symlinkPath}`,`Create the symlink manually or add the plugin directory to PATH.`].join(`
`)}}function formatSpawnResult(state){return[`Spawned Claude session completed (spawnId: ${state.spawnId}):`,`Session ID: ${state.sessionId}`,`Transcript: ${state.transcriptPath}`,state.lastMessage.length>0?`Last assistant message:\n${state.lastMessage}`:`No assistant message was produced.`].join(`
`)}function checkCompletedChildren({parentSessionId,consume}){let entries=(function(){try{return readdirSync(SPAWNS_DIR)}catch{return null}})();if(entries===null)return null;let results=[];for(let filename of entries){if(!filename.endsWith(`.json`))continue;let filePath=join(SPAWNS_DIR,filename),reportedPath=join(SPAWNS_DIR,filename.replace(/\.json$/,`.reported`));try{let state=parseHookJson(readFileSync(filePath,`utf8`));if(state.parentSessionId!==parentSessionId||state.status!==`stopped`)continue;if(consume)try{renameSync(filePath,reportedPath)}catch{continue}results.push(formatSpawnResult(state))}catch{continue}}return results.length===0?null:results.join(`

---

`)}const HOOK_DIR=import.meta.dir;function updateChildOnStop({sessionId,lastMessage}){let spawnId=process.env.CLAUDE_SPAWN_ID;if(spawnId===void 0||lastMessage===void 0)return;let filePath=join(SPAWNS_DIR,`${spawnId}.json`);try{let existing=readFileSync(filePath,`utf8`),state=JSON.parse(existing);if(state.sessionId===sessionId){let updated={...state,lastMessage,status:`stopped`};writeFileSync(filePath,JSON.stringify(updated))}}catch{}}function stopResponse(event){if(!event.stop_hook_active){let context=checkCompletedChildren({parentSessionId:event.session_id,consume:!0});if(context!==null)return{kind:`json`,payload:{decision:`block`,reason:context}}}return{kind:`json`,payload:{}}}function additionalContextResponse(event){let context=checkCompletedChildren({parentSessionId:event.session_id,consume:!0});return context===null?{kind:`json`,payload:{}}:{kind:`json`,payload:{hookSpecificOutput:{hookEventName:event.hook_event_name,additionalContext:context}}}}function claudeSpawnHandler(event){return event.hook_event_name===`SessionStart`?{kind:`raw`,text:handleSessionStart({sessionId:event.session_id,transcriptPath:event.transcript_path,hookDir:HOOK_DIR})}:event.hook_event_name===`Stop`?(updateChildOnStop({sessionId:event.session_id,lastMessage:event.last_assistant_message}),stopResponse(event)):event.hook_event_name===`SessionEnd`?{kind:`json`,payload:{}}:additionalContextResponse(event)}function claudeSpawnParser(raw){return parseHookJson(raw)}function claudeSpawnWriter(output){return output.kind===`raw`?output.text:JSON.stringify(output.payload)}await runHookPlugin({parser:claudeSpawnParser,handler:claudeSpawnHandler,writer:claudeSpawnWriter});export{};