#!/usr/bin/env bun
import{execFileSync}from"node:child_process";import{chmodSync,mkdirSync,readFileSync,readdirSync,renameSync,symlinkSync,unlinkSync,writeFileSync}from"node:fs";import{join,resolve}from"node:path";import{text}from"node:stream/consumers";async function readStdin(){return text(process.stdin)}const SPAWN_RESULTS_DIR=join(process.env.HOME??`/tmp`,`.claude`,`spawn-results`),BY_PID_DIR=join(SPAWN_RESULTS_DIR,`.by-pid`),SPAWNS_DIR=join(SPAWN_RESULTS_DIR,`spawns`);function formatSpawnResult(state){return[`Spawned Claude session completed (spawnId: ${state.spawnId}):`,`Session ID: ${state.sessionId}`,`Transcript: ${state.transcriptPath}`,state.lastMessage.length>0?`Last assistant message:\n${state.lastMessage}`:`No assistant message was produced.`].join(`
`)}function checkCompletedChildren({parentSessionId,consume}){let entries=[];try{entries=readdirSync(SPAWNS_DIR)}catch{return null}let results=[];for(let filename of entries){if(!filename.endsWith(`.json`))continue;let filePath=join(SPAWNS_DIR,filename),reportedPath=join(SPAWNS_DIR,filename.replace(/\.json$/,`.reported`));try{let raw=readFileSync(filePath,`utf8`),state=JSON.parse(raw);if(state.parentSessionId!==parentSessionId||state.status!==`stopped`)continue;if(consume)try{renameSync(filePath,reportedPath)}catch{continue}results.push(formatSpawnResult(state))}catch{continue}}return results.length===0?null:results.join(`

---

`)}const raw=await readStdin(),event=JSON.parse(raw);if(event.hook_event_name===`SessionStart`){mkdirSync(BY_PID_DIR,{recursive:!0});let mapping={sessionId:event.session_id,transcriptPath:event.transcript_path};writeFileSync(join(BY_PID_DIR,String(process.ppid)),JSON.stringify(mapping));let spawnId=process.env.CLAUDE_SPAWN_ID;if(spawnId!==void 0){let jsonPath=join(SPAWNS_DIR,`${spawnId}.json`);try{let raw=readFileSync(jsonPath,`utf8`),state=JSON.parse(raw);if(state.sessionId===``){let updated={...state,sessionId:event.session_id,transcriptPath:event.transcript_path};writeFileSync(jsonPath,JSON.stringify(updated))}}catch{}}let cliWarning=null,cliOnPath=!1;try{execFileSync(`which`,[`spawn-claude`],{stdio:`ignore`}),cliOnPath=!0}catch{}if(!cliOnPath){let cliSource=join(resolve(import.meta.dir,`..`,`..`,`..`),`src`,`cli.ts`),localBin=join(process.env.HOME??`/tmp`,`.local`,`bin`),symlinkPath=join(localBin,`spawn-claude`);try{mkdirSync(localBin,{recursive:!0}),chmodSync(cliSource,493);try{unlinkSync(symlinkPath)}catch{}symlinkSync(cliSource,symlinkPath),cliWarning=(process.env.PATH??``).split(`:`).includes(localBin)?null:[`[claude-spawn] Symlinked spawn-claude to ~/.local/bin/spawn-claude,`,`but ~/.local/bin is not on PATH. Add it to your shell profile:`,`  export PATH="$HOME/.local/bin:$PATH"`].join(`
`)}catch{cliWarning=[`[claude-spawn] Could not auto-setup spawn-claude CLI.`,`Symlink target: ${cliSource}`,`Symlink path: ${symlinkPath}`,`Create the symlink manually or add the plugin directory to PATH.`].join(`
`)}}cliWarning===null?process.stdout.write(JSON.stringify({})):process.stdout.write(cliWarning)}else if(event.hook_event_name===`Stop`){let spawnId=process.env.CLAUDE_SPAWN_ID;if(spawnId!==void 0&&event.last_assistant_message!==void 0){let filePath=join(SPAWNS_DIR,`${spawnId}.json`);try{let existing=readFileSync(filePath,`utf8`),state=JSON.parse(existing);if(state.sessionId===event.session_id){let updated={...state,lastMessage:event.last_assistant_message,status:`stopped`};writeFileSync(filePath,JSON.stringify(updated))}}catch{}}if(!event.stop_hook_active){let context=checkCompletedChildren({parentSessionId:event.session_id,consume:!0});if(context!==null){let output={decision:`block`,reason:context};process.stdout.write(JSON.stringify(output)),process.exit(0)}}process.stdout.write(JSON.stringify({}))}else if(event.hook_event_name===`SessionEnd`)process.stdout.write(JSON.stringify({}));else{let context=checkCompletedChildren({parentSessionId:event.session_id,consume:!0});if(context!==null){let output={hookSpecificOutput:{hookEventName:event.hook_event_name,additionalContext:context}};process.stdout.write(JSON.stringify(output))}else process.stdout.write(JSON.stringify({}))}export{};