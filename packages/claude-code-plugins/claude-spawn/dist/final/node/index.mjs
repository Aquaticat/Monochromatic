#!/usr/bin/env bun
import{mkdirSync,readFileSync,readdirSync,renameSync,writeFileSync}from"node:fs";import{join}from"node:path";import{text}from"node:stream/consumers";async function readStdin(){return text(process.stdin)}const SPAWN_RESULTS_DIR=join(process.env.HOME??`/tmp`,`.claude`,`spawn-results`),BY_PID_DIR=join(SPAWN_RESULTS_DIR,`.by-pid`),SPAWNS_DIR=join(SPAWN_RESULTS_DIR,`spawns`);function checkCompletedChildren({parentSessionId}){let entries;try{entries=readdirSync(SPAWNS_DIR)}catch{return null}let results=[];for(let filename of entries){if(!filename.endsWith(`.json`))continue;let filePath=join(SPAWNS_DIR,filename),reportedPath=join(SPAWNS_DIR,filename.replace(/\.json$/,`.reported`)),state;try{let raw=readFileSync(filePath,`utf8`);state=JSON.parse(raw)}catch{continue}if(state.parentSessionId===parentSessionId&&state.status===`stopped`){try{renameSync(filePath,reportedPath)}catch{continue}results.push([`Spawned Claude session completed (spawnId: ${state.spawnId}):`,`Session ID: ${state.sessionId}`,`Transcript: ${state.transcriptPath}`,state.lastMessage.length>0?`Last assistant message:\n${state.lastMessage}`:`No assistant message was produced.`].join(`
`))}}return results.length===0?null:results.join(`

---

`)}const raw=await readStdin(),event=JSON.parse(raw);if(event.hook_event_name===`SessionStart`){mkdirSync(BY_PID_DIR,{recursive:!0});let mapping={sessionId:event.session_id,transcriptPath:event.transcript_path};writeFileSync(join(BY_PID_DIR,String(process.ppid)),JSON.stringify(mapping));let spawnId=process.env.CLAUDE_SPAWN_ID,parentSessionId=process.env.CLAUDE_SPAWNED_BY_SESSION;if(spawnId!==void 0&&parentSessionId!==void 0){mkdirSync(SPAWNS_DIR,{recursive:!0});let state={spawnId,sessionId:event.session_id,transcriptPath:event.transcript_path,parentSessionId,status:`running`,lastMessage:``};writeFileSync(join(SPAWNS_DIR,`${spawnId}.json`),JSON.stringify(state))}let context=checkCompletedChildren({parentSessionId:event.session_id}),output=context===null?{}:{hookSpecificOutput:{hookEventName:`SessionStart`,additionalContext:context}};process.stdout.write(JSON.stringify(output))}else if(event.hook_event_name===`Stop`){let spawnId=process.env.CLAUDE_SPAWN_ID;if(spawnId!==void 0&&event.last_assistant_message!==void 0){let filePath=join(SPAWNS_DIR,`${spawnId}.json`);try{let existing=readFileSync(filePath,`utf8`),updated={...JSON.parse(existing),lastMessage:event.last_assistant_message,status:`stopped`};writeFileSync(filePath,JSON.stringify(updated))}catch{}}process.stdout.write(JSON.stringify({}))}else if(event.hook_event_name===`SessionEnd`)process.stdout.write(JSON.stringify({}));else{let context=checkCompletedChildren({parentSessionId:event.session_id});if(context!==null){let output={hookSpecificOutput:{hookEventName:event.hook_event_name,additionalContext:context}};process.stdout.write(JSON.stringify(output))}else process.stdout.write(JSON.stringify({}))}export{};