#!/usr/bin/env node
import{text}from"node:stream/consumers";function padTwoDigits(value){return value.toString().padStart(2,`0`)}function formatTimeContext(now){return`<time>${padTwoDigits(now.getHours())}:${padTwoDigits(now.getMinutes())}</time>`}function promptTimeHandler(_event){return{hookSpecificOutput:{hookEventName:`UserPromptSubmit`,additionalContext:formatTimeContext(new Date)}}}function promptTimeParser(raw){return JSON.parse(raw)}function promptTimeWriter(output){return JSON.stringify(output)}const NO_STDOUT=Symbol(`claude-code-plugin-runtime/hook-intentionally-emits-no-stdout-bytes`);async function runHookPlugin({parser,handler,writer}){let serialized=writer(await handler(parser(await text(process.stdin))));serialized!==NO_STDOUT&&process.stdout.write(serialized)}await runHookPlugin({parser:promptTimeParser,handler:promptTimeHandler,writer:promptTimeWriter});export{};