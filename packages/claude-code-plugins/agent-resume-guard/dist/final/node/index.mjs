#!/usr/bin/env bun
import{text}from"node:stream/consumers";async function readStdin(){return text(process.stdin)}function writeOutput(output){process.stdout.write(JSON.stringify(output))}const raw=await readStdin(),event=JSON.parse(raw);if(event.tool_name!==`Agent`)writeOutput({});else{let agentInput=event.tool_input;agentInput.resume!==null&&agentInput.resume!==void 0?writeOutput({hookSpecificOutput:{hookEventName:`PreToolUse`,permissionDecision:`deny`,permissionDecisionReason:[`Blocked: Agent resume call (agent ID: ${agentInput.resume}).`,`Background agents notify automatically on completion.`,`Do not poll or resume running agents -- wait for the notification.`,`If you need the result now, use TaskOutput to check the agent's status.`].join(` `)}}):writeOutput({})}export{};