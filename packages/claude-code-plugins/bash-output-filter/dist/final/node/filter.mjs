#!/usr/bin/env bun
import{text}from"node:stream/consumers";import{realpathSync}from"node:fs";const HOME_DIR=process.env.HOME??``,REAL_HOME_DIR=(function(){try{if(HOME_DIR===``)return``;let resolved=realpathSync(HOME_DIR);return resolved===HOME_DIR?``:resolved}catch{return``}})(),CWD_PREFIX=(function(){try{let cwd=process.cwd();return cwd.endsWith(`/`)?cwd:`${cwd}/`}catch{return``}})(),ALT_CWD_PREFIX=(function(){return CWD_PREFIX===``||REAL_HOME_DIR===``||HOME_DIR===``?``:CWD_PREFIX.startsWith(`${REAL_HOME_DIR}/`)?`${HOME_DIR}${CWD_PREFIX.slice(REAL_HOME_DIR.length)}`:CWD_PREFIX.startsWith(`${HOME_DIR}/`)?`${REAL_HOME_DIR}${CWD_PREFIX.slice(HOME_DIR.length)}`:``})(),GIT_FILE_MODE_PATTERN=/^ (create|delete|copy|rename|mode change) mode /,GIT_TRANSPORT_PROGRESS_PATTERNS=[/^Enumerating objects:/,/^Counting objects:/,/^Compressing objects:/,/^Delta compression/,/^Writing objects:/,/^Total \d+/,/^Resolving deltas:/,/^Unpacking objects:/,/^remote: (Enumerating|Counting|Compressing|Resolving|Writing|Total|Unpacking)/],SANDBOX_NOISE_PATTERNS=[/^\s*(\[.*?\]\s+)?mise WARN\s+failed to write cache file:.*Read-only file system/];function shouldStripLine(line){return GIT_FILE_MODE_PATTERN.test(line)||SANDBOX_NOISE_PATTERNS.some(function(pattern){return pattern.test(line)})?!0:GIT_TRANSPORT_PROGRESS_PATTERNS.some(function(pattern){return pattern.test(line)})}function collapseRepeatedChars(line){return line.replaceAll(/([^\w\s])\1{9,}/g,function(match,char){return`${char.repeat(3)} (x${match.length})`})}function collapseCwdPaths(line){if(CWD_PREFIX===``)return line;let result=line;return ALT_CWD_PREFIX===``?result=result.replaceAll(CWD_PREFIX,``):ALT_CWD_PREFIX.length>=CWD_PREFIX.length?(result=result.replaceAll(ALT_CWD_PREFIX,``),result=result.replaceAll(CWD_PREFIX,``)):(result=result.replaceAll(CWD_PREFIX,``),result=result.replaceAll(ALT_CWD_PREFIX,``)),result}function collapseHomePaths(line){if(HOME_DIR===``)return line;let result=line;return REAL_HOME_DIR===``?result=result.replaceAll(HOME_DIR,`~`):REAL_HOME_DIR.length>=HOME_DIR.length?(result=result.replaceAll(REAL_HOME_DIR,`~`),result=result.replaceAll(HOME_DIR,`~`)):(result=result.replaceAll(HOME_DIR,`~`),result=result.replaceAll(REAL_HOME_DIR,`~`)),result}function truncateLine(line){return line.length<=500?line:`${line.slice(0,500)}... [${line.length} chars]`}function flushRepeated({result,line,count}){if(count!==0)if(count>=3)result.push(`${line} (x${count})`);else for(let i=0;i<count;i++)result.push(line)}function filterOutput(input){let lines=input.split(`
`),result=[],prevLine=``,repeatCount=0;for(let rawLine of lines){let trimmed=rawLine.trimEnd();if(shouldStripLine(trimmed))continue;let processed=truncateLine(collapseHomePaths(collapseCwdPaths(collapseRepeatedChars(trimmed))));processed===prevLine&&repeatCount>0?repeatCount++:(flushRepeated({result,line:prevLine,count:repeatCount}),prevLine=processed,repeatCount=1)}return flushRepeated({result,line:prevLine,count:repeatCount}),result.join(`
`)}try{let filtered=filterOutput(await text(process.stdin));process.stdout.write(filtered)}catch{try{let fallback=await text(process.stdin);process.stdout.write(fallback)}catch{}}export{};