#!/usr/bin/env node
import{text}from"node:stream/consumers";import{realpath}from"node:fs/promises";function isDigit(c){return c>=`0`&&c<=`9`}function isLowerAlpha(c){return c>=`a`&&c<=`z`}function isUpperAlpha(c){return c>=`A`&&c<=`Z`}function isAlphaNum(c){return isLowerAlpha(c)||isUpperAlpha(c)||isDigit(c)}function isWordChar(c){return isAlphaNum(c)||c===`_`}function isWhitespace(c){return c===` `||c===`	`||c===`
`||c===`\r`||c===`\f`||c===`\v`}const PATH_PREFIX_ABSENT=Symbol(`bash-output-filter/path-prefix-absent`),HOME_DIR=process.env.HOME??PATH_PREFIX_ABSENT;function withTrailingSlash(path){return path.endsWith(`/`)?path:`${path}/`}async function resolveRealHome(){if(HOME_DIR===PATH_PREFIX_ABSENT)return PATH_PREFIX_ABSENT;try{let resolved=await realpath(HOME_DIR);return resolved===HOME_DIR?PATH_PREFIX_ABSENT:resolved}catch{return PATH_PREFIX_ABSENT}}const REAL_HOME_DIR=await resolveRealHome();function resolveCwdPrefix(){try{return withTrailingSlash(process.cwd())}catch{return PATH_PREFIX_ABSENT}}const CWD_PREFIX=resolveCwdPrefix();function resolveAltCwdPrefix(){return CWD_PREFIX===PATH_PREFIX_ABSENT||REAL_HOME_DIR===PATH_PREFIX_ABSENT||HOME_DIR===PATH_PREFIX_ABSENT?PATH_PREFIX_ABSENT:CWD_PREFIX.startsWith(`${REAL_HOME_DIR}/`)?`${HOME_DIR}${CWD_PREFIX.slice(REAL_HOME_DIR.length)}`:CWD_PREFIX.startsWith(`${HOME_DIR}/`)?`${REAL_HOME_DIR}${CWD_PREFIX.slice(HOME_DIR.length)}`:PATH_PREFIX_ABSENT}const ALT_CWD_PREFIX=resolveAltCwdPrefix(),GIT_FILE_MODE_VERBS=[`create`,`delete`,`copy`,`rename`,`mode change`];function isGitFileModeLine(line){return GIT_FILE_MODE_VERBS.some(function(verb){return line.startsWith(` ${verb} mode `)})}const GIT_TRANSPORT_PROGRESS_PREFIXES=[`Enumerating objects:`,`Counting objects:`,`Compressing objects:`,`Delta compression`,`Writing objects:`,`Resolving deltas:`,`Unpacking objects:`];function hasGitTransportPrefix(line){return GIT_TRANSPORT_PROGRESS_PREFIXES.some(function(prefix){return line.startsWith(prefix)})}function isTotalProgressLine(line){return line.startsWith(`Total `)?isDigit(line.charAt(6)):!1}const REMOTE_PROGRESS_VERBS=[`Enumerating`,`Counting`,`Compressing`,`Resolving`,`Writing`,`Total`,`Unpacking`];function isRemoteProgressLine(line){if(!line.startsWith(`remote: `))return!1;let rest=line.slice(8);return REMOTE_PROGRESS_VERBS.some(function(verb){return rest.startsWith(verb)})}const GIT_TRANSPORT_PROGRESS_PREDICATES=[hasGitTransportPrefix,isTotalProgressLine,isRemoteProgressLine],MISE_WARN_TOKEN=`mise WARN`;function isSandboxMiseCacheNoise(line){function skipWs(idx){let at=idx;for(;at<line.length&&isWhitespace(line.charAt(at));)at+=1;return at}let afterLeadingWs=skipWs(0);function maybeSkipBracket(idx){if(line.charAt(idx)!==`[`)return idx;let closeIdx=line.indexOf(`]`,idx+1);if(closeIdx===-1)return idx;let afterBracket=closeIdx+1;return afterBracket>=line.length||!isWhitespace(line.charAt(afterBracket))?idx:skipWs(afterBracket)}let afterBracket=maybeSkipBracket(afterLeadingWs);if(!line.startsWith(MISE_WARN_TOKEN,afterBracket))return!1;let afterToken=afterBracket+9,afterTokenWs=skipWs(afterToken);if(afterTokenWs===afterToken||!line.startsWith(`failed to write cache file:`,afterTokenWs))return!1;let afterFailed=afterTokenWs+27;return line.slice(afterFailed).includes(`Read-only file system`)}const SANDBOX_NOISE_PREDICATES=[isSandboxMiseCacheNoise];function isMiseBootstrapEchoLine(line){return line.startsWith(`[//:bootstrap] $`)}const MISE_TOOLS_SUMMARY_LINES=new Set([`mise all tools are installed`,`mise All tools are up to date`]);function isMiseToolsSummaryLine(line){return MISE_TOOLS_SUMMARY_LINES.has(line)}function isMiseMinReleaseAgeWarn(line){return line.includes(MISE_WARN_TOKEN)&&line.includes(`ignored by minimum_release_age`)}const MISE_NOISE_PREDICATES=[isMiseBootstrapEchoLine,isMiseToolsSummaryLine,isMiseMinReleaseAgeWarn],STRIP_LINE_PREDICATES=[isGitFileModeLine,...GIT_TRANSPORT_PROGRESS_PREDICATES,...SANDBOX_NOISE_PREDICATES,...MISE_NOISE_PREDICATES];function shouldStripLine(line){return STRIP_LINE_PREDICATES.some(function(predicate){return predicate(line)})}function isCollapseCandidate(c){return!isWordChar(c)&&!isWhitespace(c)}function collapseRepeatedChars(line){let parts=[];for(let cursorIndex=0;cursorIndex<line.length;){let c=line.charAt(cursorIndex);if(!isCollapseCandidate(c)){parts.push(c),cursorIndex+=1;continue}let runEnd=cursorIndex+1;for(;runEnd<line.length&&line.charAt(runEnd)===c;)runEnd+=1;let runLength=runEnd-cursorIndex;runLength>=10?parts.push(`${c.repeat(3)} (x${runLength} repeated characters)`):parts.push(line.slice(cursorIndex,runEnd)),cursorIndex=runEnd}return parts.join(``)}function hasPathPrefix(prefix){return prefix!==PATH_PREFIX_ABSENT}function collapseCwdPaths(line){if(!hasPathPrefix(CWD_PREFIX))return line;if(hasPathPrefix(ALT_CWD_PREFIX))if(ALT_CWD_PREFIX.length>=CWD_PREFIX.length){if(line.startsWith(ALT_CWD_PREFIX))return line.slice(ALT_CWD_PREFIX.length);if(line.startsWith(CWD_PREFIX))return line.slice(CWD_PREFIX.length)}else{if(line.startsWith(CWD_PREFIX))return line.slice(CWD_PREFIX.length);if(line.startsWith(ALT_CWD_PREFIX))return line.slice(ALT_CWD_PREFIX.length)}else if(line.startsWith(CWD_PREFIX))return line.slice(CWD_PREFIX.length);return line}function collapseHomePaths(line){if(!hasPathPrefix(HOME_DIR))return line;if(hasPathPrefix(REAL_HOME_DIR))if(REAL_HOME_DIR.length>=HOME_DIR.length){if(line.startsWith(REAL_HOME_DIR))return`~${line.slice(REAL_HOME_DIR.length)}`;if(line.startsWith(HOME_DIR))return`~${line.slice(HOME_DIR.length)}`}else{if(line.startsWith(HOME_DIR))return`~${line.slice(HOME_DIR.length)}`;if(line.startsWith(REAL_HOME_DIR))return`~${line.slice(REAL_HOME_DIR.length)}`}else if(line.startsWith(HOME_DIR))return`~${line.slice(HOME_DIR.length)}`;return line}function truncateLine(line){return line.length<=500?line:`${line.slice(0,500)}... [${line.length} chars]`}function flushRepeated({line,count}){return count===0?[]:count>=3?[`${line} (x${count} repeated lines)`]:Array.from({length:count},function(){return line})}const STDIN_UNAVAILABLE=Symbol(`bash-output-filter/stdin-stream-could-not-be-read`);function filterOutput(input){return collapseLines(input.split(`
`)).join(`
`)}function collapseLines(lines){let result=[],prevLine=``,repeatCount=0;for(let rawLine of lines){let trimmed=rawLine.trimEnd();if(shouldStripLine(trimmed))continue;let processed=truncateLine(collapseHomePaths(collapseCwdPaths(collapseRepeatedChars(trimmed))));processed===prevLine&&repeatCount>0?repeatCount++:(result.push(...flushRepeated({line:prevLine,count:repeatCount})),prevLine=processed,repeatCount=1)}return result.push(...flushRepeated({line:prevLine,count:repeatCount})),result}async function readStdin(){try{return await text(process.stdin)}catch{return STDIN_UNAVAILABLE}}async function runFilter(){let input=await readStdin();if(input!==STDIN_UNAVAILABLE)try{let filtered=filterOutput(input);process.stdout.write(filtered)}catch{process.stdout.write(input)}}await runFilter();export{};