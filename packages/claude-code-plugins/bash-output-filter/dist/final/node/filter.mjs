#!/usr/bin/env bun
import{a as isDigit,o as isWhitespace,s as isWordChar}from"./text-scan-CYPNafuL.mjs";import{text}from"node:stream/consumers";import{realpathSync}from"node:fs";const HOME_DIR=process.env.HOME??``,REAL_HOME_DIR=(function(){try{if(HOME_DIR===``)return``;let resolved=realpathSync(HOME_DIR);return resolved===HOME_DIR?``:resolved}catch{return``}})(),CWD_PREFIX=(function(){try{let cwd=process.cwd();return cwd.endsWith(`/`)?cwd:`${cwd}/`}catch{return``}})(),ALT_CWD_PREFIX=(function(){return CWD_PREFIX===``||REAL_HOME_DIR===``||HOME_DIR===``?``:CWD_PREFIX.startsWith(`${REAL_HOME_DIR}/`)?`${HOME_DIR}${CWD_PREFIX.slice(REAL_HOME_DIR.length)}`:CWD_PREFIX.startsWith(`${HOME_DIR}/`)?`${REAL_HOME_DIR}${CWD_PREFIX.slice(HOME_DIR.length)}`:``})(),GIT_FILE_MODE_VERBS=[`create`,`delete`,`copy`,`rename`,`mode change`];function isGitFileModeLine(line){return GIT_FILE_MODE_VERBS.some(function(verb){return line.startsWith(` ${verb} mode `)})}const GIT_TRANSPORT_PROGRESS_PREFIXES=[`Enumerating objects:`,`Counting objects:`,`Compressing objects:`,`Delta compression`,`Writing objects:`,`Resolving deltas:`,`Unpacking objects:`];function hasGitTransportPrefix(line){return GIT_TRANSPORT_PROGRESS_PREFIXES.some(function(prefix){return line.startsWith(prefix)})}function isTotalProgressLine(line){return line.startsWith(`Total `)?isDigit(line.charAt(6)):!1}const REMOTE_PROGRESS_VERBS=[`Enumerating`,`Counting`,`Compressing`,`Resolving`,`Writing`,`Total`,`Unpacking`];function isRemoteProgressLine(line){if(!line.startsWith(`remote: `))return!1;let rest=line.slice(8);return REMOTE_PROGRESS_VERBS.some(function(verb){return rest.startsWith(verb)})}const GIT_TRANSPORT_PROGRESS_PREDICATES=[hasGitTransportPrefix,isTotalProgressLine,isRemoteProgressLine];function isSandboxMiseCacheNoise(line){function skipWs(idx){let at=idx;for(;at<line.length&&isWhitespace(line.charAt(at));)at+=1;return at}let afterLeadingWs=skipWs(0);function maybeSkipBracket(idx){if(line.charAt(idx)!==`[`)return idx;let closeIdx=line.indexOf(`]`,idx+1);if(closeIdx===-1)return idx;let afterBracket=closeIdx+1;return afterBracket>=line.length||!isWhitespace(line.charAt(afterBracket))?idx:skipWs(afterBracket)}let afterBracket=maybeSkipBracket(afterLeadingWs);if(!line.startsWith(`mise WARN`,afterBracket))return!1;let afterToken=afterBracket+9,afterTokenWs=skipWs(afterToken);if(afterTokenWs===afterToken||!line.startsWith(`failed to write cache file:`,afterTokenWs))return!1;let afterFailed=afterTokenWs+27;return line.slice(afterFailed).includes(`Read-only file system`)}const SANDBOX_NOISE_PREDICATES=[isSandboxMiseCacheNoise];function shouldStripLine(line){return isGitFileModeLine(line)||SANDBOX_NOISE_PREDICATES.some(function(predicate){return predicate(line)})?!0:GIT_TRANSPORT_PROGRESS_PREDICATES.some(function(predicate){return predicate(line)})}function isCollapseCandidate(c){return!isWordChar(c)&&!isWhitespace(c)}function collapseRepeatedChars(line){let parts=[];for(let idx=0;idx<line.length;){let c=line.charAt(idx);if(!isCollapseCandidate(c)){parts.push(c),idx+=1;continue}let runEnd=idx+1;for(;runEnd<line.length&&line.charAt(runEnd)===c;)runEnd+=1;let runLength=runEnd-idx;runLength>=10?parts.push(`${c.repeat(3)} (x${runLength} repeated characters)`):parts.push(line.slice(idx,runEnd)),idx=runEnd}return parts.join(``)}function collapseCwdPaths(line){if(CWD_PREFIX===``)return line;if(ALT_CWD_PREFIX!==``)if(ALT_CWD_PREFIX.length>=CWD_PREFIX.length){if(line.startsWith(ALT_CWD_PREFIX))return line.slice(ALT_CWD_PREFIX.length);if(line.startsWith(CWD_PREFIX))return line.slice(CWD_PREFIX.length)}else{if(line.startsWith(CWD_PREFIX))return line.slice(CWD_PREFIX.length);if(line.startsWith(ALT_CWD_PREFIX))return line.slice(ALT_CWD_PREFIX.length)}else if(line.startsWith(CWD_PREFIX))return line.slice(CWD_PREFIX.length);return line}function collapseHomePaths(line){if(HOME_DIR===``)return line;if(REAL_HOME_DIR!==``)if(REAL_HOME_DIR.length>=HOME_DIR.length){if(line.startsWith(REAL_HOME_DIR))return`~${line.slice(REAL_HOME_DIR.length)}`;if(line.startsWith(HOME_DIR))return`~${line.slice(HOME_DIR.length)}`}else{if(line.startsWith(HOME_DIR))return`~${line.slice(HOME_DIR.length)}`;if(line.startsWith(REAL_HOME_DIR))return`~${line.slice(REAL_HOME_DIR.length)}`}else if(line.startsWith(HOME_DIR))return`~${line.slice(HOME_DIR.length)}`;return line}function truncateLine(line){return line.length<=500?line:`${line.slice(0,500)}... [${line.length} chars]`}function flushRepeated({line,count}){return count===0?[]:count>=3?[`${line} (x${count} repeated lines)`]:Array.from({length:count},function(){return line})}function filterOutput(input){return collapseLines(input.split(`
`)).join(`
`)}function collapseLines(lines){let result=[],prevLine=``,repeatCount=0;for(let rawLine of lines){let trimmed=rawLine.trimEnd();if(shouldStripLine(trimmed))continue;let processed=truncateLine(collapseHomePaths(collapseCwdPaths(collapseRepeatedChars(trimmed))));processed===prevLine&&repeatCount>0?repeatCount++:(result.push(...flushRepeated({line:prevLine,count:repeatCount})),prevLine=processed,repeatCount=1)}return result.push(...flushRepeated({line:prevLine,count:repeatCount})),result}async function runFilter(){try{let filtered=filterOutput(await text(process.stdin));process.stdout.write(filtered)}catch{try{let fallback=await text(process.stdin);process.stdout.write(fallback)}catch{}}}await runFilter();export{};