#!/usr/bin/env bun
import{a as isWhitespace,n as containsWordBoundedPhrase,r as isAlphaNum,s as splitWhitespace,t as containsAnyOfWordBounded}from"./text-scan-diyAklN7.mjs";import{text}from"node:stream/consumers";function parseHookJson(raw){return JSON.parse(raw)}async function runHookPlugin({parser,handler,writer}){let output=await handler(parser(await text(process.stdin)));process.stdout.write(writer(output))}function startsWithSafeChar(cmd){if(cmd.length===0)return!1;let c=cmd.charAt(0);return isAlphaNum(c)||`_/.~"'-`.includes(c)}const ALLOW_PREDICATES=[startsWithSafeChar];function isAllowed(command){return ALLOW_PREDICATES.some(function(predicate){return predicate(command)})}const BINARY_TOOL_NAMES=[`xxd`,`hexdump`,`od`,`base64`,`tar`,`gzip`,`gunzip`,`zip`,`unzip`,`bzip2`,`xz`,`zstd`];function hasBinaryTool(cmd){return containsAnyOfWordBounded({haystack:cmd,phrases:BINARY_TOOL_NAMES})!==void 0}function hasFileRedirect(cmd){function check(fromIdx){let gtIdx=cmd.indexOf(`>`,fromIdx);if(gtIdx===-1)return!1;function skipWs(at){return at>=cmd.length||!isWhitespace(cmd.charAt(at))?at:skipWs(at+1)}let afterWs=skipWs(gtIdx+1);if(afterWs<cmd.length){let next=cmd.charAt(afterWs);if(next!==`|`&&next!==`&`&&next!==`;`&&!isWhitespace(next))return!0}return check(gtIdx+1)}return check(0)}function invokesFilterScript(cmd){return containsWordBoundedPhrase({haystack:cmd,phrase:`filter.mjs`})||containsWordBoundedPhrase({haystack:cmd,phrase:`filter.ts`})}function hasBofMarker(cmd){return cmd.includes(`___BOF_EC:`)}function endsWithBackgroundOp(cmd){return cmd.trimEnd().endsWith(`&`)}const DETACH_WRAPPER_NAMES=[`nohup`,`setsid`];function hasDetachWrapper(cmd){return containsAnyOfWordBounded({haystack:cmd,phrases:DETACH_WRAPPER_NAMES})!==void 0}const CONTAINER_RUNTIMES=new Set([`docker`,`podman`]),CONTAINER_TTY_SUBCOMMANDS=new Set([`exec`,`run`]);function isTtyFlag(token){if(!token.startsWith(`-`)||token.length<2)return!1;let body=token.slice(1);for(let c of body)if(c<`a`||c>`z`)return!1;let last=body.at(-1)??``;return last===`i`||last===`t`}function hasTtyContainerInvoke(cmd){let tokens=splitWhitespace(cmd);function check(idx){if(idx+1>=tokens.length)return!1;let runtime=tokens[idx]??``,sub=tokens[idx+1]??``;if(CONTAINER_RUNTIMES.has(runtime)&&CONTAINER_TTY_SUBCOMMANDS.has(sub)){for(let t of tokens.slice(idx+2))if(isTtyFlag(t))return!0}return check(idx+1)}return check(0)}function hasBunBuild(cmd){let tokens=splitWhitespace(cmd);function check(idx){return idx+1>=tokens.length?!1:tokens[idx]===`bun`&&tokens[idx+1]===`build`?!0:check(idx+1)}return check(0)}function hasDollarParen(cmd){return cmd.includes(`$(`)}function hasBacktickPair(cmd){let first=cmd.indexOf("`");if(first===-1)return!1;let second=cmd.indexOf("`",first+1);return second===-1?!1:second>first+1}function hasProcessSubstitution(cmd){return cmd.includes(`<(`)||cmd.includes(`>(`)}function hasHeredoc(cmd){function check(fromIdx){let idx=cmd.indexOf(`<<`,fromIdx);if(idx===-1)return!1;let afterOpener=cmd.charAt(idx+2),afterMarker=afterOpener===`<`||afterOpener===`-`?idx+2+1:idx+2;function skipWs(at){return at>=cmd.length||!isWhitespace(cmd.charAt(at))?at:skipWs(at+1)}let afterWs=skipWs(afterMarker);return afterWs<cmd.length&&!isWhitespace(cmd.charAt(afterWs))?!0:check(idx+1)}return check(0)}const STATE_BUILTIN_NAMES=[`cd`,`pushd`,`popd`,`export`,`unset`,`source`];function hasStateBuiltin(cmd){return containsAnyOfWordBounded({haystack:cmd,phrases:STATE_BUILTIN_NAMES})!==void 0}function isSourceShorthand(cmd){return cmd.length>=2&&cmd.startsWith(`.`)&&isWhitespace(cmd.charAt(1))}function hasEval(cmd){return containsWordBoundedPhrase({haystack:cmd,phrase:`eval`})}const SKIP_PREDICATES=[hasBinaryTool,hasFileRedirect,invokesFilterScript,hasBofMarker,endsWithBackgroundOp,hasDetachWrapper,hasTtyContainerInvoke,hasBunBuild,hasDollarParen,hasBacktickPair,hasProcessSubstitution,hasHeredoc,hasStateBuiltin,isSourceShorthand,hasEval];function shouldSkip(command){return SKIP_PREDICATES.some(function(predicate){return predicate(command)})}function isBashToolInput(input){return typeof input.command==`string`}function bashOutputFilterHandler(event){if(event.tool_name!==`Bash`||!isBashToolInput(event.tool_input))return{};let bashInput=event.tool_input;if(!isAllowed(bashInput.command)||shouldSkip(bashInput.command))return{};let filterPath=import.meta.url.endsWith(`.mjs`)?`${import.meta.dirname}/filter.mjs`:`${import.meta.dirname}/filter.ts`,wrappedCommand=`set -o pipefail && ${bashInput.command} 2>&1 | bun ${filterPath} && true`;return{hookSpecificOutput:{hookEventName:`PreToolUse`,updatedInput:{...bashInput,command:wrappedCommand}}}}function bashOutputFilterParser(raw){return parseHookJson(raw)}function bashOutputFilterWriter(output){return JSON.stringify(output)}await runHookPlugin({parser:bashOutputFilterParser,handler:bashOutputFilterHandler,writer:bashOutputFilterWriter});export{};