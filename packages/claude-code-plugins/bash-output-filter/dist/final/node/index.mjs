#!/usr/bin/env bun
import{text}from"node:stream/consumers";function isDigit(c){return c>=`0`&&c<=`9`}function isLowerAlpha(c){return c>=`a`&&c<=`z`}function isUpperAlpha(c){return c>=`A`&&c<=`Z`}function isAlphaNum(c){return isLowerAlpha(c)||isUpperAlpha(c)||isDigit(c)}function isWordChar(c){return isAlphaNum(c)||c===`_`}function isWhitespace(c){return c===` `||c===`	`||c===`
`||c===`\r`||c===`\f`||c===`\v`}function splitWhitespace(s){let tokens=[];for(let cursorIndex=0;cursorIndex<s.length;){if(isWhitespace(s.charAt(cursorIndex))){cursorIndex+=1;continue}let start=cursorIndex,end=cursorIndex+1;for(;end<s.length&&!isWhitespace(s.charAt(end));)end+=1;tokens.push(s.slice(start,end)),cursorIndex=end}return tokens}function boundariesSatisfied({haystack,phrase,index}){let before=index===0?``:haystack.charAt(index-1),afterIdx=index+phrase.length,after=afterIdx>=haystack.length?``:haystack.charAt(afterIdx),firstChar=phrase.charAt(0),lastChar=phrase.at(-1)??``,startBoundaryOk=isWordChar(firstChar)?before===``||!isWordChar(before):!0,endBoundaryOk=isWordChar(lastChar)?after===``||!isWordChar(after):!0;return startBoundaryOk&&endBoundaryOk}function containsWordBoundedPhrase({haystack,phrase}){if(phrase.length===0)return!1;let lowerHay=haystack.toLowerCase(),lowerPhrase=phrase.toLowerCase();for(let cursorIndex=lowerHay.indexOf(lowerPhrase,0);cursorIndex!==-1;cursorIndex=lowerHay.indexOf(lowerPhrase,cursorIndex+1))if(boundariesSatisfied({haystack:lowerHay,phrase:lowerPhrase,index:cursorIndex}))return!0;return!1}const PHRASE_NOT_FOUND=Symbol(`text-scan/phrase-not-found`);function containsAnyOfWordBounded({haystack,phrases}){for(let phrase of phrases)if(containsWordBoundedPhrase({haystack,phrase}))return{phrase};return PHRASE_NOT_FOUND}function startsWithSafeChar(cmd){if(cmd.length===0)return!1;let c=cmd.charAt(0);return isAlphaNum(c)||`_/.~"'-`.includes(c)}const ALLOW_PREDICATES=[startsWithSafeChar];function isAllowed(command){return ALLOW_PREDICATES.some(function(predicate){return predicate(command)})}const BINARY_TOOL_NAMES=[`xxd`,`hexdump`,`od`,`base64`,`tar`,`gzip`,`gunzip`,`zip`,`unzip`,`bzip2`,`xz`,`zstd`];function hasBinaryTool(cmd){return containsAnyOfWordBounded({haystack:cmd,phrases:BINARY_TOOL_NAMES})!==PHRASE_NOT_FOUND}function hasFileRedirect(cmd){for(let gtIdx=cmd.indexOf(`>`,0);gtIdx!==-1;gtIdx=cmd.indexOf(`>`,gtIdx+1)){let afterWs=gtIdx+1;for(;afterWs<cmd.length&&isWhitespace(cmd.charAt(afterWs));)afterWs+=1;if(afterWs<cmd.length){let next=cmd.charAt(afterWs);if(next!==`|`&&next!==`&`&&next!==`;`&&!isWhitespace(next))return!0}}return!1}function invokesFilterScript(cmd){return containsWordBoundedPhrase({haystack:cmd,phrase:`filter.mjs`})||containsWordBoundedPhrase({haystack:cmd,phrase:`filter.ts`})}function hasBofMarker(cmd){return cmd.includes(`___BOF_EC:`)}function endsWithBackgroundOp(cmd){return cmd.trimEnd().endsWith(`&`)}const DETACH_WRAPPER_NAMES=[`nohup`,`setsid`];function hasDetachWrapper(cmd){return containsAnyOfWordBounded({haystack:cmd,phrases:DETACH_WRAPPER_NAMES})!==PHRASE_NOT_FOUND}const CONTAINER_RUNTIMES=new Set([`docker`,`podman`]),CONTAINER_TTY_SUBCOMMANDS=new Set([`exec`,`run`]);function isTtyFlag(token){if(!token.startsWith(`-`)||token.length<2)return!1;let body=token.slice(1);for(let c of body)if(c<`a`||c>`z`)return!1;let last=body.at(-1)??``;return last===`i`||last===`t`}function hasTtyContainerInvoke(cmd){let tokens=splitWhitespace(cmd);for(let cursorIndex=0;cursorIndex+1<tokens.length;cursorIndex+=1){let runtime=tokens[cursorIndex]??``,sub=tokens[cursorIndex+1]??``;if(CONTAINER_RUNTIMES.has(runtime)&&CONTAINER_TTY_SUBCOMMANDS.has(sub)){for(let t of tokens.slice(cursorIndex+2))if(isTtyFlag(t))return!0}}return!1}function hasBunBuild(cmd){let tokens=splitWhitespace(cmd);for(let cursorIndex=0;cursorIndex+1<tokens.length;cursorIndex+=1)if(tokens[cursorIndex]===`bun`&&tokens[cursorIndex+1]===`build`)return!0;return!1}function hasDollarParen(cmd){return cmd.includes(`$(`)}function hasBacktickPair(cmd){let first=cmd.indexOf("`");if(first===-1)return!1;let second=cmd.indexOf("`",first+1);return second===-1?!1:second>first+1}function hasProcessSubstitution(cmd){return cmd.includes(`<(`)||cmd.includes(`>(`)}function hasHeredoc(cmd){for(let cursorIndex=cmd.indexOf(`<<`,0);cursorIndex!==-1;cursorIndex=cmd.indexOf(`<<`,cursorIndex+1)){let afterOpener=cmd.charAt(cursorIndex+2),afterWs=afterOpener===`<`||afterOpener===`-`?cursorIndex+2+1:cursorIndex+2;for(;afterWs<cmd.length&&isWhitespace(cmd.charAt(afterWs));)afterWs+=1;if(afterWs<cmd.length&&!isWhitespace(cmd.charAt(afterWs)))return!0}return!1}const STATE_BUILTIN_NAMES=[`cd`,`pushd`,`popd`,`export`,`unset`,`source`];function hasStateBuiltin(cmd){return containsAnyOfWordBounded({haystack:cmd,phrases:STATE_BUILTIN_NAMES})!==PHRASE_NOT_FOUND}function isSourceShorthand(cmd){return cmd.length>=2&&cmd.startsWith(`.`)&&isWhitespace(cmd.charAt(1))}function hasEval(cmd){return containsWordBoundedPhrase({haystack:cmd,phrase:`eval`})}const SKIP_PREDICATES=[hasBinaryTool,hasFileRedirect,invokesFilterScript,hasBofMarker,endsWithBackgroundOp,hasDetachWrapper,hasTtyContainerInvoke,hasBunBuild,hasDollarParen,hasBacktickPair,hasProcessSubstitution,hasHeredoc,hasStateBuiltin,isSourceShorthand,hasEval];function shouldSkip(command){return SKIP_PREDICATES.some(function(predicate){return predicate(command)})}function isBashToolInput(input){return typeof input.command==`string`}function bashOutputFilterHandler(event){if(event.tool_name!==`Bash`||!isBashToolInput(event.tool_input))return{};let bashInput=event.tool_input;if(!isAllowed(bashInput.command)||shouldSkip(bashInput.command))return{};let filterPath=import.meta.url.endsWith(`.mjs`)?`${import.meta.dirname}/filter.mjs`:`${import.meta.dirname}/filter.ts`,wrappedCommand=`set -o pipefail && ${bashInput.command} 2>&1 | bun ${filterPath} && true`;return{hookSpecificOutput:{hookEventName:`PreToolUse`,updatedInput:{...bashInput,command:wrappedCommand}}}}function bashOutputFilterParser(raw){return JSON.parse(raw)}function bashOutputFilterWriter(output){return JSON.stringify(output)}async function runHookPlugin({parser,handler,writer}){let output=await handler(parser(await text(process.stdin)));process.stdout.write(writer(output))}await runHookPlugin({parser:bashOutputFilterParser,handler:bashOutputFilterHandler,writer:bashOutputFilterWriter});export{};