#!/usr/bin/env bun
import{text}from"node:stream/consumers";function parseHookJson(raw){return JSON.parse(raw)}async function runHookPlugin({parser,handler,writer}){let output=await handler(parser(await text(process.stdin)));process.stdout.write(writer(output))}function isDigit(c){return c>=`0`&&c<=`9`}function isLowerAlpha(c){return c>=`a`&&c<=`z`}function isUpperAlpha(c){return c>=`A`&&c<=`Z`}function isAlphaNum(c){return isLowerAlpha(c)||isUpperAlpha(c)||isDigit(c)}function isWordChar(c){return isAlphaNum(c)||c===`_`}function isWhitespace(c){return c===` `||c===`	`||c===`
`||c===`\r`||c===`\f`||c===`\v`}function boundariesSatisfied({haystack,phrase,index}){let before=index===0?``:haystack.charAt(index-1),afterIdx=index+phrase.length,after=afterIdx>=haystack.length?``:haystack.charAt(afterIdx),firstChar=phrase.charAt(0),lastChar=phrase.at(-1)??``,startBoundaryOk=isWordChar(firstChar)?before===``||!isWordChar(before):!0,endBoundaryOk=isWordChar(lastChar)?after===``||!isWordChar(after):!0;return startBoundaryOk&&endBoundaryOk}function containsWordBoundedPhrase({haystack,phrase}){if(phrase.length===0)return!1;let lowerHay=haystack.toLowerCase(),lowerPhrase=phrase.toLowerCase();function checkFrom(idx){let next=lowerHay.indexOf(lowerPhrase,idx);return next===-1?!1:boundariesSatisfied({haystack:lowerHay,phrase:lowerPhrase,index:next})?!0:checkFrom(next+1)}return checkFrom(0)}function containsAnyOfWordBounded({haystack,phrases}){for(let phrase of phrases)if(containsWordBoundedPhrase({haystack,phrase}))return{phrase}}function stripBetweenDelims({text,openDelim,closeDelim,disallowedInside}){function walk({idx,acc}){let openIdx=text.indexOf(openDelim,idx);if(openIdx===-1)return acc+text.slice(idx);let closeSearchStart=openIdx+openDelim.length,closeIdx=text.indexOf(closeDelim,closeSearchStart);if(closeIdx===-1)return acc+text.slice(idx);let inside=text.slice(closeSearchStart,closeIdx);return disallowedInside!==void 0&&inside.includes(disallowedInside)?walk({idx:closeSearchStart,acc:acc+text.slice(idx,closeSearchStart)}):walk({idx:closeIdx+closeDelim.length,acc:acc+text.slice(idx,openIdx)})}return walk({idx:0,acc:``})}function stripLinesStartingWith({text,prefix}){return text.split(`
`).filter(function(line){return!line.trimStart().startsWith(prefix)}).join(`
`)}const FILE_EXTENSIONS=[`ts`,`tsx`,`js`,`jsx`,`json`,`md`,`yaml`,`yml`,`toml`,`rs`,`py`];function isPathChar(c){return isAlphaNum(c)||c===`_`||c===`.`||c===`/`||c===`@`||c===`-`}function hasFilePathWithExtension(line){let lower=line.toLowerCase();function checkExt(ext){let token=`.${ext}`;function search(fromIdx){let idx=lower.indexOf(token,fromIdx);if(idx===-1)return!1;if(idx===0||!isPathChar(line.charAt(idx-1)))return search(idx+1);let endIdx=idx+token.length;return endIdx<line.length&&isWordChar(line.charAt(endIdx))?search(idx+1):!0}return search(0)}return FILE_EXTENSIONS.some(function(ext){return checkExt(ext)})}function hasLineNumberSuffix(line){function search(fromIdx){let colonIdx=line.indexOf(`:`,fromIdx);if(colonIdx===-1)return!1;function countDigits({at,count}){return count>=5||at>=line.length||!isDigit(line.charAt(at))?count:countDigits({at:at+1,count:count+1})}let digitCount=countDigits({at:colonIdx+1,count:0});if(digitCount>=1&&digitCount<=5){let afterIdx=colonIdx+1+digitCount;if(afterIdx>=line.length||!isWordChar(line.charAt(afterIdx)))return!0}return search(colonIdx+1)}return search(0)}function hasAgentsMdMention(line){return containsWordBoundedPhrase({haystack:line,phrase:`AGENTS.md`})}function lineHasCitation(line){return hasFilePathWithExtension(line)||hasLineNumberSuffix(line)||hasAgentsMdMention(line)}function normaliseApostrophes(text){return text.replaceAll(`‘`,`'`).replaceAll(`’`,`'`)}const UNCERTAINTY_PHRASES=`probably.maybe.may be.may only be.worth testing.perhaps.possibly.presumably.presume.i think.i believe.i assume.i suspect.i imagine.i guess.i suppose.might be.could be.should be.not sure.not entirely sure.not certain.hard to say.hard to tell.difficult to say.difficult to tell.if i recall.if i remember.from what i recall.from what i remember.as far as i know.as far as i can tell.likely.worse than most.more than most.less than most`.split(`.`);function findErThanMost(text){let lower=text.toLowerCase();function search(fromIdx){let idx=lower.indexOf(`er than most`,fromIdx);if(idx===-1)return;if(idx===0||!isWordChar(lower.charAt(idx-1)))return search(idx+1);let endIdx=idx+12;if(endIdx<lower.length&&isWordChar(lower.charAt(endIdx)))return search(idx+1);function findWordStart(at){return at<0?0:isWordChar(text.charAt(at))?findWordStart(at-1):at+1}let wordStart=findWordStart(idx-1);return text.slice(wordStart,endIdx)}return search(0)}function containsErThanMost(text){return findErThanMost(text)!==void 0}const DISMISSAL_PHRASES=[`the project doesn't use`,`the project doesnt use`,`project doesn't use`,`project doesnt use`,`we don't use`,`we dont use`,`the codebase doesn't use`,`the codebase doesn't have`,`the codebase doesnt use`,`the codebase doesnt have`,`codebase doesn't use`,`codebase doesn't have`,`codebase doesnt use`,`codebase doesnt have`,`doesn't apply here`,`doesnt apply here`,`is already handled by`,`is already covered by`];function stripCodeBlocks(text){return stripBetweenDelims({text,openDelim:"```",closeDelim:"```"})}function stripInlineCode(text){return stripBetweenDelims({text,openDelim:"`",closeDelim:"`"})}function stripBlockquotes(text){return stripLinesStartingWith({text,prefix:`>`})}function stripQuotedStrings(text){return stripBetweenDelims({text:stripBetweenDelims({text,openDelim:`"`,closeDelim:`"`,disallowedInside:`
`}),openDelim:`'`,closeDelim:`'`,disallowedInside:`
`})}function stripNonProseRegions(text){return stripQuotedStrings(stripBlockquotes(stripInlineCode(stripCodeBlocks(text))))}function findUncertainty(prose){let phraseHit=containsAnyOfWordBounded({haystack:prose,phrases:UNCERTAINTY_PHRASES});if(phraseHit!==void 0)return{phrase:phraseHit.phrase};if(containsErThanMost(prose))return{phrase:findErThanMost(prose)??`er than most`}}function findCategoricalDismissal(prose){let lines=prose.split(`
`);for(let line of lines){if(lineHasCitation(line))continue;let hit=containsAnyOfWordBounded({haystack:normaliseApostrophes(line),phrases:DISMISSAL_PHRASES});if(hit!==void 0)return{phrase:hit.phrase}}}const RHETORICAL_PREFIXES=[`what if`,`why does`,`why would`,`how does`,`have you ever`];function startsWithWordBounded({s,prefix}){if(!s.startsWith(prefix))return!1;if(s.length===prefix.length)return!0;let next=s.charAt(prefix.length);return(next<`a`||next>`z`)&&(next<`A`||next>`Z`)&&(next<`0`||next>`9`)&&next!==`_`}function findLastSentenceStart(text){function skipWs(idx){return idx>=text.length||!isWhitespace(text.charAt(idx))?idx:skipWs(idx+1)}function walk(at){if(at<=0)return 0;let prev=text.charAt(at-1);return`.!?`.includes(prev)&&isWhitespace(text.charAt(at))?skipWs(at):walk(at-1)}return walk(text.length-1)}function findTrailingQuestion(prose){let trimmed=prose.slice(-500).trimEnd();if(!trimmed.endsWith(`?`))return;let sentenceStart=findLastSentenceStart(trimmed),sentence=trimmed.slice(sentenceStart);if(sentence.length===0)return;let firstChar=sentence.charAt(0);if(firstChar<`A`||firstChar>`Z`)return;let lower=sentence.toLowerCase();for(let prefix of RHETORICAL_PREFIXES)if(startsWithWordBounded({s:lower,prefix}))return;return{sentence}}function stopRemindersHandler(event){if(event.stop_hook_active)return{};let prose=stripNonProseRegions(event.last_assistant_message??``),match=findUncertainty(prose),dismissal=findCategoricalDismissal(prose),question=findTrailingQuestion(prose),reasons=[];return match!==void 0&&reasons.push(`Your response contains uncertain language ("${match.phrase}").`,`Search for evidence, read the relevant code, or check documentation.`,`Always research thoroughly before responding.`,`If you have already investigated and the uncertainty is genuinely warranted,`,`say so explicitly and continue with your response.`,`This may be a false positive; use your judgement.`),dismissal!==void 0&&reasons.push(`Your response contains an uncited categorical dismissal ("${dismissal.phrase}").`,`Categorical dismissals are one rg/find/config-read/AGENTS.md-grep away from being verified.`,`Cite the search result inline on the same line as the dismissal:`,`a file path with an extension, a path:line reference, or the literal "AGENTS.md".`,`If the dismissal was wrong, fold the now-relevant option back into the analysis.`,`This may be a false positive; use your judgement.`),question!==void 0&&reasons.push(`Your response ends with a question to the user ("${question.sentence}").`,`Use the AskUserQuestion tool to ask the user instead of ending your response with a question.`,`The AskUserQuestion tool ensures the user sees and can respond to your question directly.`,`Rephrase your question as an AskUserQuestion tool call and continue.`),reasons.length>0?{decision:`block`,reason:reasons.join(` `)}:{}}function stopRemindersParser(raw){return parseHookJson(raw)}function stopRemindersWriter(output){return JSON.stringify(output)}await runHookPlugin({parser:stopRemindersParser,handler:stopRemindersHandler,writer:stopRemindersWriter});export{};