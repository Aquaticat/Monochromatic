#!/usr/bin/env bun
import{text}from"node:stream/consumers";async function readStdin(){return text(process.stdin)}const UNCERTAINTY_PATTERNS=[/\bprobably\b/i,/\bmaybe\b/i,/\bmay be\b/i,/\bmay only be\b/i,/\bworth testing\b/i,/\bperhaps\b/i,/\bpossibly\b/i,/\bpresum(?:ably|e)\b/i,/\bi think\b/i,/\bi believe\b/i,/\bi assume\b/i,/\bi suspect\b/i,/\bi imagine\b/i,/\bi guess\b/i,/\bi suppose\b/i,/\bmight be\b/i,/\bcould be\b/i,/\bshould be\b/i,/\bnot (?:entirely )?sure\b/i,/\bnot certain\b/i,/\bhard to (?:say|tell)\b/i,/\bdifficult to (?:say|tell)\b/i,/\bif i (?:recall|remember)\b/i,/\bfrom what i (?:recall|remember)\b/i,/\bas far as i (?:know|can tell)\b/i,/\blikely\b/i];function stripCodeBlocks(text){return text.replaceAll(/```[\s\S]*?```/g,``)}function stripInlineCode(text){return text.replaceAll(/`[^`]+`/g,``)}function stripBlockquotes(text){return text.replaceAll(/^>.*$/gm,``)}function stripQuotedStrings(text){return text.replaceAll(/"[^"\n]+"/g,``).replaceAll(/'[^'\n]+'/g,``)}function stripNonProseRegions(text){return stripQuotedStrings(stripBlockquotes(stripInlineCode(stripCodeBlocks(text))))}function findUncertainty(prose){for(let pattern of UNCERTAINTY_PATTERNS){let match=pattern.exec(prose);if(match!==null)return{phrase:match[0],pattern}}}function findTrailingQuestion(prose){let tail=prose.slice(-500),match=/(?:^|[.!?]\s+)([A-Z][^.!?]*\?)\s*$/.exec(tail);if(match===null)return;let sentence=match[1]??match[0];for(let prefix of[/^what if\b/i,/^why does\b/i,/^why would\b/i,/^how does\b/i,/^have you ever\b/i])if(prefix.test(sentence))return;return{sentence}}const raw=await readStdin(),event=JSON.parse(raw);if(event.stop_hook_active)process.stdout.write(JSON.stringify({}));else{let prose=stripNonProseRegions(event.last_assistant_message??``),match=findUncertainty(prose),question=findTrailingQuestion(prose),reasons=[];if(match!==void 0&&reasons.push(`Your response contains uncertain language ("${match.phrase}").`,`Search for evidence, read the relevant code, or check documentation.`,`Always research thoroughly before responding.`,`If you have already investigated and the uncertainty is genuinely warranted,`,`say so explicitly and continue with your response.`,`This may be a false positive -- use your judgement.`),question!==void 0&&reasons.push(`Your response ends with a question to the user ("${question.sentence}").`,`Use the AskUserQuestion tool to ask the user instead of ending your response with a question.`,`The AskUserQuestion tool ensures the user sees and can respond to your question directly.`,`Rephrase your question as an AskUserQuestion tool call and continue.`),reasons.length>0){let output={decision:`block`,reason:reasons.join(` `)};process.stdout.write(JSON.stringify(output))}else process.stdout.write(JSON.stringify({}))}export{};