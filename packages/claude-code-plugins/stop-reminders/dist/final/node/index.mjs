#!/usr/bin/env bun
import{text}from"node:stream/consumers";function parseHookJson(raw){return JSON.parse(raw)}async function runHookPlugin({parser,handler,writer}){let output=await handler(parser(await text(process.stdin)));process.stdout.write(writer(output))}const UNCERTAINTY_PATTERNS=[/\bprobably\b/i,/\bmaybe\b/i,/\bmay be\b/i,/\bmay only be\b/i,/\bworth testing\b/i,/\bperhaps\b/i,/\bpossibly\b/i,/\bpresum(?:ably|e)\b/i,/\bi think\b/i,/\bi believe\b/i,/\bi assume\b/i,/\bi suspect\b/i,/\bi imagine\b/i,/\bi guess\b/i,/\bi suppose\b/i,/\bmight be\b/i,/\bcould be\b/i,/\bshould be\b/i,/\bnot (?:entirely )?sure\b/i,/\bnot certain\b/i,/\bhard to (?:say|tell)\b/i,/\bdifficult to (?:say|tell)\b/i,/\bif i (?:recall|remember)\b/i,/\bfrom what i (?:recall|remember)\b/i,/\bas far as i (?:know|can tell)\b/i,/\blikely\b/i,/\wer than most\b/i,/\b(?:worse|more|less) than most\b/i];function stripCodeBlocks(text){return text.replaceAll(/```[\s\S]*?```/g,``)}function stripInlineCode(text){return text.replaceAll(/`[^`]+`/g,``)}function stripBlockquotes(text){return text.replaceAll(/^>.*$/gm,``)}function stripQuotedStrings(text){return text.replaceAll(/"[^"\n]+"/g,``).replaceAll(/'[^'\n]+'/g,``)}function stripNonProseRegions(text){return stripQuotedStrings(stripBlockquotes(stripInlineCode(stripCodeBlocks(text))))}const DISMISSAL_PATTERNS=[/\b(?:the )?project doesn['']?t use\b/i,/\bwe don['']?t use\b/i,/\b(?:the )?codebase doesn['']?t (?:use|have)\b/i,/\bdoesn['']?t apply here\b/i,/\bis already (?:handled|covered) by\b/i],CITATION_PATTERNS=[/[\w./@-]+\.(?:ts|tsx|js|jsx|json|md|yaml|yml|toml|rs|py)\b/i,/:\d{1,5}\b/,/\bAGENTS\.md\b/];function findUncertainty(prose){for(let pattern of UNCERTAINTY_PATTERNS){let match=pattern.exec(prose);if(match!==null)return{phrase:match[0],pattern}}}function lineHasCitation(line){for(let pattern of CITATION_PATTERNS)if(pattern.test(line))return!0;return!1}function findCategoricalDismissal(prose){let lines=prose.split(`
`);for(let line of lines)if(!lineHasCitation(line))for(let pattern of DISMISSAL_PATTERNS){let match=pattern.exec(line);if(match!==null)return{phrase:match[0],pattern}}}function findTrailingQuestion(prose){let tail=prose.slice(-500),match=/(?:^|[.!?]\s+)([A-Z][^.!?]*\?)\s*$/.exec(tail);if(match===null)return;let sentence=match[1]??match[0];for(let prefix of[/^what if\b/i,/^why does\b/i,/^why would\b/i,/^how does\b/i,/^have you ever\b/i])if(prefix.test(sentence))return;return{sentence}}function stopRemindersHandler(event){if(event.stop_hook_active)return{};let prose=stripNonProseRegions(event.last_assistant_message??``),match=findUncertainty(prose),dismissal=findCategoricalDismissal(prose),question=findTrailingQuestion(prose),reasons=[];return match!==void 0&&reasons.push(`Your response contains uncertain language ("${match.phrase}").`,`Search for evidence, read the relevant code, or check documentation.`,`Always research thoroughly before responding.`,`If you have already investigated and the uncertainty is genuinely warranted,`,`say so explicitly and continue with your response.`,`This may be a false positive; use your judgement.`),dismissal!==void 0&&reasons.push(`Your response contains an uncited categorical dismissal ("${dismissal.phrase}").`,`Categorical dismissals are one rg/find/config-read/AGENTS.md-grep away from being verified.`,`Cite the search result inline on the same line as the dismissal:`,`a file path with an extension, a path:line reference, or the literal "AGENTS.md".`,`If the dismissal was wrong, fold the now-relevant option back into the analysis.`,`This may be a false positive; use your judgement.`),question!==void 0&&reasons.push(`Your response ends with a question to the user ("${question.sentence}").`,`Use the AskUserQuestion tool to ask the user instead of ending your response with a question.`,`The AskUserQuestion tool ensures the user sees and can respond to your question directly.`,`Rephrase your question as an AskUserQuestion tool call and continue.`),reasons.length>0?{decision:`block`,reason:reasons.join(` `)}:{}}function stopRemindersParser(raw){return parseHookJson(raw)}function stopRemindersWriter(output){return JSON.stringify(output)}await runHookPlugin({parser:stopRemindersParser,handler:stopRemindersHandler,writer:stopRemindersWriter});export{};