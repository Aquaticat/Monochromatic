#!/usr/bin/env bun
import{text}from"node:stream/consumers";const CORRECTION_PATTERNS=[/\bdemonstrably (?:false|wrong)\b/i,/\byou missed\b/i,/\bdidn['']?t you\b/i,/\byou['']?re wrong\b/i,/\byou are wrong\b/i,/\bshouldn['']?t have\b/i,/\bshould not have\b/i,/\bwhy would you\b/i,/\bthat['']?s wrong\b/i,/\bthis is wrong\b/i,/\byou got (?:that|this|it) wrong\b/i,/\byou['']?re not (?:feeling|paying) (?:well|attention)\b/i,/\bplease be more careful\b/i,/\b(?:are you|aren['']?t you) (?:not )?paying attention\b/i];function detectCorrection(prompt){for(let pattern of CORRECTION_PATTERNS)if(pattern.test(prompt))return!0;return!1}const CORRECTION_REMINDER_TEXT=[`<correction-detected>`,`The user is correcting a substantive claim from your previous response.`,`Per AGENTS.md Pre-response checklist item 11: a user correction is an`,`approach-change moment, not a small patch. The blind spot that produced`,`the original claim is still active for the revision.`,``,`Before your next substantive response, call the advisor tool.`,`It receives your full transcript, so it can see the blind spot you cannot.`,`</correction-detected>`].join(`
`);function correctionReminderHandler(event){return{hookSpecificOutput:{hookEventName:`UserPromptSubmit`,additionalContext:detectCorrection(event.prompt)?CORRECTION_REMINDER_TEXT:``}}}function correctionReminderParser(raw){return JSON.parse(raw)}function correctionReminderWriter(output){return JSON.stringify(output)}async function runHookPlugin({parser,handler,writer}){let output=await handler(parser(await text(process.stdin)));process.stdout.write(writer(output))}await runHookPlugin({parser:correctionReminderParser,handler:correctionReminderHandler,writer:correctionReminderWriter});export{};