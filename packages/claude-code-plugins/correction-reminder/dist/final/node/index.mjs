#!/usr/bin/env bun
import{text}from"node:stream/consumers";function isDigit(c){return c>=`0`&&c<=`9`}function isLowerAlpha(c){return c>=`a`&&c<=`z`}function isUpperAlpha(c){return c>=`A`&&c<=`Z`}function isAlphaNum(c){return isLowerAlpha(c)||isUpperAlpha(c)||isDigit(c)}function isWordChar(c){return isAlphaNum(c)||c===`_`}function boundariesSatisfied({haystack,phrase,index}){let before=index===0?``:haystack.charAt(index-1),afterIdx=index+phrase.length,after=afterIdx>=haystack.length?``:haystack.charAt(afterIdx),firstChar=phrase.charAt(0),lastChar=phrase.at(-1)??``,startBoundaryOk=isWordChar(firstChar)?before===``||!isWordChar(before):!0,endBoundaryOk=isWordChar(lastChar)?after===``||!isWordChar(after):!0;return startBoundaryOk&&endBoundaryOk}function containsWordBoundedPhrase({haystack,phrase}){if(phrase.length===0)return!1;let lowerHay=haystack.toLowerCase(),lowerPhrase=phrase.toLowerCase();function checkFrom(idx){let next=lowerHay.indexOf(lowerPhrase,idx);return next===-1?!1:boundariesSatisfied({haystack:lowerHay,phrase:lowerPhrase,index:next})?!0:checkFrom(next+1)}return checkFrom(0)}function containsAnyOfWordBounded({haystack,phrases}){for(let phrase of phrases)if(containsWordBoundedPhrase({haystack,phrase}))return{phrase}}const CORRECTION_PHRASES=`demonstrably false.demonstrably wrong.you missed.didn't you.didnt you.you're wrong.youre wrong.you are wrong.shouldn't have.shouldnt have.should not have.why would you.that's wrong.thats wrong.this is wrong.you got that wrong.you got this wrong.you got it wrong.you're not feeling well.you're not feeling attention.you're not paying well.you're not paying attention.youre not feeling well.youre not feeling attention.youre not paying well.youre not paying attention.please be more careful.are you paying attention.are you not paying attention.aren't you paying attention.aren't you not paying attention.arent you paying attention.arent you not paying attention`.split(`.`);function normaliseApostrophes(prompt){return prompt.replaceAll(`‘`,`'`).replaceAll(`’`,`'`)}function detectCorrection(prompt){return containsAnyOfWordBounded({haystack:normaliseApostrophes(prompt),phrases:CORRECTION_PHRASES})!==void 0}const CORRECTION_REMINDER_TEXT=[`<correction-detected>`,`The user is correcting a substantive claim from your previous response.`,`Per AGENTS.md Pre-response checklist item 11: a user correction is an`,`approach-change moment, not a small patch. The blind spot that produced`,`the original claim is still active for the revision.`,``,`Before your next substantive response, re-check the claim against primary`,`sources, local files, logs, or command output. Do not run a same-session`,"self-review or write an `Advisor pass: ...` line; self-review is not","independent evidence. See `docs/agent-self-review.md`.",`</correction-detected>`].join(`
`);function correctionReminderHandler(event){return{hookSpecificOutput:{hookEventName:`UserPromptSubmit`,additionalContext:detectCorrection(event.prompt)?CORRECTION_REMINDER_TEXT:``}}}function correctionReminderParser(raw){return JSON.parse(raw)}function correctionReminderWriter(output){return JSON.stringify(output)}async function runHookPlugin({parser,handler,writer}){let output=await handler(parser(await text(process.stdin)));process.stdout.write(writer(output))}await runHookPlugin({parser:correctionReminderParser,handler:correctionReminderHandler,writer:correctionReminderWriter});export{};