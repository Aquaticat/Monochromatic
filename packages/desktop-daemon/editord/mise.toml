[tasks.build]
extends = "build"

[tasks."build:js"]
extends = "build:js"

[tasks."build:js:client"]
extends = "build:js:client"

[tasks."watch:build"]
extends = "watch:build"

[tasks."watch:build:js"]
extends = "watch:build:js"

[tasks."watch:build:js:client"]
extends = "watch:build:js:client"

[tasks."dev"]
description = "Start editord in watch mode (rebuilds client, restarts server)"
run = [{ tasks = ["watch:build:js:client", "dev:server"] }]

[tasks."dev:server"]
hide = true
description = "Start editord server with file-watch auto-restart"
run = "mise watch -w src/server -r -- start:server"

[tasks."start:server"]
hide = true
description = "Start editord server (single run)"
run = "bun src/server/index.ts"

[tasks.lint]
extends = "lint"

[tasks."lint:types"]
extends = "lint:types"
hide = true

[tasks."lint:oxlint"]
extends = "lint:oxlint"
