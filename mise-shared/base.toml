# Generated by file-enforcer from mise-packages.bodies.config.ts; do not hand-edit.
# Included by per-package mise.toml via [task_config].includes; root [vars] interpolate here.

"build".shell = "node -e"
"build".run = "{{vars.fanout}}"
"build:js".shell = "node -e"
"build:js".run = "{{vars.fanout}}"
"watch:build".shell = "node -e"
"watch:build".run = "{{vars.fanout}}"
"watch:build:js".shell = "node -e"
"watch:build:js".run = "{{vars.fanout}}"
