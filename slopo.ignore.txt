# Cluster hashes reviewed and dismissed. One hash per line.
#
# A hash covers each unit's code body and its path relative to the index root.
# It changes when the code or that path changes, so the cluster re-surfaces.
# Re-indexing from a different root shifts every path, changing the hashes.
#
# This inventory was refreshed against the voyage/voyage-code-4 report.
# Entries mapped by historical unit identity unless marked as semantic remaps.

# ── Deliberately NOT dismissed: music-player android/desktop drift ──
# The android app (Kotlin, package/music-player/android-app/app/src/main/kotlin/.../core)
# and the desktop app (Rust, package/music-player/desktop-app/src) carry parallel core
# logic (Queue, Pagination, RelPath, AudioExtensions, Normalization, PlayerController) that
# slopo flags as cross-language pairs. Those clusters are intentionally left OUT of this
# ignore list so they keep surfacing in the report. The duplication is real and drifting;
# it is tracked in package/music-player/PROPOSAL.shared-core.md, whose plan is to rewrite
# the Android core in Rust and delete the Kotlin copy. The visible clusters are the standing
# reminder that the drift is still open. Do NOT dismiss them until Android is rewritten and
# the Kotlin/Rust divergence is gone. (Cross-language code cannot share a function, so these
# are not extract-a-helper candidates; the remediation is the rewrite, not an ignore entry.)

# ── INTENTIONAL-VARIANT (17) ──
# Intentional sibling variants (sync/async, single/batch, get/set,
# plain-vs-from constructors, mirror event handlers). Meant to mirror each
# other and stay separate; merging would hurt clarity.
# batch_sheng/batch_sheng2 SIMD-variant twins
f498089d4a03
# per-event handlers (touchstart/touchmove)
1fa31c0bbf53
# mirror yields-rule handlers, opposite conditions/messageIds
7bb436bcb362
# distinct AVX512 kernels: single-byte sheng vs pair sheng2
e3a81ba95d1e
# healBackends/healBackendsSync sync/async twins
58a7daddad88
# sync/async queryBackend twins
676b3bc00e50
# sibling or-throw guards, one per iterable kind
3fb24b23d353
# sync/async readCssFile twins
4e3502a0862f
# mirror keydown/keyup handlers, opposite cursor
714056a4f14c
# sheng/sheng2 per-strategy platform dispatch, including NEON and AVX paths
13ad287ede50
# sync/async delete twins (kv-store)
c2f031e4e416
# sync/async get twins (kv-store)
df0864563834
# lazyOnce/lazyOnceAsync sync/async twins
fedafcaaa399
# isPrefix/isStrictPrefix complementary predicates, differing only by <= vs <
54a947960da7
# sync/async clear twins (kv-store)
0ed673440f37
# embedAll/embedBatchAll single vs batch twins
3351a8d310ee
# sync/async captureError twins (test fixtures)
a1b664fb91ed

# ── STRUCTURAL-IDIOM (14) ──
# Structural idioms mandated by conventions. Shared skeleton (oxlint createOnce
# rule scaffolding and per-locale i18n grammar renderers) where each body's actual
# logic differs. Not extractable without a false abstraction.
# semantic split of reviewed wh-question renderers: locale dispatcher
7ec17c4edbdf
# semantic split of reviewed wh-question renderers: adverbial questions
0a43dc0e4435
# semantic split of reviewed wh-question renderers: subject questions
6ffeaa95d661
# semantic split of reviewed wh-question renderers: object questions
e9510c048af2
# semantic remap: sentence dispatcher/factory glue; signatures differ
b3c3ce7ac317
# imperative renderers diverge per language
92ae195f9975
# semantic remap: noun-phrase renderers diverge (articles vs classifiers)
e0af1a5083e9
# semantic remap: adverbial renderers diverge (prepositions vs coverbs)
3350cc2028ca
# yes/no question renderers diverge per language
0a6536d03a2b
# declarative renderers diverge per language
329c6892e8f0
# per-locale wrapper binding locale to shared applyCapitalization
f563c71baeff
# possessive suffix differs per language grammar
238136eee5b5
# semantic merge of reviewed returns/params createOnce scaffolding
b80f583dad64
# per-rule TSDoc handlers; conditions differ, report shape shared
cbff52731c64

# ── COINCIDENTAL-SHAPE (3) ──
# Coincidental shape matches. Units share an implementation idiom but do
# unrelated work; no common logic to extract.
# engine Drop differs per app (fields, unpark)
7a68ec2bdb9c
# two is_match_batch impls on different types, unrelated bodies
3a4a83b47c31
# one database-row helper wraps another with mapping; return-type shape only
1a33c04f7f3d

# ── BOILERPLATE-TRIVIAL (18) ──
# Trivial boilerplate fragments. One-line wrappers/forwarders, tiny fixer stubs,
# From impls, and cleanup fragments where extraction costs more than it saves.
# one-line winit with_name idiom, two unrelated desktop apps
8d50990c49cb
# one-line From impl (error.rs; duplicate kept through a larger Display cluster)
811c54eb8a98
# one-line From impl (error.rs)
39dbd96a2ba3
# one-line From impl (error.rs)
654e1000a9c0
# expanded fake-API sendMessage stubs; whole fake harness stays visible
798d10651694
# expanded fake-API exec stubs
40f0c1f3840b
# expanded fake-API registerTool stubs
0e41fd1057e6
# standard web-component super/attachShadow boilerplate
a907be03b808
# dynamic browserslist import wrappers; returns differ
b9640ab1d320
# one-line bitset empty-check on distinct types
0f21701dfcb4
# thin getLastToken wrappers; divergent null handling
8717b08104c9
# one-line atomic-store setter across two audio backends
9052405ce43f
# one-line method wrapper delegating to shared send_and_wake
fce6f6ca1d38
# single-line table-engine closure
6d15bb200a5b
# four-line onEnd fragment; broader onConnect logic stays visible
1682a0e0531e
# error-subclass constructors each setting their own name
0a3773a1c347
# short onTimeout handlers differing only in message, per transport
77653aa954c8
# block-body opening/closing fixer stubs, different ranges and indentation
6be7f9516c9d

# ── TEST-OR-GENERATED (3) ──
# Test and generated code where DRY does not apply.
# bench harness closures (truepeak-core.bench)
8df402384c84
# .js transpiled output of collapse() in its .ts sibling (handover)
da53cb2e3bfa
# .js transpiled output of parseImports() in its .ts sibling (handover)
6ddb97d5cd76
