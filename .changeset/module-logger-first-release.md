---
"@monochromatic-dev/module-logger": minor
---

First npm release. Console output neutralizes terminal control characters,
`flush()` runs under a deadline (`flushDeadlineMs`), and the published package
exposes the built artifact only.
