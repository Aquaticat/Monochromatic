# Bun remains installed for explicit Bun islands only. Keep this root config
# minimal so ad-hoc Bun use does not look like the workspace default runtime.
logLevel = 'warn'
telemetry = false

[install]
# Prevent Bun from auto-installing missing imports; pnpm manages node_modules.
auto = 'disable'
