# Cargo work runs on the HOST when Zig and the native dev libraries are present,
# and falls back to the Fedora build container otherwise (see Containerfile).
# Host build deps: Zig 0.15.x (provided repo-wide via the root mise `zig` tool)
# plus fontconfig-devel and freetype-devel (layer with rpm-ostree on an immutable
# Fedora). libghostty-vt-sys compiles vendored Ghostty with Zig either way.
# The cargo_dispatch var (node -e) probes for cargo + zig + pkg-config
# fontconfig/freetype2; runCargo runs on the host with the bindgen/zig env set
# when all are present, else containerCargo runs the same cargo command in podman
# (package dir bind-mounted at /work, cargo registry and Ghostty git cache in a
# named volume, `label=disable` to skip SELinux relabeling). The *:container tasks
# call containerCargo directly to force the podman path regardless of host
# libraries; verify:container is the umbrella assertion.
# Why podman and not mise-managed deps (conda route rejected):
# see ../../../doc/decisions/desktop-app-podman-build.md

[env]
# Slint's experimental language surface used by the UI must be compiled
# consistently on the host, matching the Containerfile's baked ENV. podman does
# not forward host env into the container, so this only affects the host branch
# (the image sets its own copy).
SLINT_ENABLE_EXPERIMENTAL_FEATURES = "1"
# Host libclang ships only versioned (libclang.so.NN, no unversioned symlink), so
# point bindgen (libghostty-vt-sys) at the lib dir explicitly. Host only; not
# forwarded into the container, which carries its own clang.
LIBCLANG_PATH = "/usr/lib64"

[vars]
# Bootstrap-safe cargo dispatch for this package. Interpolated into the cargo
# tasks, each of which sets `shell = "node --input-type=module-typescript -e"`.
# - hostOk: cargo + zig on PATH and pkg-config resolves the Slint font libraries.
# - hostEnv(): host-only env for the native cargo branch. BINDGEN_EXTRA_CLANG_ARGS
#   points bindgen (libghostty-vt-sys) at the clang resource dir (globbed so an
#   LLVM bump needs no edit), since libclang.so in /usr/lib64 looks beside itself
#   for stdbool.h, which lives under /usr/lib/clang/<v>. ZIG_GLOBAL_CACHE_DIR is
#   pinned to a physical path under target/ because HOME (/home/user) is a symlink
#   to /var/home/user while the build runs from the physical path; process.cwd()
#   resolves physically, matching the spawned codegen exe's path-space.
# - containerCargo(args): build the image when missing, then run the cargo
#   subcommand in podman with memory/cpu caps bounding the Ghostty + Slint compile.
# - runCargo(args): host branch when hostOk, else container.
# Its imported bindings (execFileSync, globSync) are reused by inline follow-ups,
# so those must not reimport them.
cargo_dispatch = """
const { execFileSync } = await import('node:child_process')
const { globSync } = await import('node:fs')
const onPath = (command) => { try { execFileSync('which', [command], { stdio: 'ignore' }); return true } catch { return false } }
const pkgConfigOk = () => { try { execFileSync('pkg-config', ['--exists', 'fontconfig', 'freetype2'], { stdio: 'ignore' }); return true } catch { return false } }
const hostOk = onPath('cargo') && onPath('zig') && pkgConfigOk()
const hostEnv = () => {
  const clangDirs = globSync('/usr/lib/clang/*').sort()
  const resourceDir = clangDirs[clangDirs.length - 1]
  return { ...process.env, BINDGEN_EXTRA_CLANG_ARGS: `-resource-dir=${resourceDir}`, ZIG_GLOBAL_CACHE_DIR: `${process.cwd()}/target/zig-global-cache` }
}
const containerCargo = (cargoArgs) => {
  let imageExists = true
  try { execFileSync('podman', ['image', 'exists', 'localhost/monochromatic/terminal'], { stdio: 'ignore' }) } catch { imageExists = false }
  if (!imageExists) { execFileSync('podman', ['build', '--tag', 'localhost/monochromatic/terminal', '--file', 'Containerfile', '.'], { stdio: 'inherit' }) }
  execFileSync('podman', ['run', '--rm', '--memory=4g', '--cpus=4', '--security-opt', 'label=disable', '--volume', `${process.cwd()}:/work`, '--volume', 'terminal-cargo:/cargo', '--workdir', '/work', 'localhost/monochromatic/terminal', 'cargo', ...cargoArgs], { stdio: 'inherit' })
}
const runCargo = (cargoArgs) => { if (hostOk) { execFileSync('cargo', cargoArgs, { stdio: 'inherit', env: hostEnv() }) } else { containerCargo(cargoArgs) } }
"""

[tasks.image]
description = "Build the Fedora build/run container image"
run = "podman build --tag localhost/monochromatic/terminal --file Containerfile ."

[tasks.build]
description = "Release build (host if Zig + dev libs present, else container), then stage runtime shared libraries"
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
runCargo(['build', '--release'])
execFileSync('mise', ['run', '//package/desktop-app/terminal:stage:runtime-libs:release'], { stdio: 'inherit' })
"""

[tasks."build:debug"]
description = "Debug build (host if Zig + dev libs present, else container), then stage runtime shared libraries"
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
runCargo(['build'])
execFileSync('mise', ['run', '//package/desktop-app/terminal:stage:runtime-libs:debug'], { stdio: 'inherit' })
"""

[tasks."stage:runtime-libs:release"]
description = "Copy release libghostty-vt shared libraries into the binary rpath directory"
shell = "node --input-type=module-typescript -e"
run = """
const { globSync, mkdirSync, copyFileSync } = await import('node:fs')
const { join } = await import('node:path')
const libDirs = globSync('target/release/build/libghostty-vt-sys-*/out/ghostty-install/lib').sort()
if (libDirs.length === 0) { throw new Error('missing release libghostty-vt build output; run build first') }
const sourceLibDir = libDirs[libDirs.length - 1]
const stagedLibDir = 'target/lib/monochromatic-terminal'
mkdirSync(stagedLibDir, { recursive: true })
for (const name of ['libghostty-vt.so', 'libghostty-vt.so.0', 'libghostty-vt.so.0.1.0']) {
  copyFileSync(join(sourceLibDir, name), join(stagedLibDir, name))
}
"""

[tasks."stage:runtime-libs:debug"]
description = "Copy debug libghostty-vt shared libraries into the binary rpath directory"
shell = "node --input-type=module-typescript -e"
run = """
const { globSync, mkdirSync, copyFileSync } = await import('node:fs')
const { join } = await import('node:path')
const libDirs = globSync('target/debug/build/libghostty-vt-sys-*/out/ghostty-install/lib').sort()
if (libDirs.length === 0) { throw new Error('missing debug libghostty-vt build output; run build:debug first') }
const sourceLibDir = libDirs[libDirs.length - 1]
const stagedLibDir = 'target/lib/monochromatic-terminal'
mkdirSync(stagedLibDir, { recursive: true })
for (const name of ['libghostty-vt.so', 'libghostty-vt.so.0', 'libghostty-vt.so.0.1.0']) {
  copyFileSync(join(sourceLibDir, name), join(stagedLibDir, name))
}
"""

[tasks.lint]
description = "cargo check (host if Zig + dev libs present, else container)"
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
runCargo(['check'])
"""

[tasks."lint:clippy"]
description = "Run clippy with warnings denied (host if Zig + dev libs present, else container)"
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
runCargo(['clippy', '--release', '--', '--deny', 'warnings'])
"""

[tasks."format:clippy"]
description = "Apply clippy's machine-applicable suggestions in place (host if Zig + dev libs present, else container)"
# --allow-no-vcs is harmless on the host (the repo .git is visible) and required
# in the container, where only the package dir is bind-mounted at /work so the
# repo-root .git is not visible and cargo fix would otherwise refuse to run.
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
runCargo(['clippy', '--fix', '--allow-dirty', '--allow-staged', '--allow-no-vcs'])
"""

[tasks."lint:rust"]
description = "Run monochromatic-rust-linter (max-lines + require-rustdoc) over this package's .rs"
run = "cargo run --quiet --manifest-path ../../linter/rust/Cargo.toml -- ."

[tasks.test]
description = "cargo nextest run (host if Zig + dev libs present, else container)"
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
runCargo(['nextest', 'run', '--release'])
"""

[tasks.mcp]
description = "Build --features slint/mcp (debug), stage libs, and run HEADLESS with the embedded Slint MCP server on SLINT_MCP_PORT (default 9316) so an agent can drive/inspect the terminal UI"
# Same embedded-MCP path as package/music-player/desktop-app:mcp (see its comment
# and doc/runbook Slint testing). `mcp` is command-line only (never in Cargo.toml
# [features]); SLINT_EMIT_DEBUG_INFO=1 makes element ids survive; SLINT_BACKEND=headless
# renders offscreen. install_backend() early-returns when SLINT_MCP_PORT is set, so
# Slint creates the backend itself and starts the MCP server. State is isolated under
# target/mcp-xdg. Port 9316 keeps it distinct from the music player's 9315.
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
const { mkdirSync, copyFileSync } = await import('node:fs')
const { join } = await import('node:path')
process.env.SLINT_EMIT_DEBUG_INFO = '1'
process.env.SLINT_MCP_PORT = process.env.SLINT_MCP_PORT ?? '9316'
process.env.SLINT_BACKEND = process.env.SLINT_BACKEND ?? 'headless'
const xdgRoot = join(process.cwd(), 'target', 'mcp-xdg')
for (const sub of ['config', 'cache', 'data']) { mkdirSync(join(xdgRoot, sub), { recursive: true }) }
process.env.XDG_CONFIG_HOME = join(xdgRoot, 'config')
process.env.XDG_CACHE_HOME = join(xdgRoot, 'cache')
process.env.XDG_DATA_HOME = join(xdgRoot, 'data')
runCargo(['build', '--features', 'slint/mcp'])
// Stage libghostty-vt beside the binary INLINE (not via a nested `mise run`, which
// re-loads this repo's sops-encrypted config and fails without the decrypt key).
// Mirrors stage:runtime-libs:debug. globSync comes from cargo_dispatch above.
const libDirs = globSync('target/debug/build/libghostty-vt-sys-*/out/ghostty-install/lib').sort()
if (libDirs.length === 0) { throw new Error('missing debug libghostty-vt build output') }
const sourceLibDir = libDirs[libDirs.length - 1]
const stagedLibDir = 'target/lib/monochromatic-terminal'
mkdirSync(stagedLibDir, { recursive: true })
for (const name of ['libghostty-vt.so', 'libghostty-vt.so.0', 'libghostty-vt.so.0.1.0']) { copyFileSync(join(sourceLibDir, name), join(stagedLibDir, name)) }
console.error(`monochromatic-terminal MCP server: http://127.0.0.1:${process.env.SLINT_MCP_PORT}/mcp (backend=${process.env.SLINT_BACKEND || 'default'})`)
execFileSync('./target/debug/monochromatic-terminal', [], { stdio: 'inherit' })
"""

[tasks."lint:slint"]
description = "Type-check ui/app.slint with slint-viewer --check (compiles the markup, opens no window, exits non-zero on error)"
run = "slint-viewer --check ui/app.slint"

[tasks.screenshot]
description = "Render ui/app.slint to target/app.slint.png HEADLESS (slint-viewer --screenshot, default property values, no window) for a quick static-layout snapshot"
shell = "node --input-type=module-typescript -e"
run = """
const { execFileSync } = await import('node:child_process')
const { mkdirSync } = await import('node:fs')
mkdirSync('target', { recursive: true })
execFileSync('slint-viewer', ['--screenshot', 'target/app.slint.png', 'ui/app.slint'], { stdio: 'inherit', env: { ...process.env, SLINT_BACKEND: 'software' } })
console.error('rendered ui/app.slint -> target/app.slint.png')
"""

[tasks."build:container"]
description = "Release build forced inside the container, then stage runtime libraries (asserts the container path)"
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
containerCargo(['build', '--release'])
execFileSync('mise', ['run', '//package/desktop-app/terminal:stage:runtime-libs:release'], { stdio: 'inherit' })
"""

[tasks."lint:clippy:container"]
description = "clippy with warnings denied forced inside the container"
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
containerCargo(['clippy', '--release', '--', '--deny', 'warnings'])
"""

[tasks."test:container"]
description = "cargo nextest run forced inside the container"
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
containerCargo(['nextest', 'run', '--release'])
"""

[tasks."verify:container"]
description = "Assert the container build/clippy/test path works even when the host has Zig and the dev libs"
# Aborts on the first failing step (execFileSync throws) so a broken container
# path fails the task rather than being masked by a later passing step.
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
execFileSync('mise', ['run', 'image'], { stdio: 'inherit' })
containerCargo(['build', '--release'])
containerCargo(['clippy', '--release', '--', '--deny', 'warnings'])
containerCargo(['nextest', 'run', '--release'])
"""

[tasks.run]
description = "Build (host if Zig + dev libs present, else container), install desktop metadata and runtime libraries, then run the GUI on the host"
shell = "node --input-type=module-typescript -e"
run = """
{{vars.cargo_dispatch}}
const { mkdirSync, copyFileSync } = await import('node:fs')
const { join } = await import('node:path')
const home = process.env.HOME ?? ''
runCargo(['build', '--release'])
execFileSync('mise', ['run', '//package/desktop-app/terminal:stage:runtime-libs:release'], { stdio: 'inherit' })
const stagedLibDir = 'target/lib/monochromatic-terminal'
let bin
if (home !== '') {
  const appsDir = join(home, '.local/share/applications')
  mkdirSync(appsDir, { recursive: true })
  copyFileSync('share/applications/monochromatic.terminal.desktop', join(appsDir, 'monochromatic.terminal.desktop'))
  const binDir = join(home, '.local/bin')
  mkdirSync(binDir, { recursive: true })
  copyFileSync('target/release/monochromatic-terminal', join(binDir, 'monochromatic-terminal'))
  const installedLibDir = join(home, '.local/lib/monochromatic-terminal')
  mkdirSync(installedLibDir, { recursive: true })
  for (const name of ['libghostty-vt.so', 'libghostty-vt.so.0', 'libghostty-vt.so.0.1.0']) {
    copyFileSync(join(stagedLibDir, name), join(installedLibDir, name))
  }
  bin = join(binDir, 'monochromatic-terminal')
} else {
  bin = './target/release/monochromatic-terminal'
}
execFileSync(bin, [], { stdio: 'inherit' })
"""
