/**
 Pi extension that runs `!` commands in the background and pokes the model when they finish.

 @module
 */

//region Extension registration

/**
 Registers the bash-poke extension.
 
 @param pi - Pi extension API receiving the event registrations
 
 @example
 ```ts
 bashPoke(pi);
 ```
 */
export default function bashPoke(pi: unknown,): void {
  void pi;
}

//endregion Extension registration
