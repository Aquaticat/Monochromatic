/**
 * Package behavior that tests must reach through the built artifact.
 *
 * @module
 */

/**
 * Returns its argument unchanged.
 *
 * @param text - value echoed back
 *
 * @returns same value
 *
 * @example
 * ```ts
 * parse('a');
 * ```
 */
export function parse(text: string,): string {
  return text;
}
