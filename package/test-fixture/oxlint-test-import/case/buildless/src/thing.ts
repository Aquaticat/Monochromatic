/**
 * Source of a package that builds nothing.
 *
 * @module
 */

/**
 * Returns its argument unchanged.
 *
 * @param text - value echoed back
 *
 * @returns same value
 *
 * @example
 * ```ts
 * thing('a');
 * ```
 */
export function thing(text: string,): string {
  return text;
}
