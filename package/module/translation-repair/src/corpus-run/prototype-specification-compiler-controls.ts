// PROTOTYPE ONLY: Candidate B local controls.

import { runSpecificationCompilerControls, } from '../prototype-spec-compiler-controls.ts';

runSpecificationCompilerControls();
console.log('PROTOTYPE specification compiler local controls accepted',);
