{
  "name": "guardrail",
  "description": "Blocks Agent resume calls (preventing polling loops) and bun test invocations (which misreport under the module-test harness).",
  "version": "0.0.1",
  "author": {
    "name": "Aquaticat"
  },
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bundle/node/index.mjs"
          }
        ]
      }
    ]
  }
}
