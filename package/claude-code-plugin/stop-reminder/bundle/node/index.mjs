#!/usr/bin/env node
import{text}from"node:stream/consumers";const DISABLING_VALUES=new Set([`0`,`off`,`false`,`no`]);function autoContinueEnabled(rawSetting){return!DISABLING_VALUES.has(rawSetting.trim().toLowerCase())}function autoContinueActive(){return autoContinueEnabled(process.env.MONOCHROMATIC_STOP_AUTO_CONTINUE??``)}function autoContinueReason(){return[`You are stopping while tracked work may remain.`,`Resume the next item now rather than reporting that you will resume it.`,`If your response named a next action, perform that action in this turn.`,`Keep writing status and next-step prose exactly as you would have;`,`do not delete, shorten, or rephrase it to avoid this check,`,`because a silent stop is worse than an announced one.`,`If work genuinely cannot proceed, name the concrete blocker`,`and the user action or external event required to clear it.`]}function isDigit(c){return c>=`0`&&c<=`9`}function isLowerAlpha(c){return c>=`a`&&c<=`z`}function isUpperAlpha(c){return c>=`A`&&c<=`Z`}function isAlphaNum(c){return isLowerAlpha(c)||isUpperAlpha(c)||isDigit(c)}function isWordChar(c){return isAlphaNum(c)||c===`_`}function isWhitespace(c){return c===` `||c===`	`||c===`
`||c===`\r`||c===`\f`||c===`\v`}function stripBetweenDelims({text,openDelim,closeDelim,disallowedInside}){let parts=[],cursorIndex=0;for(let openIndex=text.indexOf(openDelim,0);openIndex!==-1;openIndex=text.indexOf(openDelim,cursorIndex)){let closeSearchStart=openIndex+openDelim.length,closeIndex=text.indexOf(closeDelim,closeSearchStart);if(closeIndex===-1)break;let inside=text.slice(closeSearchStart,closeIndex);disallowedInside!==void 0&&inside.includes(disallowedInside)?(parts.push(text.slice(cursorIndex,closeSearchStart)),cursorIndex=closeSearchStart):(parts.push(text.slice(cursorIndex,openIndex)),cursorIndex=closeIndex+closeDelim.length)}return parts.push(text.slice(cursorIndex)),parts.join(``)}function stripLinesStartingWith({text,prefix}){return text.split(`
`).filter(function(line){return!line.trimStart().startsWith(prefix)}).join(`
`)}const PHRASE_NOT_FOUND=Symbol(`text-scan/phrase-not-found`);function boundariesSatisfied({haystack,phrase,index}){let before=index===0?``:haystack.charAt(index-1),afterIndex=index+phrase.length,after=afterIndex>=haystack.length?``:haystack.charAt(afterIndex),firstChar=phrase.charAt(0),lastChar=phrase.at(-1)??``,startBoundaryOk=!isWordChar(firstChar)||before===``||!isWordChar(before),endBoundaryOk=!isWordChar(lastChar)||after===``||!isWordChar(after);return startBoundaryOk&&endBoundaryOk}function containsWordBoundedPhrase({haystack,phrase}){if(phrase.length===0)return!1;let lowerHaystack=haystack.toLowerCase(),lowerPhrase=phrase.toLowerCase();for(let cursorIndex=lowerHaystack.indexOf(lowerPhrase,0);cursorIndex!==-1;cursorIndex=lowerHaystack.indexOf(lowerPhrase,cursorIndex+1))if(boundariesSatisfied({haystack:lowerHaystack,phrase:lowerPhrase,index:cursorIndex}))return!0;return!1}function containsAnyOfWordBounded({haystack,phrases}){for(let phrase of phrases)if(containsWordBoundedPhrase({haystack,phrase}))return{phrase};return PHRASE_NOT_FOUND}const FILE_EXTENSIONS=[`ts`,`tsx`,`js`,`jsx`,`json`,`md`,`yaml`,`yml`,`toml`,`rs`,`py`];function isPathChar(c){return isAlphaNum(c)||c===`_`||c===`.`||c===`/`||c===`@`||c===`-`}function hasFilePathWithExtension(line){let lower=line.toLowerCase();function checkExt(ext){let token=`.${ext}`;for(let idx=lower.indexOf(token,0);idx!==-1;idx=lower.indexOf(token,idx+1)){if(idx===0||!isPathChar(line.charAt(idx-1)))continue;let endIdx=idx+token.length;if(!(endIdx<line.length&&isWordChar(line.charAt(endIdx))))return!0}return!1}return FILE_EXTENSIONS.some(function(ext){return checkExt(ext)})}function hasLineNumberSuffix(line){function countDigits(at){let count=0;for(;count<5&&at+count<line.length&&isDigit(line.charAt(at+count));)count+=1;return count}for(let colonIdx=line.indexOf(`:`,0);colonIdx!==-1;colonIdx=line.indexOf(`:`,colonIdx+1)){let digitCount=countDigits(colonIdx+1);if(digitCount>=1&&digitCount<=5){let afterIdx=colonIdx+1+digitCount;if(afterIdx>=line.length||!isWordChar(line.charAt(afterIdx)))return!0}}return!1}function hasAgentsMdMention(line){return containsWordBoundedPhrase({haystack:line,phrase:`AGENTS.md`})}function lineHasCitation(line){return hasFilePathWithExtension(line)||hasLineNumberSuffix(line)||hasAgentsMdMention(line)}function normaliseApostrophes(text){return text.replaceAll(`‘`,`'`).replaceAll(`’`,`'`)}const UNCERTAINTY_PHRASES=`probably.maybe.may be.may only be.worth testing.perhaps.possibly.presumably.presume.i think.i believe.i assume.i suspect.i imagine.i guess.i suppose.might be.could be.should be.not sure.not entirely sure.not certain.hard to say.hard to tell.difficult to say.difficult to tell.if i recall.if i remember.from what i recall.from what i remember.as far as i know.as far as i can tell.likely.worse than most.more than most.less than most`.split(`.`),ER_THAN_MOST_PHRASE=`er than most`,ER_NOT_FOUND=Symbol(`uncertainty-phrases/er-not-found`);function findErThanMost(text){let lower=text.toLowerCase();for(let idx=lower.indexOf(ER_THAN_MOST_PHRASE,0);idx!==-1;idx=lower.indexOf(ER_THAN_MOST_PHRASE,idx+1)){if(idx===0||!isWordChar(lower.charAt(idx-1)))continue;let endIdx=idx+12;if(endIdx<lower.length&&isWordChar(lower.charAt(endIdx)))continue;let wordStart=idx-1;for(;wordStart>0&&isWordChar(text.charAt(wordStart-1));)--wordStart;return text.slice(wordStart,endIdx)}return ER_NOT_FOUND}function containsErThanMost(text){return findErThanMost(text)!==ER_NOT_FOUND}const DISMISSAL_PHRASES=[`the project doesn't use`,`the project doesnt use`,`project doesn't use`,`project doesnt use`,`we don't use`,`we dont use`,`the codebase doesn't use`,`the codebase doesn't have`,`the codebase doesnt use`,`the codebase doesnt have`,`codebase doesn't use`,`codebase doesn't have`,`codebase doesnt use`,`codebase doesnt have`,`doesn't apply here`,`doesnt apply here`,`is already handled by`,`is already covered by`];function stripCodeBlocks(text){return stripBetweenDelims({text,openDelim:"```",closeDelim:"```"})}function stripInlineCode(text){return stripBetweenDelims({text,openDelim:"`",closeDelim:"`"})}function stripBlockquotes(text){return stripLinesStartingWith({text,prefix:`>`})}function stripQuotedStrings(text){return stripBetweenDelims({text:stripBetweenDelims({text,openDelim:`"`,closeDelim:`"`,disallowedInside:`
`}),openDelim:`'`,closeDelim:`'`,disallowedInside:`
`})}function stripNonProseRegions(text){return stripQuotedStrings(stripBlockquotes(stripInlineCode(stripCodeBlocks(text))))}const NO_MATCH=Symbol(`stop-reminders/prose-detector-match-absent`);function findUncertainty(prose){let phraseHit=containsAnyOfWordBounded({haystack:prose,phrases:UNCERTAINTY_PHRASES});if(phraseHit!==PHRASE_NOT_FOUND)return{phrase:phraseHit.phrase};if(containsErThanMost(prose)){let fragment=findErThanMost(prose);return{phrase:fragment===ER_NOT_FOUND?`er than most`:fragment}}return NO_MATCH}function findCategoricalDismissal(prose){let lines=prose.split(`
`);for(let line of lines){if(lineHasCitation(line))continue;let hit=containsAnyOfWordBounded({haystack:normaliseApostrophes(line),phrases:DISMISSAL_PHRASES});if(hit!==PHRASE_NOT_FOUND)return{phrase:hit.phrase}}return NO_MATCH}const RHETORICAL_PREFIXES=[`what if`,`why does`,`why would`,`how does`,`have you ever`];function startsWithWordBounded({s,prefix}){if(!s.startsWith(prefix))return!1;if(s.length===prefix.length)return!0;let next=s.charAt(prefix.length);return(next<`a`||next>`z`)&&(next<`A`||next>`Z`)&&(next<`0`||next>`9`)&&next!==`_`}function findLastSentenceStart(text){function skipWs(idx){let at=idx;for(;at<text.length&&isWhitespace(text.charAt(at));)at+=1;return at}function walk(at){for(let cursor=at;cursor>0;--cursor){let prev=text.charAt(cursor-1);if(`.!?`.includes(prev)&&isWhitespace(text.charAt(cursor)))return skipWs(cursor)}return 0}return walk(text.length-1)}function findTrailingQuestion(prose){let trimmed=prose.slice(-500).trimEnd();if(!trimmed.endsWith(`?`))return NO_MATCH;let sentenceStart=findLastSentenceStart(trimmed),sentence=trimmed.slice(sentenceStart);if(sentence.length===0)return NO_MATCH;let firstChar=sentence.charAt(0);if(firstChar<`A`||firstChar>`Z`)return NO_MATCH;let lower=sentence.toLowerCase();for(let prefix of RHETORICAL_PREFIXES)if(startsWithWordBounded({s:lower,prefix}))return NO_MATCH;return{sentence}}function stopRemindersHandler(event){let responseQualityApplies=!event.stop_hook_active,prose=stripNonProseRegions(event.last_assistant_message??``),match=responseQualityApplies?findUncertainty(prose):NO_MATCH,dismissal=responseQualityApplies?findCategoricalDismissal(prose):NO_MATCH,question=responseQualityApplies?findTrailingQuestion(prose):NO_MATCH,reasons=[];return match!==NO_MATCH&&reasons.push(`Your response contains uncertain language ("${match.phrase}").`,`Search for evidence, read the relevant code, or check documentation.`,`Always research thoroughly before responding.`,`If you have already investigated and the uncertainty is genuinely warranted,`,`say so explicitly and continue with your response.`,`This may be a false positive; use your judgement.`),dismissal!==NO_MATCH&&reasons.push(`Your response contains an uncited categorical dismissal ("${dismissal.phrase}").`,`Categorical dismissals are one rg/find/config-read/AGENTS.md-grep away from being verified.`,`Cite the search result inline on the same line as the dismissal:`,`a file path with an extension, a path:line reference, or the literal "AGENTS.md".`,`If the dismissal was wrong, fold the now-relevant option back into the analysis.`,`This may be a false positive; use your judgement.`),question!==NO_MATCH&&reasons.push(`Your response ends with a question to the user ("${question.sentence}").`,`Use the AskUserQuestion tool to ask the user instead of ending your response with a question.`,`The AskUserQuestion tool ensures the user sees and can respond to your question directly.`,`Rephrase your question as an AskUserQuestion tool call and continue.`),question===NO_MATCH&&autoContinueActive()&&reasons.push(...autoContinueReason()),reasons.length>0?{decision:`block`,reason:reasons.join(` `)}:{}}function stopRemindersParser(raw){return JSON.parse(raw)}function stopRemindersWriter(output){return JSON.stringify(output)}const NO_STDOUT=Symbol(`claude-code-plugin-runtime/hook-intentionally-emits-no-stdout-bytes`);async function runHookPlugin({parser,handler,writer}){let serialized=writer(await handler(parser(await text(process.stdin))));serialized!==NO_STDOUT&&process.stdout.write(serialized)}await runHookPlugin({parser:stopRemindersParser,handler:stopRemindersHandler,writer:stopRemindersWriter});export{};