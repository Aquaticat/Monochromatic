# Scoped repology config for package index generation.
# Only processes repos corresponding to our supported package managers:
#   brew, apt, dnf, pacman, apk, zypper, scoop, choco
#
# Overrides REPOSITORIES to limit fetch/parse to these repos only.

REPOSITORIES = [
    "ubuntu_26_04",
    "fedora_43",
    "arch",
    "alpine_3_23",
    "opensuse_tumbleweed",
    "homebrew",
    "chocolatey",
    "scoop",
]

STATE_DIR = "/state"
PARSED_DIR = "/parsed"

# Disable safety checks -- we're processing a subset of repos
# so package counts will be below the default minimums
ENABLE_SAFETY_CHECKS = False
