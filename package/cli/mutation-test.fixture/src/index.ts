/**
 * Fixture surface: half-tested and untested helpers for mutation runs.
 *
 * @example
 * ```ts
 * import { clampedSum } from '@monochromatic-dev/cli-mutation-test.fixture/ts';
 * ```
 */

export {
  clampedSum,
  describeSign,
} from './calc.ts';
export { doubled, } from './untested.ts';
