# DRY violations audit

Generated 2026-05-09 from a mechanical sweep of the 83 packages under `package/`.

This audit covers behaviorally equivalent code or config copies (DRY).
Structural drift across same-class files (package naming,
 README headings,
 version fields,
`private` field presence) is already covered by [the consistency audit](consistency.md);
findings are not duplicated here.

## Scope

In scope:

- Source files reimplementing logic that already lives in a shared workspace package
- Constants redeclared under different names across packages
- Build,
   lint,
   and test config files copy-pasted with drift
- Package manifest fields (`exports`,
   `devDependencies`) repeated verbatim across packages
- Forks where two packages share most of their tree
- Single-occurrence helpers in non-shared packages whose shape is generic
  enough that they should be folded out into a shared module so the next caller
  can reuse them instead of writing their own copy.
   These do not violate DRY today
  but encode the same future-DRY-violation risk:
   the second caller is going to
  reinvent them.
   Section:
   "Reusable utilities awaiting extraction".

Excluded by construction:

- Files generated by [`file-enforcer.config.ts`](../../file-enforcer.config.ts):
  `CLAUDE.md`,
   `mise.toml` at repo root,
   `forbidden-strings.local.txt`,
  `forbidden-strings.append.local.txt`,
   `.factory/skills/*/*.md`,
   `.claude/skills/*/*.md`
- `package/test-fixture/*` where duplicate inputs are the point of the fixture
- Cross-runtime `*.browser.ts` / `*.node.ts` variants where the platform forces separate code
- `pnpm-lock.yaml` and other lockfiles

## Methodology

Per category,
 files were enumerated,
 hashed with `sha256sum`,
 and bucketed by hash to find
exact byte-level duplicates.
 Near-duplicates were sampled and diffed pairwise.
For source-level duplication,
 `rg` queries enumerated function definitions,
 type aliases,
const declarations,
 regex patterns,
 and shared filenames;
 the most-frequent occurrences
were inspected line by line.

The "Reusable utilities awaiting extraction" section was generated separately:
candidate function names were enumerated by utility-shape patterns
(`format*`,
 `parse*`,
 `escape*`,
 `normalize*`,
 `encode*`,
 `decode*`,
 `is*`,
`require*`,
 `ensure*`,
 `truncate*`,
 `chunk*`,
 etc.),
 occurrence counts captured,
and each candidate's body inspected to confirm primitive-in / primitive-out shape
and absence of package-specific dependencies.
 The current `module/es` taxonomy
(via `find package/module/es/src -type d`) was checked first so candidates already
covered by an existing export were excluded.

Every finding cites file paths and where applicable line numbers,
 so claims can be
verified by opening the file.
 Numeric counts in headings are exact occurrence counts at
audit time,
 not estimates.

## Findings

The findings are grouped:
 build configuration first,
 then large structural duplication
(package forks),
 then function-level duplication,
 type-level duplication,
 constant-level
duplication,
 file-template duplication,
 and miscellaneous patterns.

### Build configuration

#### Three packages declare per-platform mise tasks without matching config files

**Resolved 2026-05-09**:
 each package's bare `tsdown.config.ts` was renamed to the
platform-suffixed form matching its declared mise tasks.
`package/module/hyperscript/` now has `tsdown.browser.config.ts` only;
`package/figma/{kiwi,penpot}/` now have both `tsdown.browser.config.ts` and
`tsdown.node.config.ts`.
 `dist/final/{neutral,node}/` populates accordingly.

`mise.toml` files in three packages declare `build:js:browser` and/or `build:js:node`
task children whose root templates pass `--config tsdown.<platform>.config.ts`,
 but the
matching config file does not exist on disk:

- `package/module/hyperscript/mise.toml` declares `build:js:browser`;
   only
  `tsdown.config.ts` is present
- `package/figma/kiwi/mise.toml` declares `build:js:browser` and
  `build:js:node`;
   only `tsdown.config.ts` is present
- `package/figma/to-penpot/mise.toml` declares `build:js:browser` and
  `build:js:node`;
   only `tsdown.config.ts` is present

Empirical effect (verified by inspecting `dist/final/`):
 each contains only `neutral/`
and `types/`,
 never `node/` or `browser/`.
 The declared task names misrepresent what the
build produces.

Suggested resolution:
 drop the unused per-platform task children from the three
`mise.toml` files,
 or add the matching suffixed config files if the per-platform output
is wanted.

#### Seven webapp packages each ship a near-identical `tsdown.client.config.ts`

`tsdown.client.config.ts` appears in seven packages as a thin override of
`@monochromatic-dev/config-tsdown/.client.ts`.
 Two are byte-identical
(`package/webapp-productivity/rss/tsdown.client.config.ts` and
`package/webapp-search/exa-search/tsdown.client.config.ts`);
 the remaining five differ
only in their `entry` arrays:

- `package/webapp-productivity/done/tsdown.client.config.ts`
- `package/webapp-productivity/done-h-css-test/tsdown.client.config.ts`
- `package/webapp-productivity/doodle-widget/tsdown.client.config.ts`
- `package/webapp-productivity/syllable-break-demo/tsdown.client.config.ts`
- `package/webapp-edu/paper2vn/tsdown.client.config.ts`
- `package/ssg/aquati.cat/tsdown.client.config.ts`
- `package/webapp-content/messages-demo/tsdown.client.config.ts`

Suggested resolution:
 this is the third structural duplication of the same shape
(`tsdown.browser.config.ts`,
 `tsdown.node.config.ts`,
 `tsdown.client.config.ts`).
Out of scope for a single fix,
 but worth noting that the per-package one-liner pattern
multiplies linearly with package count.

### Major package forks

#### `webapp-productivity/done` and `webapp-productivity/done-postcss` are intentional forks

Status:
 superseded.
 The two packages share **52 of 67 / 79 source file paths** (relative
paths after stripping the package prefix),
 but the duplication is intentional and kept
by design for side-by-side CSS framework comparison;
 see the package READMEs.

Of those 52:

- 4 files are byte-identical:
  - `src/client/lib/page-data.ts`
  - `src/env.d.ts`
  - `src/lib/types.ts`
  - `src/server/pages/settings.ts`
- 17 files have under 50 lines of diff each (mostly imports and minor renames)
- 31 files have 50 or more lines of diff but share substantial structure

Examples of near-twins (line counts then diff line counts):

- `src/server/pages/layout.ts`:
   148 lines each,
   12-line diff
- `src/lib/db.ts`:
   82 vs 85 lines,
   30-line diff
- `src/server/api/timer.ts`:
   roughly 100-line diff
- `src/server/pages/inbox.ts`:
   11-line diff
- `src/server/pages/in-progress.ts`:
   11-line diff
- `src/lib/args.ts`:
   12-line diff
- `src/lib/ai/client.ts`:
   12-line diff

Resolution:
 not consolidated.
 `done` is canonical (h-css).
 `done-postcss` is kept as a
PostCSS `@apply` reference;
 future variants (`done-tailwind`,
 `done-vanilla-extract`,
 etc.)
may join it.
 The earlier suggestion to extract a `done-shared/` package was rejected
because the comparison only works while each variant carries its own full implementation
of the surfaces being compared.
 The non-CSS overlap is the cost of keeping the comparison
honest at full-app scale.

#### `pi/terminal-title` and `claude-code-plugin/source/handler/terminal-title` are forks

Both directories implement the same terminal-title generation logic for two consumer
runtimes (pi extension and Claude Code plugin):

- `package/pi-plugin/terminal-title/src/formatter-utils.ts`:
   219 lines
- `package/claude-code-plugin/source/src/handler/terminal-title/formatter-utils.ts`:
   154 lines
- 132 lines of diff between them

Plus parallel files:

- `index.ts` (219 vs 158 lines)
- `tool-titles.ts` (115 vs 82 lines,
   160-line diff)
- `title-builder.ts` (179 lines,
   no exact peer;
   the claude-code-plugin fork splits it
  into `tool-titles-search.ts` and `tool-titles-extended.ts`)

Functions duplicated across both forks:

- `titleForTool` and `titleForEvent`
- `stringField`,
   `shortPath`,
   `shortCommand`,
   `truncate`

Suggested resolution:
 extract the formatter and title-builder into a new
`package/module/terminal-title-core/` package with a runtime-agnostic public API.
Both consumers wrap it with their respective hook adapter.

#### Four `webapp-forge/stress/scenarios/*.ts` files share a scenario shell

The four scenarios (`force-push.ts`,
 `wide-service.ts`,
 `hot-repo.ts`,
`bursty-comment.ts`) each declare:

- `const DEFAULT_BURST_EVENTS = <number>;`
- `const DEFAULT_BURST_DURATION_MS = 1_000;`
- `const REPO_SEED_FACTOR = 1_000_000;` (in three of four)
- `function readConfig(): <ScenarioConfig> { ... }` (one per scenario,
   same shape)

Suggested resolution:
 lift the constants and the `readConfig` shape into
`package/webapp-forge/stress/src/scenarios/shared.ts` (which already exists).

#### Four single-page-app `build.ts` files duplicate the inline-bundling shell

These files all use the same shape (`PACKAGE_DIR -> dist/final/index.html` with inlined
JS+CSS),
 differing only in package name and the specific assets bundled:

- `package/webapp-edu/paper2vn/src/build.ts` (90 lines)
- `package/webapp-productivity/syllable-break-demo/src/build.ts` (76 lines)
- `package/webapp-productivity/doodle-widget/src/build.ts` (99 lines)
- `package/dev-script/inference-canary-viewer/src/build.ts` (199 lines,
   with extras)

`paper2vn` and `syllable-break-demo` differ by only 69 lines of diff out of 90/76 lines
total:
 roughly 70% of `build.ts` is structural copy-paste between them.

All four declare:

- `const DIST_DIR = join(PACKAGE_DIR, 'dist', 'final');`
- A leading `console.error('[<name>] building...')` log

Suggested resolution:
 extract a `build-single-page-app(...)` helper into
`package/build-tool/css/` or a new sibling `package/build-tool/inline-html/`.

#### Six `paper2vn` screen modules each redefine `mount(root)` with the same signature

`package/webapp-edu/paper2vn/src/client/screens/{menu,saves,log,settings,lecture,select-topic}.ts`
each declare `function mount(root: HTMLElement,): void`.
 The bodies are screen-specific,
but the entry-point pattern (a single `mount` per screen) is duplicated six times.

Suggested resolution:
 factor a `Screen` interface and a registration table;
 each module
exports a `Screen` value rather than a free function.

### Function-level duplication

#### `resolvePort` is reimplemented in five files

Verbatim 12-line bodies (one variant uses env only):

- `package/webapp-forge/server/src/index.ts`
- `package/webapp-content/messages-demo/src/server.ts`
- `package/webapp-productivity/done/src/server.ts`
- `package/webapp-productivity/done-h-css-test/src/server.ts`
- `package/desktop-daemon/editord/src/server/index.ts` (env-only variant)

Each parses `--port=` from `process.argv` (via `getArgumentValue`),
 falls back to
`process.env.PORT`,
 and finally to `DEFAULT_PORT`.

Suggested home:
 `package/module/es/src/path/` already hosts cross-package helpers;
 add
`package/module/es/src/server/resolve-port.ts` (or similar).

#### `getArgumentValue` (or `getFlag`) is reimplemented in five files

Four `lib/args.ts` files declare an identical `getArgumentValue(name: string)`
helper that scans `process.argv` for `--name=value`:

- `package/webapp-forge/server/src/lib/args.ts`
- `package/webapp-productivity/done/src/lib/args.ts`
- `package/webapp-productivity/done-h-css-test/src/lib/args.ts`
- `package/webapp-content/messages-demo/src/lib/args.ts`

A fifth file,
 `package/webapp-forge/stress/src/scenarios/shared.ts:getFlag`,
declares the same logic under a different name (`getFlag` instead of
`getArgumentValue`).

The `lib/args.ts` docblocks even cite each other as "mirrors",
 showing the duplication
is acknowledged.
 Three of the four packages use `@optique` elsewhere for richer CLI
parsing,
 so this single helper exists only because no shared module hosts it.

Notable:
 `webapp-forge/stress` and `webapp-forge/server` are sibling packages under
`webapp-forge/`.
 The fact that `stress/src/scenarios/shared.ts` already exists as a
local extraction but `webapp-forge/server` does not import from it shows the
within-`webapp-forge/` boundary is itself porous.

Suggested home:
 `package/module/es/src/cli/get-argument-value.ts`.

#### `requireParam` is reimplemented in four files

- `package/webapp-content/messages-demo/src/server.ts`
- `package/webapp-forge/server/src/server/routes/helpers.ts`
- `package/webapp-productivity/done/src/server.ts`
- `package/webapp-productivity/done-h-css-test/src/server-api-routes.ts`

Each takes a `URLPattern.exec` result and throws if a named group is missing.

Suggested home:
 a small `package/module/es/src/web/` route helper,
 since the function
is HTTP-route-agnostic.

#### `isRecord` is reimplemented in six files

Two equivalent shapes:

Shape A (`typeof === 'object' && !== null`):

- `package/webapp-productivity/doodle-widget/src/source-url.ts`
- `package/webapp-edu/paper2vn/src/client/dialogue/generator.ts`
- `package/webapp-productivity/done-h-css-test/src/server/api/tasks-parse.ts`
- `package/webapp-productivity/done/src/server/api/task-validation.ts`

Shape B (also rules out arrays):

- `package/webapp-content/messages-demo/src/server/api/messages.ts`
- `package/webapp-content/messages-demo/src/server/api/drafts.ts`

Suggested home:
 `package/module/es/src/types/t object/` already houses similar guards;
add `is-record.ts` as a public export.

#### `stringField` is reimplemented in four files

Each takes `(input, key)` and returns the string value at `input[key]` if it is a
string:

- `package/pi-plugin/terminal-title/src/formatter-utils.ts`
- `package/claude-code-plugin/source/src/handler/terminal-title/formatter-utils.ts`
- `package/webapp-content/messages-demo/src/server/api/messages.ts`
- `package/webapp-content/messages-demo/src/server/api/drafts.ts`

Suggested home:
 `package/module/es/src/types/t object/`.

#### `parseStringArray` is reimplemented in four files

Two shapes (one parses JSON-encoded strings,
 one validates `unknown` input):

- `package/webapp-productivity/done/src/server/api/task-validation.ts`
- `package/webapp-productivity/done/src/lib/db/task-mapping.ts`
- `package/webapp-productivity/done-h-css-test/src/server/api/tasks-parse.ts`
- `package/webapp-productivity/done-h-css-test/src/lib/db/tasks-helpers.ts`

Suggested resolution:
 hoist the validation form into `task-validation` shared with
`done-h-css-test`;
 hoist the JSON-decode form into the shared `lib/db/`.

#### `escapeHtml` is reimplemented in three files

**Resolved 2026-05-09**:
 both consumer copies were deleted;
 the
`messages-demo` export had no importers,
 and `ssg-test/client/search.ts` now
imports `escapeHtml` from `@monochromatic-dev/module-hyperscript/ts`.
 Only the
canonical source remains.

- `package/module/hyperscript/src/html/index.ts:escapeHtml` (canonical)
- `package/webapp-content/messages-demo/src/server/pages/_layout.ts:escapeHtml`
- `package/ssg/aquati.cat/src/client/search.ts:escapeHtml`

Each replaces `&`,
 `<`,
 `>`,
 `"` with their HTML entities.

Suggested resolution:
 import from `@monochromatic-dev/module-hyperscript/ts` (the
canonical source) in the two consumers.

#### `renderPage` is reimplemented in seven files

The signature varies,
 but each returns an HTML document around app-specific content:

- `package/webapp-content/messages-demo/src/server/pages/_layout.ts`
- `package/webapp-edu/paper2vn/src/page.ts`
- `package/webapp-productivity/syllable-break-demo/src/page.ts`
- `package/dev-script/inference-canary-viewer/src/html/page.ts`
- `package/webapp-productivity/doodle-widget/src/page.ts`
- `package/webapp-productivity/done-h-css-test/src/server/pages/layout.ts`
- `package/webapp-productivity/done/src/server/pages/layout.ts`

Suggested resolution:
 extract a `renderHtmlShell({title, body, ...})` helper into
`package/module/hyperscript/` or a new `package/module/html-shell/`.

#### `jsonResponse` is reimplemented in three files

Each wraps `Response.json(payload, { status })`:

- `package/webapp-productivity/done/src/server/api/http-utils.ts:jsonResponse`
- `package/webapp-productivity/done-h-css-test/src/server/api/timer.ts:jsonResponse`
- `package/webapp-productivity/done-h-css-test/src/server/api/tasks.ts:jsonResponse`

Suggested resolution:
 hoist into `package/module/es/src/web/json-response.ts`.
 Note that
the wrapper itself is one line;
 the duplication exists because each module wants
`HTTP_OK` as a default and inlines a fresh constant declaration too.

#### `buildHeader` is reimplemented in four files (with different signatures)

- `package/webapp-search/exa-search/src/asset-header.ts:buildHeader`
- `package/webapp-productivity/done-h-css-test/src/client/components/side-drawer-helpers.ts:buildHeader`
- `package/mcp/nvim/src/tool-helpers.ts:buildHeader`
- `package/webapp-productivity/done/src/client/components/side-drawer-nav.ts:buildHeader`

The two `done` forks share a signature;
 the others diverge.
 Listed for completeness;
this is more "name collision" than DRY violation.

#### `resolveDatabasePath` / `normalizeDatabasePath` / `ensureDatabaseDirectoryExists` triple is reimplemented in four files

Each `lib/db.ts` (or equivalent) defines all three of these with the same signatures:

- `package/webapp-productivity/done/src/lib/db.ts`
- `package/webapp-productivity/done-h-css-test/src/lib/db.ts`
- `package/webapp-forge/server/src/data/db.ts`
- `package/webapp-content/messages-demo/src/lib/db.ts`

The three functions together form a complete database-path bootstrap routine.
 With the
current spread,
 every webapp that needs a SQLite path reinvents the path resolution.

Suggested resolution:
 extract a `resolveSqliteDatabase({ defaultPath })` helper into
`package/module/es/` or a new `package/module/sqlite-bootstrap/`.

#### `runMigrations` is reimplemented in three files

- `package/webapp-productivity/done/src/lib/db/migrations.ts`
- `package/webapp-productivity/done-h-css-test/src/lib/db-migrations.ts`
- `package/webapp-content/messages-demo/src/lib/db/migrations.ts`

Each iterates a list of SQL statements and runs them sequentially against a
`Database` instance.
 The migration content differs per app;
 the runner is generic.

Suggested resolution:
 factor a `runMigrations(database, migrations)` helper into the
SQLite-bootstrap module proposed above.

#### `completeTask` and `openTask` duplicated across the `done` fork

- `package/webapp-productivity/done/src/lib/db/task-timer.ts:completeTask` and
  `package/webapp-productivity/done-h-css-test/src/lib/db/tasks-timer.ts:completeTask`
- `package/webapp-productivity/done/src/client/inbox.ts:completeTask` and
  `package/webapp-productivity/done-h-css-test/src/client/inbox.ts:completeTask`
- `package/webapp-productivity/done/src/client/{search,in-progress,inbox}.ts:openTask`
  and `package/webapp-productivity/done-h-css-test/src/client/inbox.ts:openTask`

These collapse if the `done` fork is consolidated (see fork finding above).

#### `findRepoRoot` in ssg-test reimplements `findMiseMonorepoRoot`

**Resolved 2026-05-09**:
 `git-dates.ts` now imports `findMiseMonorepoRoot` from
`@monochromatic-dev/module-es/find-monorepo-root`.
 The local `findRepoRoot`
function,
 its `cachedRepoRoot` cache,
 and the `access`/`dirname`/`join`
imports were deleted;
 the latent ostree bug is fixed by using the shared
helper's normalization.

`package/ssg/aquati.cat/src/lib/git-dates.ts:56` defines `findRepoRoot()`
walking up looking for `.git`.
 `package/module/es/src/path/find-monorepo-root.ts:199`
already exports `findMiseMonorepoRoot` walking up looking for `mise.toml` containing
`[monorepo]`,
 plus normalizes `/home/...` to `/var/home/...` for Fedora ostree.

The semantic distinction (`.git` vs `mise.toml`) is not exploited and the ostree
normalization is missing from `findRepoRoot`,
 which is a latent bug under ostree.

Suggested resolution:
 import `findMiseMonorepoRoot` from
`@monochromatic-dev/module-es/find-monorepo-root` in `git-dates.ts`.

#### Six `mount(root)` screen functions in paper2vn

Already covered as part of the paper2vn fork finding;
 listing here for completeness in
the function-level section.

#### `which(name)` reimplemented in two files in `cli/terminal-exec`

- `package/cli/terminal-exec/src/validate.ts:which`
- `package/cli/terminal-exec/src/windows.ts:which`

Both return the absolute path of the named binary or `null`.
 The two implementations
likely differ by Windows-specific PATHEXT handling.

Suggested resolution:
 keep the platform-specific copy if needed but expose a single
public entry point that dispatches;
 otherwise consolidate.

### Type-level duplication

#### `Logger` declared with two different shapes

**Resolved 2026-05-09**:
 `ssg-test/src/lib/types.ts` now re-exports `Logger`
from `@monochromatic-dev/module-logger/types`;
 the `ReturnType<typeof tagged>`
alias and the `tagged` import were removed.

- `package/module/logger/src/types.ts:6` (canonical,
   structural)
- `package/ssg/aquati.cat/src/lib/types.ts` (`ReturnType<typeof tagged>`)

Suggested resolution:
 replace the `ReturnType<typeof tagged>` with a direct import.

#### `CommandResult` declared locally in ssg-test

**Resolved 2026-05-10**:
 folded into the `nano-spawn` migration.
 The
local type now reads `type CommandResult = Pick<Result, 'stdout' | 'stderr'>`
with `Result` imported from `nano-spawn`.

`package/ssg/aquati.cat/src/lib/git-dates.ts:31` defines a local
`{ stdout, stderr }` type matching the public type returned by `nano-spawn`.

Suggested resolution:
 switch to `nano-spawn` and import its result type.

#### `ChatMessage` type declared in five places

- `package/webapp-productivity/done/src/lib/ai/client.ts`
- `package/webapp-productivity/done-h-css-test/src/lib/ai/client.ts`
- `package/module/image-diff/src/describe.ts`
- `package/dev-script/inference-canary/src/runner-types.ts`
- `package/desktop-daemon/hall-monitor/src/analyze.ts`

Each represents an OpenAI-compatible message envelope with `role` and `content`.
 Five
implementations track to four different LLM client patterns.

Suggested resolution:
 a `package/module/llm-type/` package or a section in
`package/module/es/src/types/`.
 Useful given the recurring openai/anthropic/openrouter
client pattern.

#### `ChatCompletionResponse` type declared in four places

- `package/webapp-productivity/done-h-css-test/src/lib/ai/client.ts`
- `package/webapp-productivity/done/src/lib/ai/client.ts`
- `package/webapp-edu/paper2vn/src/client/llm/openai-compatible.ts`
- `package/module/image-diff/src/describe.ts`

Same justification as `ChatMessage`.
 The two `done`-fork copies fold into the fork
consolidation.

#### `JsonRpcRequest`, `JsonRpcNotification`, `JsonRpcResponse` declared in two packages

- `package/mcp/stdio/src/json-rpc.ts` (used by MCP server scaffolding)
- `package/desktop-daemon/editord/src/server/lsp/json-rpc-encode.ts` (used by LSP)

Both protocols use JSON-RPC 2.0 wire format;
 the type definitions are the same
substring.

Suggested resolution:
 extract `package/module/json-rpc/` (or place under
`package/module/es/src/protocol/json-rpc/`).

#### `BlockerSummary` declared in six places (done fork)

- `package/webapp-productivity/done-h-css-test/src/lib/db/tasks-helpers.ts`
- `package/webapp-productivity/done-h-css-test/src/client/task-details.ts`
- `package/webapp-productivity/done-h-css-test/src/client/components/task-detail-types.ts`
- `package/webapp-productivity/done/src/lib/db/task-timer.ts`
- `package/webapp-productivity/done/src/client/task-details.ts`
- `package/webapp-productivity/done/src/client/components/task-detail-types.ts`

Three copies per fork.
 Folds into the fork consolidation.

#### `RenderResult` declared in four places

- `package/webapp-forge/server/src/worker/render-phase2.ts`
- `package/webapp-forge/server/src/worker/render.ts`
- `package/webapp-productivity/done/src/client/components/task-detail-render.ts`
- `package/webapp-productivity/done-h-css-test/src/client/components/task-detail-render.ts`

The two webapp-forge copies are intra-package;
 the two done copies fold into the fork
consolidation.
 Two intra-package webapp-forge copies should be unified directly.

#### `AutofillResult` declared in four places

- `package/webapp-productivity/done/src/server/api/ai-autofill.ts`
- `package/webapp-productivity/done-h-css-test/src/server/api/ai-autofill.ts`
- `package/webapp-productivity/done/src/client/components/task-detail-types.ts`
- `package/webapp-productivity/done-h-css-test/src/client/components/task-detail-types.ts`

Two pairs (server-side and client-side type for the same payload).
 Folds into the
fork consolidation.

#### `Translations`, `RootTranslation`, `TranslationFunctions` declared in two packages

- `package/webapp-edu/paper2vn/src/client/i18n/i18n-types.ts`
- `package/ssg/aquati.cat/src/i18n/i18n-types.ts`

Two consumers of `typesafe-i18n` each declare the same wrapper triple.

Suggested resolution:
 extract a `package/module/typesafe-i18n-types/` package,
 or
expose the types from a single existing helper module.

#### `HighlightGroup` type and `HIGHLIGHT_GROUPS` array declared in three packages

- `package/dev-script/inference-canary-viewer/src/client/tags.ts:HIGHLIGHT_GROUPS`
- `package/desktop-daemon/editord/src/client/highlight/tags.ts:HIGHLIGHT_GROUPS`
- `package/ssg/aquati.cat/src/client/highlight-groups.ts:HIGHLIGHT_GROUPS`

The first two share a 7-entry list (`keyword, string, comment, number, type, function,
property`).
 The third has 10 entries (extra:
 `heading, link, emphasis`).

Each cited the others in its docblock as the inspiration.
 The first two are
byte-near-identical;
 they should share the export.

Suggested resolution:
 extract a `package/module/highlight-groups/` package exporting a
configurable group set (or two presets).

#### `TaskStatus`, `TaskPriority`, `TaskComplexity`, `TaskRow`, `TaskCreateInput`, `TaskUpdateInput`, `TaskCardOptions` declared in `done` fork

Each appears in `package/webapp-productivity/done/src/lib/types.ts` (or
`task-card-render.ts`) and the corresponding paths under `done-h-css-test/`.
 All fold
into the fork consolidation.

#### `LayoutOptions` declared in two places

- `package/webapp-productivity/done/src/server/pages/layout.ts`
- `package/webapp-productivity/done-h-css-test/src/server/pages/layout.ts`

Folds into the fork consolidation.

### Plugin and protocol entry shells

#### Seven `claude-code-plugin/<plugin>/src/index.ts` files duplicate the same shell

Each plugin entry-point file is 26 to 38 lines,
 follows the same template:

```ts
#!/usr/bin/env bun
/** doc */
import { <plugin>Handler, <plugin>Parser, <plugin>Writer } from '@monochromatic-dev/claude-code-plugin-source/handler/<plugin>';
import { runHookPlugin, } from '@monochromatic-dev/claude-code-plugin-source/runtime';
await runHookPlugin({ parser, handler, writer });
```

Files:

- `package/claude-code-plugin/prompt-time/src/index.ts` (36 lines)
- `package/claude-code-plugin/guardrail/src/index.ts` (35 lines)
- `package/claude-code-plugin/bash-output-filter/src/index.ts` (38 lines)
- `package/claude-code-plugin/stop-reminder/src/index.ts` (36 lines)
- `package/claude-code-plugin/terminal-title/src/index.ts` (27 lines)
- `package/claude-code-plugin/claude-spawn/src/index.ts` (26 lines)
- `package/claude-code-plugin/session-start-housekeeping/src/index.ts` (36 lines)

Per-file diff is ~12 lines (handler name in 4 imports,
 command alias in the example
JSON,
 plugin description in the docblock).
 Most of the file is repeated docblock
(`@example` JSON config,
 `@module`).

The runtime `runHookPlugin` already centralizes the dispatch logic;
 the shells exist
only because each marketplace-installable plugin needs its own `dist/final/node/index.mjs`
output.
 So the duplication is per-build-target,
 not per-feature.

Suggested resolution:
 a `dev-script/file-enforcer` rule that generates the seven
`index.ts` files from a single template plus a per-plugin manifest (handler name,
command alias,
 hook event,
 description).
 The shells then exist as generated files (like
`mise.toml` is generated from `mise.no-env.toml`) and drift between them becomes
mechanical.

#### MCP server base correctly extracted via `@monochromatic-dev/mcp-stdio`

Both `package/mcp/nvim/` and `package/mcp/mvm/` declare
`@monochromatic-dev/mcp-stdio` as a workspace dependency and import server-side helpers
(`defineTool`,
 `runStdioServer`,
 etc.) from it.
 No JSON-RPC plumbing is reimplemented
inside the consumer packages.

Listed as a positive finding (already-shared category) so the next audit does not flag
the three MCP packages for duplication.

#### `JsonRpcRequest`/`JsonRpcNotification`/`JsonRpcResponse` duplicated between mcp/stdio and editord/lsp

Already covered in the type-level section.
 The JSON-RPC base could host this triple
(or a `package/module/json-rpc-types/` package) so that LSP code can also consume the
shared definitions instead of redeclaring them.

### Documentation duplication

#### TROUBLESHOOTING files share prefixes but cover distinct topics

The TROUBLESHOOTING.
* tree (46 files) groups by leading segment as follows
(all groups inspected by reading the first three lines of each file):

- `bun-*` (3 files:
   fetch-streaming,
   fs-glob-dotfiles,
   test) plus `bundling.md`
  (4 files in the family):
   each addresses a distinct symptom;
   no content overlap.
- `pi-*` (2:
   compaction-empty-summary,
   safeguard):
   distinct.
- `css-*` (2:
   hidden-attribute-specificity,
   css-tooling):
   distinct.
- `dprint*` (2:
   dprint,
   dprint-exec):
   distinct.
- `performance.*` (3:
   performance,
   performance.
  build,
   performance.
  logging):
   distinct.

No duplicates among TROUBLESHOOTING files at the file level.
 Listed as a positive
finding so the next audit does not re-flag the prefix similarity.

#### TODO.performance is a navigation index

`TODO.performance.md` is a navigation hub linking to six area-specific files:
build,
 bundle,
 caching,
 infrastructure,
 runtime,
 monitoring.
 Each child file has a
distinct topic;
 the duplication is only in the heading conventions
(`# X Performance Todo` / `## Y Optimization`) which is structural,
 not behavioral.
Listed as a positive finding.

### Numeric and string constant duplication

> Note:
>  this section mixes two related-but-distinct categories.
>  **Redeclaration**
> (multiple files declare the same name with the same value) is straightforward DRY:
> consolidate into a single export.
>  **Name collision with mixed values** (multiple
> files declare the same name with *different* values) is a probable bug surface:
> the resolution is renaming or value alignment rather than consolidation.
>  Bullets
> below tag the category where it matters.

#### `MS_PER_SECOND = 1_000` declared in 16 files

The single value `1_000` (one second in milliseconds) is redeclared as a named const in:

- `package/webapp-content/messages-demo/src/server/pages/feed.ts`
- `package/webapp-productivity/done-h-css-test/src/lib/db/tasks-helpers.ts`
- `package/webapp-content/messages-demo/src/lib/db/sweep.ts`
- `package/dev-script/task-util/src/depends-parse.ts`
- `package/dev-script/inference-canary-viewer/src/html/overlay-meta.ts`
- `package/dev-script/inference-canary/src/linter-artifacts-timestamp.ts`
- `package/test-fixture/file-enforcer-perf/src/validate-resources.ts`
- `package/cli/mvm/src/virsh-wait.ts`
- `package/webapp-productivity/done-h-css-test/src/client/lib/task-card.ts`
- `package/dev-script/inference-canary/src/runner-stream-helpers.ts`
- `package/webapp-productivity/rss/src/interval.ts`
- `package/dev-script/inference-canary/src/runner-probe.ts`
- `package/webapp-productivity/done/src/lib/db/task-timer.ts`
- `package/desktop-daemon/hall-monitor/src/index.ts`
- `package/desktop-daemon/hall-monitor/src/analyze/memory.ts`
- `package/webapp-productivity/done/src/client/lib/format-tracked-time.ts`

Suggested home:
 a `package/module/es/src/time/units.ts` exporting `MS_PER_SECOND`,
`SECONDS_PER_MINUTE`,
 `MINUTES_PER_HOUR`,
 `HOURS_PER_DAY`.

#### `DECIMAL_RADIX = 10` declared in 15 files

Used as the second argument to `Number.parseInt`:

- `package/webapp-forge/stress/src/scenarios/shared.ts`
- `package/webapp-forge/seed/src/cli.ts`
- `package/webapp-forge/server/src/index.ts`
- `package/desktop-daemon/editord/src/server/index.ts`
- `package/webapp-content/messages-demo/src/server.ts`
- `package/webapp-content/messages-demo/src/client/composer.ts`
- `package/webapp-content/messages-demo/src/client/composer/helpers.ts`
- `package/webapp-content/messages-demo/src/server/api/messages.ts`
- `package/webapp-content/messages-demo/src/server/api/drafts.ts`
- `package/webapp-content/messages-demo/src/lib/seed.ts`
- `package/webapp-content/messages-demo/src/lib/pagination.ts`
- `package/webapp-forge/server/src/server/routes/me.ts`
- `package/webapp-forge/server/src/server/routes/helpers.ts`
- `package/webapp-productivity/done-h-css-test/src/server.ts`
- `package/webapp-productivity/done/src/server.ts`

Suggested home:
 `package/module/es/src/types/t string/t parse-int/`.

#### `SECONDS_PER_MINUTE = 60` declared in 10 files

- `package/claude-code-plugin/statusline/statusline.ts`
- `package/dev-script/inference-canary/src/linter-artifacts-timestamp.ts`
- `package/dev-script/inference-canary/src/runner-probe.ts`
- `package/webapp-content/messages-demo/src/server/pages/feed.ts`
- `package/webapp-content/messages-demo/src/lib/db/sweep.ts`
- `package/webapp-productivity/done-h-css-test/src/client/lib/task-card-helpers.ts`
- `package/webapp-productivity/rss/src/interval.ts`
- `package/webapp-productivity/done/src/client/lib/format-tracked-time.ts`
- `package/desktop-daemon/hall-monitor/src/index.ts`
- `package/desktop-daemon/hall-monitor/src/analyze/memory.ts`

Folds into the proposed `time/units.ts` module.

#### `FULL_PERCENT = 100` declared in 10 files (all in done-h-css-test)

All 10 are inside `package/webapp-productivity/done-h-css-test/src/client/`,
 mostly in
component style modules:

- `client/styles-utilities.ts`
- `client/inbox-styles.ts`
- `client/components/{side-drawer-styles-sidebar, side-drawer-styles-nav,
  toggle-switch-styles, task-detail-styles, side-drawer-styles, focus-dropdown-styles,
  side-drawer-styles-panel, search-bar-styles}.ts`
- `client/components/section-heading.ts`

A single import from a sibling module would dedupe all 10.
 The cause is the project
convention "magic literals as named const (exception:
 -2 through 2)":
 each style file
imports from the module-hyperscript CSS helpers but cannot import a const from a sibling
without a circular import in some cases,
 so each file declares its own.

Suggested home:
 a `package/webapp-productivity/done-h-css-test/src/client/constants.ts`
local module,
 or hoist into `module-hyperscript`.

#### `BYTES_PER_KIB = 1_024` declared under five names in 13 files

Already in scope.
 Names:

- `BYTES_PER_KIB` (6):
   `runtime-error/bun/src/oom.ts`,
  `webapp-content/messages-demo/src/{client/composer, lib/markdown-stream, lib/seed,
  server/pages/edit, server/pages/message}.ts`
- `KIB` (3):
   `cli/mvm/src/download-progress.ts`,
  `webapp-forge/server/src/server/routes/git.cli.unit.test.ts`,
  `webapp-forge/stress/src/scenarios/force-push.ts`
- `BYTES_PER_KB` (2):
   `desktop-daemon/{editord/src/constants.ts,
  hall-monitor/src/cycle.ts}`
- `BYTES_PER_UNIT` (1):
   `dev-script/page-weight/src/index.ts`
- `BYTES_PER_KILOBYTE` (1):
   `test-fixture/file-enforcer-perf/src/validate-benchmarks.ts`

Suggested home:
 `package/module/es/src/types/t number/t bytes/`.

#### `HEX_RADIX = 16` declared in 7 files

- `package/webapp-forge/seed/src/generate-phase2-helpers.ts`
- `package/webapp-forge/server/src/git/pkt-line.ts`
- `package/webapp-productivity/doodle-widget/src/client/export-pdf.ts`
- `package/desktop-daemon/editord/src/server/operations/hex-dump.ts`
- `package/ssg/aquati.cat/src/components/question-radio.ts`
- `package/ssg/aquati.cat/src/components/question-checkbox.ts`
- `package/ssg/aquati.cat/src/lib/icons/codepoints.ts`

Folds into the parse-int helper proposal.

#### `HOURS_PER_DAY = 24` declared in 5 files

- `package/dev-script/inference-canary/src/linter-artifacts-timestamp.ts`
- `package/webapp-productivity/done-h-css-test/src/client/lib/task-card-helpers.ts`
- `package/webapp-content/messages-demo/src/server/pages/feed.ts`
- `package/webapp-productivity/done/src/client/lib/format-tracked-time.ts`
- `package/webapp-content/messages-demo/src/lib/db/sweep.ts`

Folds into the proposed `time/units.ts` module.

#### `SECONDS_PER_HOUR = 3_600` declared in 4 files

- `package/webapp-content/messages-demo/src/server/pages/feed.ts`
  (composes from `MINUTES_PER_HOUR * SECONDS_PER_MINUTE`)
- `package/claude-code-plugin/statusline/statusline.ts`
- `package/webapp-productivity/done-h-css-test/src/client/lib/task-card-helpers.ts`
- `package/webapp-productivity/done/src/client/lib/format-tracked-time.ts`

Folds into the proposed `time/units.ts` module.

#### `HALF`, `QUARTER`, `THREE_QUARTERS` declared in 4 packages

- `package/webapp-edu/paper2vn/src/styles/tokens.ts`
- `package/webapp-productivity/doodle-widget/src/style-tokens.ts`
- `package/dev-script/inference-canary-viewer/src/chart/axis.ts`
- `package/desktop-daemon/editord/src/client/binary-viewer/media.styles.ts` (HALF only)

These are forced by the project rule "compose fractional values from -2 to 2".
 The
constants themselves can still be exported once.

Suggested home:
 `package/module/es/src/types/t number/t fraction/`.

#### HTTP status code constants declared across at least four files

- `package/webapp-forge/server/src/lib/http.ts` (canonical 7 codes)
- `package/webapp-content/messages-demo/src/lib/http.ts` (canonical 7 codes)
- `package/webapp-productivity/done/src/server/api/http-utils.ts` (4 codes)
- `package/webapp-productivity/done-h-css-test/src/{server-api-routes,client/lib/api,
  server/api/timer,server/api/tasks}.ts` (each declares the codes it uses inline)
- `package/webapp-forge/server/src/storage/adapter-s3.ts` (HTTP_NOT_FOUND,
   HTTP_OK)

The first two `lib/http.ts` files are byte-near-identical with self-acknowledging
docblocks ("Mirrors the pattern from
`package/webapp-content/messages-demo/src/lib/http.ts`").

Suggested home:
 `package/module/es/src/web/http-status.ts`.

#### `DEFAULT_PORT` declared in 8 packages with 4 different values

- `package/webapp-forge/server/src/index.ts`:
   3000
- `package/webapp-productivity/done/src/server.ts`:
   3000
- `package/webapp-productivity/done-h-css-test/src/server.ts`:
   3000
- `package/webapp-content/messages-demo/src/server.ts`:
   3000
- `package/desktop-daemon/editord/src/server/index.ts`:
   4400
- `package/webapp-search/ai-tree/src/index.ts`:
   4111
- `package/webapp-search/exa-search/src/port.ts`:
   4115
- `package/webapp-productivity/rss/src/port.ts`:
   4112

Four packages use the same default (3000) and would collide if running together.
 The
overlap is undocumented and a likely UX bug.

Suggested resolution:
 a port-allocation document or registry.
 For DRY purposes,
 no
shared module is needed;
 the constant is per-app by design.

#### `DEFAULT_DATABASE_PATH` declared in 5 files

- `package/webapp-content/messages-demo/src/lib/db.ts`:
   `./data/messages-demo.db`
- `package/webapp-forge/server/src/lib/auth.ts`:
   `./data/forge.db`
- `package/webapp-forge/server/src/data/db.ts`:
   `./data/forge.db` (duplicated within
  webapp-forge-server)
- `package/webapp-productivity/done-h-css-test/src/lib/db.ts`:
   `./data/done.db`
- `package/webapp-productivity/done/src/lib/db.ts`:
   `./data/done.db`

The two `webapp-forge-server` copies should be merged regardless of whether anything
else changes.

#### `PREVIEW_MAX_LENGTH = 200` declared in 6 files (all in messages-demo)

- `package/webapp-content/messages-demo/src/{client/composer.worker,
  lib/seed, client/composer/compile, client/composer/send, client/composer/edit,
  server/api/import}.ts`

Six files in one package each declare the same constant.
 A single
`messages-demo/src/lib/preview.ts` would dedupe.

#### `REPO_SEED_FACTOR = 1_000_000` declared in 4 files (all in webapp-forge)

- `package/webapp-forge/stress/src/scenarios/{wide-service, hot-repo}.ts`
- `package/webapp-forge/seed/src/{dataset, generate}.ts`

Suggested home:
 `package/webapp-forge/seed/src/lib/constants.ts` (a single shared
constants module across the seed and stress sibling packages).

#### `DEFAULT_BURST_DURATION_MS = 1_000` declared in all 4 stress scenarios

- `package/webapp-forge/stress/src/scenarios/{force-push, wide-service, hot-repo,
  bursty-comment}.ts`

Same value,
 same name;
 folds into the stress-scenarios shared shell finding.

#### `STYLES` const declared in 23 places

23 files declare `const STYLES = [...]` or `const STYLES = $({...})`.
 These are mostly
in `desktop-daemon/editord/src/client/` and `webapp-productivity/done-h-css-test/`.
Each is intra-package and represents a per-component style block,
 so this is a name
collision,
 not a DRY violation.

Listed for completeness so the next audit does not re-flag it.

#### `LINE_HEIGHT` declared with mixed values in 4 files

- `package/desktop-daemon/editord/src/client/editor/editor-pane.styles.ts`:
   `(2 + 1) / 2`
- `package/desktop-daemon/editord/src/client/inlay/styles.ts`:
   `(2 + 1) / 2`
- `package/webapp-productivity/done-h-css-test/src/client/components/top-nav-styles.ts`:
  `1 / 2 / 2`
- `package/ssg/aquati.cat/src/styles/constants.ts`:
   `1.6`

Same name,
 different values.
 Worth surfacing as a probable copy-paste bug:
 the first
two compose to 1.5,
 the third to 0.25,
 the fourth to 1.6.
 Either the value or the name
is wrong in at least two of the four.

#### `TOUCH_TARGET` declared with mixed values in 3 files

- `package/desktop-daemon/editord/src/client/styles/fullscreen-fab.styles.ts`:
   `2 + 1`
- `package/webapp-productivity/done-h-css-test/src/client/mixins.ts`:
   `3`
- `package/ssg/aquati.cat/src/styles/constants.ts`:
   `3` (exported)

Two values match;
 one is the same value composed from primitives.
 Should converge on a
single export.

#### `DEBOUNCE_MS` declared with three different values in 4 files

- `package/dev-script/file-enforcer/src/watch/watch-dir.ts:DEBOUNCE_MS = 100` (exported)
- `package/desktop-daemon/editord/src/client/search/search.ts:DEBOUNCE_MS = 150`
- `package/desktop-daemon/editord/src/server/operations/watch-filesystem-filter.ts:DEBOUNCE_MS = 200` (exported)
- `package/ssg/aquati.cat/src/client/search.ts:DEBOUNCE_MS = 200`

Listed so reviewers can decide whether the values reflect deliberate choice (likely yes)
or copy-paste drift.

#### `VERTICAL_OFFSET` declared with mixed values in 4 places (all in editord)

- `package/desktop-daemon/editord/src/client/{rename/rename-input, toast/toast,
  completion/completion-popup, hover/hover-popup}.ts`

Three of four use `4`;
 one uses `8`.
 Should converge on a single per-popup constants
module.

#### `FONT_WEIGHT_NORMAL = 400` and `FOCUS_OFFSET = -(1/2/2/2)` declared 3+ times in done-h-css-test

`FONT_WEIGHT_NORMAL` appears in three component style files;
 `FOCUS_OFFSET` in four.
Both are intra-package and would dedupe via a `client/style-tokens.ts` import.

### File-template duplication

#### `lib/args.ts` files in four webapps duplicate the same module

- `package/webapp-forge/server/src/lib/args.ts`
- `package/webapp-productivity/done/src/lib/args.ts`
- `package/webapp-productivity/done-h-css-test/src/lib/args.ts`
- `package/webapp-content/messages-demo/src/lib/args.ts`

Each contains the same `getArgumentValue` (already noted) plus identical companion
helpers (`hasArgument`).
 The docblocks self-acknowledge the duplication.

#### `lib/http.ts` files in two webapps duplicate the same module

- `package/webapp-forge/server/src/lib/http.ts`
- `package/webapp-content/messages-demo/src/lib/http.ts`

Both define the same 7 HTTP status code constants.
 The second file's docblock cites
the first as the pattern source.

#### `lib/db.ts` files in three webapps duplicate the same bootstrap

- `package/webapp-productivity/done/src/lib/db.ts` (82 lines)
- `package/webapp-productivity/done-h-css-test/src/lib/db.ts` (85 lines)
- `package/webapp-content/messages-demo/src/lib/db.ts` (165 lines)

Each defines `DEFAULT_DATABASE_PATH`,
 `normalizeDatabasePath`,
 `resolveDatabasePath`,
`ensureDatabaseDirectoryExists`,
 plus the `Database` instance constructor.

#### `env.d.ts` files: 4 byte-identical copies plus 5 distinct ones

Byte-identical (all `declare module '*.css' { ... }`):

- `package/webapp-productivity/done/src/env.d.ts`
- `package/webapp-productivity/done-h-css-test/src/env.d.ts`
- `package/webapp-productivity/rss/src/env.d.ts`
- `package/webapp-search/exa-search/src/env.d.ts`

Distinct (each declares its own `process.env` and asset modules):

- `package/webapp-forge/{server, seed, stress}/src/env.d.ts`
- `package/webapp-content/messages-demo/src/env.d.ts`
- `package/typeface/aquaticat/src/env.d.ts`

Suggested resolution:
 extract the `*.css` declaration into a shared
`@monochromatic-dev/config-typescript/env.d.ts` that downstream consumers
`/// <reference types="@monochromatic-dev/config-typescript/env" />`.
 The distinct
`env.d.ts` files keep their package-specific `process.env` extensions.

#### Identical `devDependencies` blocks across 26 packages (3 shapes)

Shape A `{ config-tsdown, config-typescript, @types/bun }` (11 packages):
`backup-path, inference-canary-viewer, paper2vn, cli/git`,
 plus all 7
`claude-code-plugin/*` (excluding `source` itself).

Shape B `{ config-typescript, @types/bun }` (9 packages):
`config/oxlint, oxlint-plugin/no-restricted-syntax, dev-script/{catalog-tighten,
vm-builder}, mcp/mvm, runtime-error/bun, desktop-daemon/hall-monitor,
cli/{terminal-exec, rgffplay}`.

Shape C `{ config-typescript, module-test, @types/bun }` (6 packages):
`oxlint-plugin/stylistic, dev-script/task-util, mcp/{nvim, stdio}, cli/mvm,
claude-code-plugin/source`.

Version pins (`workspace:*`,
 `catalog:`) are also identical.

Resolution path is not obvious:
 pnpm has no "default devDependencies" mechanism.
 The
catalog already deduplicates version pins.
 A generator script (in
`package/dev-script/file-enforcer/`) could enforce one of the three shapes per package
type,
 but this is informational rather than actionable today.

#### Vendored copy of `pi-coding-agent` helpers in morph-compact

`package/pi-plugin/morph-compact/src/pi-utils.ts:1-485` reimplements `convertToLlm` and
`serializeConversation` from the upstream `@earendil-works/pi-coding-agent` package.

The file's own docblock declares the duplication explicit and intentional (avoiding a
heavy transitive runtime closure including AWS Bedrock SDK,
 chalk,
 marked,
cli-highlight).
 Listed for completeness so future audits do not re-flag it.

The maintenance cost is real:
 when upstream changes either function,
 the vendored copy
must be updated by hand.
 Worth tracking in
[`TROUBLESHOOTING.pi-safeguard.md`](../troubleshooting/pi-safeguard.md).

### Self-acknowledged inline copies

These findings are duplications where the file's own docblock cites the source.
 Each
one is ready to consolidate today;
 the inline comment is a marker the author left
saying "this should not be here long-term".

#### `webapp-forge/server/src/fragments/jsx-runtime.ts` is an inline copy of `ssg-test/src/lib/jsx-to-html.ts`

Stated explicitly at line 4 of `package/webapp-forge/server/src/fragments/jsx-runtime.ts`:

> Inline copy of `package/ssg/aquati.cat/src/lib/jsx-to-html.ts`.
> Kept inline (not factored to a workspace package) per the design plan
> so each surface can pin its own escape semantics.

182 lines (webapp-forge) vs 217 lines (ssg-test);
 101-line diff.
 Both define
`isSafeHtml`,
 `isDangerousHtml`,
 the `SafeHtml` type,
 and the JSX-to-HTML conversion
loop.

Rationale ("each surface can pin its own escape semantics") is recorded but no
divergence has occurred yet,
 so the design rationale is paying dividends in
"flexibility we have not used" while costing maintenance now.

Suggested resolution:
 consolidate into a `@monochromatic-dev/module-jsx-to-html`
package with optional escape-semantics overrides;
 if escape semantics genuinely diverge
later,
 swap the override.

#### Mulberry32 PRNG is implemented twice

Both files implement the identical 4-step Mulberry32 algorithm (verified by both
referencing the magic constant `0x6D_2B_79_F5`):

- `package/webapp-forge/seed/src/rng.ts:rng` (exported)
- `package/webapp-content/messages-demo/src/lib/seed.ts:rng` (file-local)

The first file's docblock states "Mirrors
`package/webapp-content/messages-demo/src/lib/seed.ts`".

`webapp-forge/seed/src/rng.ts` also exports `rngInt` and `rngPick` companion helpers,
which `messages-demo` does not have.
 Suggesting consolidation into a
`@monochromatic-dev/module-rng/` package with the three helpers as the public API.

#### `webapp-forge/server/src/lib/auth.ts` mirrors `data/db.ts`'s database resolution

`package/webapp-forge/server/src/lib/auth.ts` declares its own
`DEFAULT_DATABASE_PATH = './data/forge.db'` and recomputes the same
`--db argument -> DB_PATH env -> default` priority chain that already lives in
`package/webapp-forge/server/src/data/db.ts`.
 The `auth.ts` docblock acknowledges this:

> Mirrors `data/db.ts` so both libraries point at the same file.
> Mirrors `data/db.ts`'s priority chain so both libraries point at the same path.

Both files exist within `package/webapp-forge/server/`,
 so the natural fix is to lift
the resolution into a shared `lib/database-path.ts` module within the package.

#### Three other "Mirrors" docblocks (already covered elsewhere)

For completeness,
 the following self-acknowledged mirror comments were found and are
already covered by earlier findings:

- `webapp-forge/server/src/lib/http.ts` ("Mirrors the pattern from
  `package/webapp-content/messages-demo/src/lib/http.ts`"):
   covered by the
  `lib/http.ts` finding.
- `webapp-forge/server/src/lib/args.ts` ("Mirrors
  `package/webapp-content/messages-demo/src/lib/args.ts`"):
   covered by the
  `lib/args.ts` finding.
- `ssg/aquati.cat/src/client/tags.ts` ("Mirrors the mapping used by editord
  for visual consistency across surfaces"),
   covered by the `HIGHLIGHT_GROUPS`
  finding.

### Subprocess and runtime patterns

#### `nano-spawn` is the convention with four hold-outs using `promisify(execFile)`

**Resolved 2026-05-10**:
 all four hold-outs migrated to `nano-spawn`.
`git-dates.ts` also folds in the `CommandResult` finding (line 449) by
typing the wrapper return as `Pick<Result, 'stdout' | 'stderr'>` from
`nano-spawn`.
 The `update-ref --stdin` block in `git.cli.unit.test.ts`
collapses from 30 lines (manual `child.stdin.write` + `exit` listener) to
a single `spawn` call with `stdin: { string: stdinScript }`.
 Binary
stdout in `zip-writer/index.unit.test.ts` (formerly `encoding: 'buffer'`)
extracts via `unzip -o -d tempDir` then reads back,
 since `nano-spawn`
returns string-only stdout.
 Verified by `mise run //:buildAndTest`
against both test files;
 all three git CLI scenarios and all 17
zip-writer cases pass.

`nano-spawn` appears in 38 source files (verified by `rg "from 'nano-spawn'"`).
 Four
files still use the `node:child_process` + `node:util` `promisify(execFile)` pattern:

- `package/ssg/aquati.cat/src/lib/git-dates.ts:14`
- `package/figma/kiwi/src/index.ts`
- `package/webapp-forge/server/src/server/routes/git.cli.unit.test.ts`
- `package/module/zip-writer/src/index.unit.test.ts`

`execa` does not appear in source.

Suggested resolution:
 migrate the four hold-outs to `nano-spawn`.

#### `nano-spawn` import shape varies (4 forms)

- `import nanoSpawn from 'nano-spawn'`
- `import nanoSpawn, { SubprocessError, } from 'nano-spawn'`
- `import spawn from 'nano-spawn'`
- `import spawn, { type SubprocessError, } from 'nano-spawn'`

Listed as a minor inconsistency;
 not a DRY violation but worth normalizing during a
drive-by edit.

#### `process.exitCode = 1` idiom appears in 10+ CLI catch blocks

- `package/webapp-forge/stress/src/cli.ts` (twice)
- `package/dev-script/catalog-tighten/src/index.ts`
- `package/dev-script/task-util/src/{tsgo-filter, pnpm-filter, oxlint-wrapper}.ts`
- `package/claude-code-plugin/source/src/cli/spawn-claude.ts`
- plus more

This conforms to the project rule "never `process.exit()`:
 throw errors instead;
always `console.error()` in catch blocks",
 so it is not a DRY violation.
 Listed so the
next audit does not re-flag the pattern.

### Sibling-component duplication

#### `question-radio.ts` and `question-checkbox.ts` share 224 of 396/390 lines

`package/ssg/aquati.cat/src/components/question-{radio,checkbox}.ts` are
sibling React-style components (radio button group vs checkbox group).
 Combined size
786 lines;
 172-line diff.

Each defines:

- `function validate(props: QuestionProps,): void`
- `type QuestionProps`,
   `type QuestionOption`
- `const HEX_RADIX = 16` (already noted)
- The render skeleton

Suggested resolution:
 extract a `<QuestionGroup type="radio|checkbox">` component
that takes the input shape as a discriminator.
 The radio and checkbox specifics
become two short adapters.

#### `cli/vmsync/src/config.ts` and `cli/mvm/src/config.ts` share `validateName` and config shape

Both files export `validateName(name: string): void`.
 The two CLIs (vmsync = VM file
synchronizer;
 mvm = VM manager) are siblings under `cli/` and have similar config
needs (a name,
 a directory,
 validation rules).

Sizes:
 286 (vmsync) vs 84 (mvm);
 315-line diff.

Suggested resolution:
 lift the shared `validateName` into a `cli/vm-shared/` package
or into `module/es`.
 Both packages already depend on workspace utilities.

#### `summarize` declared in two places

- `package/dev-script/page-weight/src/stats.ts:summarize` (statistics summary)
- `package/webapp-forge/server/src/fragments/merge-status.ts:summarize` (merge status)

Different domains,
 different signatures.
 Listed for completeness;
 this is name
collision rather than DRY duplication.

#### `validate` declared in two places

- `package/ssg/aquati.cat/src/components/question-radio.ts:validate`
- `package/ssg/aquati.cat/src/components/question-checkbox.ts:validate`

Folds into the question-radio/checkbox sibling finding above.

#### `done` and `done-h-css-test` migrations share 88 of 103/106 lines

`package/webapp-productivity/done/src/lib/db/migrations.ts` vs
`package/webapp-productivity/done-h-css-test/src/lib/db-migrations.ts`:
 15-line diff
out of ~104 lines.
 Same `tasks` / `attachments` / `settings` tables with the same
schema,
 same migration order.
 Folds into the `done` fork finding.

### Web API endpoint duplication

#### `messages-demo/src/server/api/messages.ts` and `api/drafts.ts` share substantial structure

`package/webapp-content/messages-demo/src/server/api/messages.ts` (257 lines) and
`api/drafts.ts` (379 lines) both:

- Declare local `isRecord` and `stringField` helpers
- Declare local `BYTES_PER_KIB`,
   `DECIMAL_RADIX`,
   `PREVIEW_MAX_LENGTH`
- Use the same `URLPattern.exec` + named-group extraction shape
- Run the same `validateJsonShape -> dispatch -> return Response.json` pattern

The two endpoints serve different resources but the validation and serialization
shells are duplicated within a single package.
 Folds into the `isRecord`,
`stringField`,
 and `DECIMAL_RADIX` findings;
 listed here so reviewers can group the
work.

#### `done` timer endpoint and `done-h-css-test` timer endpoint diverge by 55 lines

`package/webapp-productivity/done/src/server/api/timer.ts` (96 lines) vs
`package/webapp-productivity/done-h-css-test/src/server/api/timer.ts` (123 lines).
Same start-stop-reset timer endpoints.
 Folds into the fork finding.

#### `done` task-validation modules duplicated across the fork

- `package/webapp-productivity/done/src/server/api/task-validation.ts`
- `package/webapp-productivity/done/src/server/api/task-validation-update.ts`
- `package/webapp-productivity/done-h-css-test/src/server/api/tasks-parse.ts`
- `package/webapp-productivity/done-h-css-test/src/server/api/tasks-parse-update.ts`

Four files;
 two pairs (create + update) per fork side.
 Folds into the fork finding.

### Test patterns

#### `describe`/`it` test framework imports from a single canonical source

All 144 test files import from `@monochromatic-dev/module-test`.
 No `bun:test` or
external `vitest`/`jest` import appears.
 Listed as a positive finding and already-shared
category.

#### `import.meta.dirname` used in 12 test files for fixture path resolution

- `package/webapp-edu/paper2vn/src/paper2vn.e2e.test.ts`
- `package/rolldown-plugin/import-attributes/src/import-attributes.unit.test.ts`
- `package/dev-script/task-util/src/{tsgo-filter, depends, command, append}.unit.test.ts`
- `package/dev-script/file-enforcer/src/integration.unit.test.ts`
- `package/dev-script/inference-canary-viewer/src/dashboard.e2e.test.ts`
- `package/build-tool/css/src/build.unit.test.ts`
- `package/oxlint-plugin/stylistic/src/oxlint-stylistic.unit.test.ts`
- plus a couple more

The pattern (`import.meta.dirname` used to resolve fixture paths relative to the test
file) is consistent across all 12 callers.
 No `fileURLToPath(import.meta.url)` variant
exists in test files.
 Listed as a positive finding.

### Reusable utilities awaiting extraction

These helpers live in a single non-shared package today,
 so they are not DRY violations
yet.
 Each is shaped like a library function (primitive in,
 primitive out,
 no domain
types,
 no hidden dependency on package-specific state),
 and the next module that needs
the same operation will write its own copy unless the helper is moved into a shared
module first.
 Listed here so they can be folded out before the second caller appears.

A few entries are duplicated across two callsites;
 those count as DRY violations under
the original definition,
 but the resolution is the same as the single-occurrence ones
(extract once,
 import everywhere),
 so they are grouped here rather than split across
two sections.

#### `formatBytes` in one place, but generally needed everywhere

`package/cli/mvm/src/download-progress.ts:45` defines
`formatBytes(bytes: number): string` returning a human-readable size string
("1.0 MiB",
 "123 KiB").
 Uses inline `KIB`/`MIB`/`GIB` constants,
 the same constants
that the audit's "Numeric and string constant duplication" section flags 13 times under
five different names (`BYTES_PER_KIB`,
 `KIB`,
 `BYTES_PER_KB`,
 `BYTES_PER_UNIT`,
`BYTES_PER_KILOBYTE`).
 Any package that prints sizes to a user (download progress,
storage quota,
 log files) will want this function.

Suggested resolution:
 extract to `module/es` next to the byte-size constants the audit
already prescribes a home for.
 Caller in `cli/mvm/src/download-progress.ts` becomes
a one-line import.

#### `formatMs` and `formatNumber` in one place each

`package/dev-script/inference-canary-viewer/src/html/overlay-meta.ts:31` defines
`formatMs(ms: number): string` returning "1.2s" for >= 1000ms and "123ms" otherwise.
The same file also defines `formatNumber(num: number): string` (locale-formatted
thousands separators).
 Both are shape-identical to general utilities;
 both will be
needed by any future package that renders timing metrics or counts.

Suggested resolution:
 move to `module/es` alongside `formatBytes`.
 The viewer keeps
the call site,
 drops the local definition.

#### `formatRelative` (relative-time formatting) in two places, near-identical concept

Two callsites with the same conceptual function but different output strings:

- `package/webapp-content/messages-demo/src/server/pages/feed.ts`:
   returns
  "just now",
   "5m ago",
   "2h ago",
   "3d ago",
   or ISO-date prefix
- `package/claude-code-plugin/statusline/statusline.ts`:
   returns
  "now",
   "30s",
   "5m",
   "2h30m",
   "3d4h" (no "ago" suffix)

Both walk the same threshold ladder using `SECONDS_PER_MINUTE` / `SECONDS_PER_HOUR` /
`SECONDS_PER_DAY` constants.
 Same algorithm,
 different format string set.
 Could be
unified as `formatRelative({ deltaSeconds, format: 'ago' | 'compact' })` or as two
separate functions sharing the threshold helper.

Suggested resolution:
 extract the threshold-ladder logic to `module/es`;
 let each
caller pick its own format.
 Companion fix to the duration-constant duplication
already in the audit.

#### `nowIso` defined twice (DRY violation; weak fold-out case)

- `package/webapp-productivity/done-h-css-test/src/lib/db/tasks-helpers.ts`
- `package/webapp-productivity/done/src/lib/db/task-mapping.ts`

Both `export function nowIso(): string { return new Date().toISOString(); }`.
 This is
within the done/done-h-css-test fork already documented and resolves when the fork
consolidates.
 The fold-out-to-`module/es` case is weak:
 the body is a single stdlib
call;
 a single named entry point adds little over inlining `new Date().toISOString()`
at each callsite.
 Listed here only so a future audit does not re-flag the
duplication after the fork merges;
 the DRY violation itself is captured under the
function-level section.

#### `parseMemoryToBytes` (inverse of `formatBytes`) in one place

`package/cli/vmsync/src/boot.ts` exports `parseMemoryToBytes(memory: string): number`
which accepts strings like `"4G"` or `"2048M"` and returns bytes.
 Includes its own
inline `GIB` and `MIB` constants.
 Pairs naturally with `formatBytes`:
 any package
that emits the human-readable form will eventually want to read it back.

Suggested resolution:
 extract together with `formatBytes` into the same `module/es`
sub-namespace.
 Two-way conversion lives next to its inverse,
 the constants are shared,
and the unit table (G,
 M,
 K) is parsed in one place.

#### `concatBytes(chunks: readonly Uint8Array[]): Uint8Array` written six times

The same loop (precompute total length,
 allocate once,
 copy each chunk in) appears
in six separate files (counts the implementation only,
 not callers):

- `package/webapp-forge/server/src/git/iso-server.ts:concatChunks`
- `package/webapp-forge/server/src/git/iso-server-advertisement.ts:concatChunks`
- `package/webapp-forge/server/src/git/iso-server.unit.test.ts:concat`
- `package/webapp-forge/server/src/git/pack-protocol.unit.test.ts:concat`
- `package/webapp-forge/server/src/git/pkt-line.unit.test.ts:concat`
- `package/webapp-forge/stress/src/scenarios/force-push.ts:concat`

Bodies are identical except for the variable name `concat` vs `concatChunks`;
 the
inputs and outputs are byte-for-byte the same.
 This is also a DRY violation that the
function-level audit missed (the function-level pass surfaced the count but not the
unification).

Suggested resolution:
 a single export from `module/es`,
 named `concatBytes` to
distinguish from `Array.prototype.concat`.
 All six call sites become single-line
imports.

#### `randomId` (UUID with non-crypto fallback) in one place

`package/webapp-content/messages-demo/src/client/composer/helpers.ts:randomId` returns
`crypto.randomUUID()` when available,
 otherwise a `${Date.now()}-${random}` synthetic
ID.
 The same fallback shape will be wanted by any browser-side package that needs an
ID generator and cares about older runtimes (the codebase's `firefox140` baseline does
support `crypto.randomUUID`,
 but the fallback is still useful for SSR or non-browser
contexts).

Suggested resolution:
 extract to `module/es`.
 `messages-demo` drops the local copy.

#### Test-only `uniqueId(tag: string): string` repeated in six test files

The same closure-counter pattern,
 identical body,
 in:

- `package/webapp-forge/server/src/worker/dispatcher.unit.test.ts`
- `package/webapp-forge/server/src/worker/dispatcher-phase2.unit.test.ts`
- `package/webapp-forge/server/src/data/queries-phase2-events.unit.test.ts`
- `package/webapp-forge/server/src/data/queries-phase2-resources.unit.test.ts`
- `package/webapp-content/messages-demo/src/lib/db/sweep.unit.test.ts`
- `package/webapp-content/messages-demo/src/lib/db/messages.unit.test.ts`

Each file has a top-level `let counter = 0;` and a
``function uniqueId(tag) { counter += 1; return `prefix-${tag}-${counter}`; }``.
The prefix differs per file (`t-`,`q2e-`,`q2r-`,`dpx-`,`t-`,`t-`);
 everything else is byte-identical.
 A genuine DRY
violation in test code that the function-level scan caught the count of (above) but
that did not appear in the audit's "Test patterns" section.

Suggested resolution:
 ship `uniqueId({ prefix: string }): string` from
`@monochromatic-dev/module-test`.
 All six test files become one-line imports plus the
prefix string literal.

#### Inline `function isDefined(x): x is T { return x !== undefined; }` filter callbacks

Anonymous-named filter-callback typeguards repeating the same body,
 in seven files:

- `package/webapp-productivity/done/src/server/pages/task-details.ts`
- `package/webapp-productivity/done-h-css-test/src/server/pages/task-details.ts`
- `package/cli/vmsync/src/list.ts`
- `package/cli/vmsync/src/nbd.ts`
- `package/dev-script/inference-canary/src/codegen/fix-prompt.ts`
- `package/dev-script/inference-canary/src/codegen/probe-factory-additional-diagnostics.ts`
- `package/pi-plugin/morph-compact/src/pi-utils.ts`

Each occurrence is `.filter(function isDefined(x): x is T { return x !== undefined; })`.
The standard library has no built-in narrowing typeguard for this;
 everyone writes
their own.
 The function is the canonical "I am writing this for the third time,
 why
isn't it shared" candidate.

Suggested resolution:
 export `isDefined<T>(x: T | undefined): x is T` from
`module/es`.
 All seven sites become `.filter(isDefined)`.
 The narrowing returns
through the imported signature,
 so callers get the type refinement without writing
the inline typeguard.

#### `concat`-style helpers also exist for strings/lines (one place but shape is generic)

`package/webapp-edu/paper2vn/src/client/state.ts:readJson` defines
`function readJson<T,>(key: string, fallback: T): T` reading and parsing JSON from
`localStorage` with a fallback on any failure.
 A second `readJson` exists in
`package/webapp-content/messages-demo/src/client/json-fetch.ts` but operates on
`Response`,
 not `localStorage`:
 different signature,
 same name.
 This is the
"name collision" case the constants section already documents.

The `localStorage` variant is generic-shaped (no app-specific types) and will be
wanted by any future browser-side package that needs settings persistence.

Suggested resolution:
 extract `readJson` (localStorage variant) to a shared module
under a distinct name like `readLocalStorageJson` or `readPersistedJson` to avoid the
collision with the fetch variant.
 The fetch variant (one-line wrapper) is too thin
to extract.

#### `readPageData<TData>(): TData` reading a `<script id="page-data">` element

Two callsites:

- `package/webapp-productivity/done/src/client/lib/page-data.ts`
- `package/webapp-productivity/done-h-css-test/src/client/lib/page-data.ts`

Both byte-identical:
 read the `<script id="page-data">` element,
 throw if missing,
JSON-parse the textContent.
 This pattern (server emits page-scoped data as an inline
script tag,
 client reads it back) is a generic SSR-to-client handoff convention,
not specific to the productivity app.

Suggested resolution:
 lift to a shared client-side utilities module (one does not
exist yet:
 create it as `package/module/dom-utils` or similar,
 or add it to
`module/dom` which already has DOM helpers).
 Both done variants import.

#### `requireElement<T>(selector): T` in one place; codified in TROUBLESHOOTING.typescript.md

`package/webapp-productivity/doodle-widget/src/client/dom-refs.ts:requireElement`
exports a typed `querySelector` that throws on null.
 The pattern is the workaround
for the TypeScript narrowing-into-function-declarations limitation,
 documented
verbatim in `TROUBLESHOOTING.typescript.md` as the canonical fix.
 Once a workaround
is documented in the troubleshooting file,
 the next session that hits the bug will
write a fresh local copy unless a shared helper exists to import.

Suggested resolution:
 add `requireElement<T extends Element>(selector: string): T`
to `module/dom`.
 Update `TROUBLESHOOTING.typescript.md` to point at the import path
instead of showing the inline definition.

#### `ensureDir` (mkdir-recursive shorthand) in three places

Three near-identical wrappers around `mkdir(..., { recursive: true })`:

- `package/dev-script/file-enforcer/src/io/write.ts:ensureDir(filePath: string)`:
  takes a file path,
   calls `mkdir(dirname(filePath), { recursive: true })`
- `package/claude-code-plugin/source/src/handler/session-start-housekeeping.ts:ensureDir(dirPath)`:
  takes a directory path directly
- `package/ssg/aquati.cat/src/build/assets.ts:ensureDir(dir)`:
   inline
  callback inside a `Promise.all(map)` chain

Three call shapes,
 three definitions,
 one underlying operation.
 The first one
(`dirname(filePath)`) is the most useful variant for write-then-mkdir flows.

Suggested resolution:
 a single `ensureDirForFile(filePath)` plus an `ensureDir(dirPath)`
in `module/es` (the path/ subdirectory already exists).
 Three call sites collapse to
imports.

#### `intFlag` / `getFlag` argument-parsing helpers in two places

`package/webapp-forge/stress/src/scenarios/shared.ts:intFlag` and
`package/webapp-forge/seed/src/cli.ts:intFlag` are byte-identical implementations of
"parse `--name=value` from `process.argv`,
 fall back to a default on missing or
non-finite".
 The same files also each define `getFlag(name: string): string | undefined`
to back the parsing;
 the audit's existing function-level section already lists
`getArgumentValue` / `getFlag` as a 5-implementation cluster.

`intFlag` is a typed wrapper one layer above `getFlag` that the existing audit
captures only as a body-level duplicate,
 not as a fold-out target.
 The shape is
generic:
 name,
 fallback,
 decimal radix.

Suggested resolution:
 bundle `getFlag`,
 `intFlag`,
 `boolFlag`,
 `stringFlag` into a
single `module/es` arg-parser submodule.
 The five `getFlag`-or-equivalent
implementations and the two `intFlag` copies all collapse to imports.

#### `truncate(value, maxLength)` with ellipsis; one fork has two copies

`package/pi-plugin/terminal-title/src/formatter-utils.ts:truncate` and the corresponding
copy in `package/claude-code-plugin/source/src/handler/terminal-title/formatter-utils.ts`
are the two truncate implementations the audit's terminal-title fork section already
covers.
 The body is generic ("if longer than max,
 slice and append `…`") and would
be useful in any package that emits text into a fixed-width display surface (CLI
tables,
 terminal titles,
 status lines).

Suggested resolution:
 extract from the terminal-title fork into `module/es` rather
than just consolidating the fork.
 The downstream value is bigger.

#### `padLeft` / `padRight` thin wrappers around `String.prototype.padStart` / `padEnd` (delete, not extract)

**Resolved 2026-05-09**:
 both wrappers were deleted from
`package/dev-script/page-weight/src/index.ts`;
 the eight call sites now use
`text.padEnd(width)` and `text.padStart(width)` directly.

`package/dev-script/page-weight/src/index.ts` defines both `padLeft({text, width})`
and `padRight({text, width})` as object-parameter wrappers around `padStart` and
`padEnd`.
 The wrappers do not add behaviour;
 they only re-shape the call site to
use the codebase's "named params for >= 2 args" convention.

Suggested resolution:
 drop the wrappers;
 call `text.padStart(width)` and
`text.padEnd(width)` directly.
 The named-params convention does not apply to method
calls on built-ins;
 the rule's purpose is to make function arguments self-documenting,
which the method form already achieves through the receiver.
 This is a delete-not-
extract finding,
 kept in this section because it surfaced through the same
utility-shape sweep;
 the resolution is opposite (collapse into the stdlib instead
of moving to a shared module) but the source signal was the same.

#### `splitGlob(pattern): [cwd, relativeGlob, ...]` written twice

Two near-identical implementations splitting a glob pattern into its non-meta prefix
and the relative-glob suffix:

- `package/dev-script/file-enforcer/src/io/glob.ts:splitGlob`
- `package/dev-script/task-util/src/depends-resolve-glob.ts:splitGlob`

Both use a `GLOB_META` regex to find the first metacharacter,
 then partition.
 Tuple
shapes differ slightly (file-enforcer also returns the `originalPrefix`),
 but the
core logic is the same.
 Any package that needs to plumb glob patterns through `cwd`
boundaries will want this;
 the next caller will write a third copy.

Suggested resolution:
 extract to `module/es/path/glob.ts` (sibling to the existing
`path/` helpers).
 Both call sites import;
 the file-enforcer site adds the
`originalPrefix` post-processing locally.

#### `round1` (round to one decimal place) in two places

`package/test-fixture/file-enforcer-perf/src/{validate-benchmarks,bench-in-container}.ts`
both define `round1(value): number => Math.round(value * 10) / 10`.
 Same body,
 no
divergence.
 While `test-fixture/*` is excluded from the broader DRY scope,
 this
function is shape-generic enough that it should leave the fixture and live in
`module/es`:
 any package that prints fractional metrics will write its own copy
otherwise.

Suggested resolution:
 move to `module/es/types/t number/...` (the taxonomic location
for number utilities).
 Generalize to `roundTo(value, decimals)`;
 provide
`round1`/`round2` as named conveniences if the call site readability matters.

#### `encodeS3Key(key)` (per-segment URI encoding) in one place

`package/webapp-forge/server/src/storage/adapter-s3.ts:encodeS3Key` URI-encodes each
`/`-separated segment of an S3 key while preserving the separator.
 Generic enough
to belong outside the S3 adapter;
 the same operation works for any path-shaped
identifier that needs URL-safe encoding (file paths,
 CDN keys,
 route segments).

Suggested resolution:
 extract to `module/es/path` (sibling to `trim`,
 `ensure`,
`empty`).
 The S3 adapter imports.

#### `escapeString` in one place

`package/dev-script/file-enforcer/src/package/mise.generate-index.ts:escapeString`
escapes `\` and `'` for embedding into single-quoted TypeScript string literals,
the canonical operation for codegen tools that emit TS source.
 Single caller today,
but file-enforcer is one of several codegen tools (forbidden-strings has
`escapeResharpOnlyMeta`;
 the messages-demo composer emits HTML);
 the next codegen
package that emits TS will reinvent this.

Suggested resolution:
 extract to `module/es/types/t string/...` next to other string
helpers.
 Provide `escapeForSingleQuoted` and `escapeForDoubleQuoted` variants since
the escape sets differ.

#### `withTimeout(promise)` browser variant alongside `module/es` async variant

**Resolved 2026-05-09**:
 `storage-probe.ts` now composes with the shared
`withTimeout` from `@monochromatic-dev/module-es/with-timeout`.
 A small
`capProbe({probe, label})` helper wraps the shared utility in a try/catch so
timeout or backend rejection still maps to `false`.
 The local timer wiring is
deleted.
 Verified by `mise run //package/webapp-content/messages-demo:build`
producing the firefox140 client bundle without warnings.

`package/webapp-content/messages-demo/src/client/storage-probe.ts:withTimeout`
implements timeout-via-`Promise.race`-with-`setTimeout`.
 The codebase already exports
`withTimeout` from `@monochromatic-dev/module-es/with-timeout` (verified at
`package/module/es/src/types/t object/t promise/f/t object/withTimeout/r a/p p/index.ts`).
The local copy hardcodes a `PROBE_TIMEOUT_MS` and resolves with `false` instead of
rejecting;
 it is a domain-specific specialization,
 but it does so by reimplementing
the timer wiring rather than by composing with the shared utility.

Suggested resolution:
 rewrite the local site to compose with the shared
`withTimeout`:
 `await withTimeout({ promise, ms: PROBE_TIMEOUT_MS, label: 'probe' })
.catch(() => false)`.
 The local function declaration goes away;
 the timer logic
stays in one place.

#### `cli/mvm/src/spawn.ts` and `cli/vmsync/src/spawn.ts` are near-byte-identical

`spawn({command, args}): Promise<string>`,
 11-line diff between two 50-line files
(46 vs 52).
 The diff is one example string in a docblock and one log-tag rename.
Both wrap `nano-spawn` with the codebase's tagged-logger pattern.

The audit's existing function-level section captures `mcp/{nvim, mvm}` already
factor through `@monochromatic-dev/mcp-stdio`;
 the cli/{mvm,
 vmsync} pair has no
analogous shared base.
 This is both a reusable-utility candidate and a DRY violation
the function-level scan missed because the function is named `spawn` (high
collision rate,
 scan dropped to noise).

Suggested resolution:
 ship a `nanoSpawnTagged({command, args, logger}): Promise<string>`
helper:
 either as a new tiny package (e.g. `module/spawn-tagged`) or as an export
from `module/logger` since the value-add over raw `nano-spawn` is purely the tagged
log-line.
 Both CLI packages import.

#### `groupBy*` family in five places, all reducing to `(items, keyFn) => Record<key, items[]>`

Five separate `groupBy`-shaped functions across two packages,
 each implementing the
same one-pass partition-by-key with different key types and result shapes:

- `package/ssg/aquati.cat/src/lib/content-group.ts:groupByLang(posts)`:
  delegates to `Map.groupBy`,
   returns `Partial<Record<Locales, Post[]>>`
- `package/ssg/aquati.cat/src/lib/content-group.ts:groupByName(posts)`:
  delegates to `Map.groupBy`,
   returns `Record<string, Post[]>`
- `package/ssg/aquati.cat/src/lib/content-group.ts:groupByTag(posts)`:
  walks `allTags(posts)`,
   returns `Record<string, Post[]>`
- `package/ssg/aquati.cat/src/lib/content-group.ts:groupByLangThenTag(posts)`:
  composition of the previous two,
   returns `Partial<Record<Locales, Record<string, Post[]>>>`
- `package/desktop-daemon/editord/src/client/inlay/group-by-line.ts:groupByLine<T>({items, keyFn})`:
  generic,
   returns `Map<number, T[]>`,
   hand-rolled (does not use `Map.groupBy`)

The `ssg-test` quartet uses the stdlib `Map.groupBy` and just wraps it in a
`Object.fromEntries` shim to match the per-call shape.
 The `editord` site predates
or ignores `Map.groupBy` and writes its own loop.
 Different result containers
(`Record` vs `Map`),
 but the same operation underneath.

Suggested resolution:
 ship a single `groupBy<T, K>({items, keyFn}): Map<K, T[]>` from
`module/es` (returning `Map` rather than `Record` so non-string keys work).
 Provide
a thin `groupByToRecord` companion for the `Record<string, T[]>` callers.
 The five
sites collapse to four `groupBy(...)` calls plus one `groupByToRecord` call;
 each
keeps its own keyFn but drops the shared loop body.

#### Cross-cutting note: where these utilities should land

`module/es` is the natural home for type-level and primitive-level helpers
(formatBytes,
 parseMemoryToBytes,
 isDefined,
 randomId,
 formatMs,
 formatNumber,
concatBytes,
 round1,
 encodeS3Key,
 escapeString,
 splitGlob,
 ensureDir,
 groupBy).
`module/test` is the home for the test-only `uniqueId({ prefix })`.
 `module/dom`
gets `requireElement` and `readPageData`.
 The spawn-tagged helper plausibly lives in
`module/logger` (since the value-add is the tagged log line) or as its own tiny
package alongside other I/O-shaped helpers.

A useful test for whether each fold-out is right-sized:
 does the helper contain any
package-specific assumption (a hardcoded constant tied to the source package,
 an
import from the source package,
 a call to a logger tagged with the package name)?
If yes,
 the extraction needs a config parameter;
 if no,
 the extraction is a direct
move.
 Most candidates above are direct moves.

### Already-shared categories

These show heavy structural duplication,
 but the duplication is the consequence of an
existing shared base.
 Listed here so they are not flagged in future audits.

#### `tsdown.*.config.ts` per-package files (43 total)

32 are byte-identical one-line re-exports of one of three bases in
`@monochromatic-dev/config-tsdown`:

- 20 re-export `.node.ts`
- 10 re-export `.ts` (neutral)
- 2 re-export `.client.ts`
- 11 override the base with a per-package entry list (each importing the relevant base
  via `import base from '@monochromatic-dev/config-tsdown/.node.ts'` or similar)

The remaining structural duplication is forced by tsdown's CWD-relative config lookup.

#### `tsconfig.json` per-package files (85 total)

68 are byte-identical extensions of `@monochromatic-dev/config-typescript/dom`:

- 57 with `{ "extends": "@monochromatic-dev/config-typescript/dom" }`
- 6 with `{ "extends": ..., "compilerOptions": {} }` (empty block)
- 5 with `{ "extends": ..., "include": [] }`

The 6-file empty-block cluster is a stylistic minor inconsistency worth normalizing
during a future drive-by.

#### `module/es` barrel re-export files

`package/module/es/src/types/...` is organized as a deeply nested directory tree
following the package's own taxonomy.
 Each level has a one-line `index.ts` that
re-exports the next level.
 Across the tree,
 several one-line barrel patterns recur:

- `export * as sync from './r s/index.ts';`:
   52 files
- `export * as positional from './p p/index.ts';`:
   51 files
- `export * as type from './t/index.ts';`:
   27 files
- `export * as from from './f/index.ts';`:
   24 files
- `export * as named from './p n/index.ts';
  export * as positional from './p p/index.ts';` (16 files)

These are byte-identical because they encode a structural relationship in the
directory tree (a level always has the same set of subfolders).
 The duplication is
forced by TypeScript's directory-as-namespace convention combined with module-es's
naming taxonomy (`t` = type,
 `f` = function,
 `r s` = return sync,
 `p p` = positional
parameters,
 etc.,
 as documented in module-es's README).

Listed as a positive finding (already-shared category) so the next audit does not
re-flag them.
 A code generator could produce them,
 but the gain is small.

#### Per-package `mise.toml` files (81 total)

The largest cluster of 7 byte-identical files contains only `extends = "..."` re-exports
of root-level task templates.
 17 packages share one of two stub shapes (7 + 7 + 3).
 The
duplication is structural and unavoidable:
 each package needs to opt into the task
templates it wants from the root.

## Cross-reference

For non-DRY structural inconsistencies (package naming drift,
 README heading drift,
`version` and `private` field presence drift,
 missing required files),
 see
[the consistency audit](consistency.md).
For dependency-security findings (Socket CLI scan,
 dependency source-code audits),
 see
[the audit index](README.md).
