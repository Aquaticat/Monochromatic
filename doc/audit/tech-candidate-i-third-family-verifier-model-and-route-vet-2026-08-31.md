# Candidate I third-family verifier model and route vet

- Status:
  validation pending;
  no recommendation.
- Lifecycle phase:
  one serious alternative identified.
- Subject:
  Candidate I third-family verifier model and route.
- Decision scope:
  choose one Hyper-hosted vision-capable verifier outside Qwen and MiniMax families over Anthropic forced-tool route for
  Candidate I.
- Started:
  2026-08-31.
- Last updated:
  2026-08-31.
- Governing skill commit:
  `a05818ad70a40e5769a36de669697ba109891b31`.
- Governing skill SHA-256:
  `393eb68c5b2b2f7b16c8f7f90c100fb8be43eefa4501511360cd0572e4ae8087`.
- Compatibility fingerprint:
  `0264f6da867ae1d282bc8876858899435a483896044d22ebb4a8d77bb7b60904`.
- Active audit owner:
  Pi session `01a041b2-cb17-798b-a0ba-f71c9e0ffd51`.
- Prior compatible report:
  none.
  `doc/audit/tech-candidate-i-kimi-verifier-model-and-route-vet-2026-08-31.md` is prior incumbent-route rejection;
  scope here widens model family while fixing Hyper Anthropic route.
- Duplicate compatible reports:
  none.

## Context and hard constraints

Candidate I's Qwen and MiniMax authors each need clean nonself ballots from two distinct model families.
Qwen and MiniMax verifiers provide one such family each for opposite author.
A third family must provide remaining independent ballot for both candidates.
Focused Kimi vet rejected every live Kimi route under repository constraints.

Hard constraints:

- Hyper-only normal operation;
- family distinct from Qwen and MiniMax;
- vision capable;
- Hyper Anthropic forced-tool structured output;
- complete source,
  archive,
  immutable shell,
  obligation manifest,
  and every page-referenced image in each request;
- exact compact status coverage with caller validation;
- measured complete candidate-scoped response within provider output cap;
- no retry or route fallback;
- default sampling and reasoning parameters;
- explicit output ceiling permitted.

Base category is managed service or SaaS.
Overlays are incumbent model-route replacement and sensitive data.
Deployment is Node.js on Linux x64 against Charm Hyper Anthropic Messages.
Restricted corpus and images cross existing provider boundary.
Raw provider output remains quarantined and cannot enter publication without duplicate-member,
shape,
binding,
and semantic validation.

No unresolved preference controls discovery or hard gates.
Quality is standing constraint;
cost is not criterion.

### Fingerprint input

```json
{"schemaVersion":1,"subject":"Candidate I third-family verifier model and route","decisionScope":"Choose one Hyper-hosted vision-capable verifier outside Qwen and MiniMax families over Anthropic forced-tool route for Candidate I.","hardConstraints":["Anthropic forced-tool structured output","Hyper-only normal operation","complete compact status coverage","default sampling and reasoning parameters; explicit output ceiling permitted","measured complete candidate-scoped response within provider output cap","model family distinct from Qwen and MiniMax","no retries","source archive and every page-referenced image in each request","vision-capable"],"deployment":{"provider":"Charm Hyper","protocols":["Anthropic Messages"],"runtime":"Node.js on Linux x64"},"trustBoundary":"Restricted translation corpus and page-referenced images cross managed-service boundary; no raw provider output enters publication without caller validation.","incumbentName":"Charm Hyper Anthropic Messages with Kimi K3","incumbentVersion":null,"baseCategories":["managed service or SaaS"],"overlays":["incumbent model-route replacement","sensitive data"]}
```

## Discovery schedule and result

Frozen initial schedule:

- complete live Hyper `GET /v1/models` enumeration;
- checked-in owner allowlist and family mappings;
- focused Kimi rejection report;
- Synthetic GLM 5.3 Flash image and schema-conformance evidence;
- `site:docs.z.ai GLM-5.3-Flash vision tool calling structured output default reasoning`;
- `site:docs.z.ai GLM 5.3 Flash multimodal tool use Anthropic Messages`;
- `GLM-5.3-Flash Hyper vision forced tool schema`;
- `GLM-5.3-Flash alternatives vision verifier structured output model family`.

Official Z.ai searches returned ten results each.
Hyper route query returned ten results.
Broader alternative query returned ten results.
No provider or model id was included or excluded from search.

Live Hyper catalog is exhaustive and returned vision-capable rows only from Qwen,
MiniMax,
Kimi,
and GLM families.
Qwen and MiniMax are author families and cannot supply independent nonself family for both authors.
Focused Kimi report exits every Kimi row.
GLM 5.3 Flash is only remaining third-family vision row.
No expansion query discovered another live candidate;
discovery is saturated.

## Candidate ledger

### Qwen 3.6 through 3.8 vision rows

- Discovery:
  exhaustive live Hyper catalog.
- Hard-gate exit:
  Qwen family is author family and cannot provide distinct nonself evidence for Qwen candidate.

### MiniMax M3

- Discovery:
  exhaustive live Hyper catalog and incumbent roster.
- Hard-gate exit:
  MiniMax family is author family and cannot provide distinct nonself evidence for MiniMax candidate.

### Kimi K2.5,
K2.6,
K2.7 Code,
and K3

- Discovery:
  exhaustive live Hyper catalog.
- Hard-gate exit:
  `doc/audit/tech-candidate-i-kimi-verifier-model-and-route-vet-2026-08-31.md`.
  K2.5 lacks vision;
  K2.6 and K2.7 Code returned non-JSON content on strict route;
  K3 failed exact guard;
  low-effort tool route violates owner policy.

### GLM 5.3 Flash

- Discovery:
  exhaustive live Hyper catalog and official Z.ai docs.
- Exact live id:
  `glm-5.3-flash`.
- Live row:
  vision true,
  1,048,576 context,
  131,072 maximum output,
  max default reasoning.
- Project request ceiling:
  32,000 output tokens.
- Official model behavior:
  native image,
  video,
  text,
  and file input;
  1M context;
  128K model output;
  forced thinking;
  function calling and structured output support.
- Official source:
  `https://docs.z.ai/guides/vlm/glm-5.3-flash`,
  accessed 2026-08-31.
- Prior Synthetic-route evidence from different provider and protocol:
  Synthetic id `hf:zai-org/GLM-5.3-Flash` passed twenty of twenty schema-conformance attempts and one real-image read on
  2026-08-29.
- Route caveat:
  Z.ai OpenAI documentation says `tool_choice` supports only `auto`;
  Candidate I requires Hyper Anthropic forced tool,
  whose exact GLM mapping is unverified.
- Screening:
  serious alternative pending one-attempt exact route probe and pinned-Carena consumer validation.

## Managed-service gates

Charm Hyper remains incumbent provider.
Related vet reports and provider decisions already inspect:

- layoffs and headcount;
- customer reviews and outages;
- funding,
  ownership,
  and business model;
- signup friction;
- security and abuse history;
- terms,
  pricing,
  suspension,
  appeal,
  and termination;
- privacy,
  data use,
  retention,
  deletion,
  and geography;
- availability,
  support,
  export,
  portability,
  lock-in,
  deprecation,
  and business continuity.

No candidate changes provider,
account,
credential,
data category,
retention,
or geography.
Those findings are inherited common mode.
Fresh catalog and exact route validation remain required because `glm-5.3-flash` appeared after checked-in Hyper mapping.
Probe may address exact live id directly for screening,
but Candidate I prototype cannot use it until owner-controlled Hyper allowlist and shared-identity mapping receive explicit
reviewed edit.

Open-source,
local executable,
native,
Wasm,
prebuilt,
human-auditability,
and multi-platform browser gates are not applicable to this managed model-route selection.
No new local dependency or artifact is proposed.

## Frozen scoring criteria

Hard gates remain outside arithmetic.
Weights were frozen before candidate-specific Hyper route validation:

- exact forced-tool and caller-guard reliability:
  weight 5;
- verifier fidelity and English-quality fit:
  weight 5;
- complete-response headroom:
  weight 4;
- operational simplicity within incumbent provider:
  weight 3;
- end-to-end latency:
  weight 1.

Ratings use zero through four:
zero serious concern,
one weak,
two acceptable,
three good,
and four strong.
Maximum is
`(5 + 5 + 4 + 3 + 1) * 4 = 72`.
No rating is assigned before route and consumer validation.
With one surviving candidate,
sensitivity still varies defaultable weights from one through five and every uncertain rating by one;
any low-signal endpoint prevents recommendation.

## Planned execution manifest

Exact model is live `glm-5.3-flash`.
Fresh catalog must match vision,
maximum output,
and default reasoning immediately before spend.

One inspected Node scratch runner will send exactly one Hyper Anthropic Messages payload with:

- one synthetic 1 by 1 image;
- Candidate I-shaped compact verifier forced tool;
- one non-corpus substantive prompt;
- default reasoning and sampling;
- 32,000 `max_tokens` output ceiling;
- streaming;
- no retry.

Network endpoints are public `GET /v1/models` and authenticated `POST /v1/messages` only.
Expected writes are private metadata-only files under `~/temp/agent/`.
No raw response,
credential,
corpus text,
or repository image is retained.
Success requires HTTP 200,
`tool_use`,
one exact tool invocation,
raw duplicate-member pass,
exact status strings,
and one durable attempt row.
Synthetic run proves transport and schema behavior only;
it does not satisfy corpus-image or translation-review constraints.

Runner uses inspected Node standard library and direct HTTPS from credential-configured worktree.
It starts no subprocess and writes no repository file.
One request receives 360,000-millisecond abort signal.
Failure is spent evidence and causes no redispatch.

If route passes,
GLM advances to same Candidate I prototype controls and one pinned-Carena calibration with complete-page review.
No recommendation follows from synthetic probe alone.

## Evidence records

### Exhaustive family roster

- Candidate:
  every ledger row.
- Claim:
  GLM is only live vision family distinct from both authors after Kimi rejection.
- Gate:
  hard family and image constraints.
- Status:
  pass for GLM only.
- Primary source:
  `https://hyper.charm.land/v1/models`,
  accessed 2026-08-31.
- Snapshot:
  `~/temp/agent/hyper-models-candidate-i-kimi-vet-20260831.json`,
  17,212 bytes,
  SHA-256 `d68a71501ffa7c67825467ef935c177ad0abb01560f81f17c3fe65fe88742ab7`.
- Outcome:
  GLM advances to exact route probe.

### GLM multimodal and output envelope

- Candidate:
  GLM 5.3 Flash.
- Claim:
  native vision and output envelope exceed Candidate I static requirements.
- Gate:
  hard capability and envelope screening.
- Status:
  documentation and live catalog pass.
- Primary source:
  `https://docs.z.ai/guides/vlm/glm-5.3-flash`,
  accessed 2026-08-31.
- Corroboration:
  Hyper live row and prior Synthetic real-image probe.
- Outcome:
  advance.

### Hyper Anthropic forced-tool route

- Candidate:
  GLM 5.3 Flash.
- Claim:
  current Hyper mapping accepts image plus forced tool at default reasoning and returns exact input within ceiling.
- Gate:
  hard route and structured-output boundary.
- Status:
  pending.
- Counterevidence:
  official Z.ai OpenAI function-calling page documents only automatic tool choice;
  protocol and gateway may differ.
- Outcome:
  one-attempt metadata-only probe required.

### Translation-review consumer fit

- Candidate:
  GLM 5.3 Flash.
- Claim:
  candidate-scoped ballot can completely and accurately review pinned Carena source,
  archive,
  candidate,
  and page evidence.
- Gate:
  hard publication evidence and scored quality.
- Status:
  pending exact Candidate I prototype and Carena calibration.
- Counterevidence:
  prior Synthetic schema and image probes are not translation-review evidence.
- Outcome:
  no recommendation before complete consumer review.

## Current disposition

No recommendation.
GLM 5.3 Flash is only serious alternative after exhaustive family screening.
It may enter one exact Hyper Anthropic forced-tool probe.
Product allowlist,
production code,
and decision records must not adopt it before route pass,
Candidate I control pass,
pinned-Carena completion,
and complete-page review.
