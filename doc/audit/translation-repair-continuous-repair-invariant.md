# Continuous repair invariant verification

## Scope

This audit closes task 26 at code and scripted-integration level.
It does not claim live-provider or corpus-output readiness.
Fresh provider verification and complete output reading remain separate work.

Quality rejection has one allowed effect:
continue inside owning stage from latest exact text and evidence.
It cannot authorize fallback publication or whole-entry retry.
Provider loss,
caller abort,
and exact deterministic task cycle pause as `INCOMPLETE`.

## Recovery paths

### Naturalness

`consolidation-naturalness-settle.ts` continues from candidate-bound rejection findings without finite correction count.
`absolute-naturalness-review-stage.ts` separates defect discovery from acceptance challenge.
Exact correction cycles and unavailable providers raise operational interruption.

### Unfilled source passages

`translate-stage-repair.ts` continues from latest rejected slate and findings.
`insertion-coverage-repair.ts` continues from latest coverage counts,
anchored evidence,
destinations,
and page shortfall.
`carried-insertion-completeness.ts` verifies carried evidence remains in would-ship page.

### Archive-only wording

`archive-block-review-stage.ts` requires aligned source evidence or narrow deterministic editorial evidence.
Retained text receives two naturalness responsibilities.
Rejected text enters contributor-preserving correction selection and re-preparation.

### Final lane choice

`consolidate-slice-buy.ts` keeps unendorsed or publication-ineligible standing inside consolidation.
`consolidation-stage-repair.ts` supplies prior slate,
role-aliased producer and judge relations,
selection ballots,
gate ballots,
terminal,
and findings to next producer strategy.
Only gate-endorsed fresh consolidation returns from unsafe-standing path.

### Source destinations

`translate-stage.ts` excludes archive incumbent that fails source-derived deterministic floor and uses absent-mode continuation.
`consolidate-driver.ts` validates ordinary standing as well as syntax-bearing standing,
so contest-endorsed repair wording missing a source destination enters final-selection recovery.
`destination-completeness.ts` is defensive pre-write invariant.

### Metadata, contributor identity, and visual evidence

`front-matter-translation.ts` and front-matter completeness guards keep source-only metadata inside stage-local repair.
Contributor authority floors translation,
contest,
consolidation,
and polish candidates before final defensive check.
Visual evidence uses distinct reading responsibilities and pauses before lanes when evidence remains unavailable.

## Strict publication boundary

`pass-entry.ts` refuses known unfilled passages before artifact construction.
`pass-entry-persist.ts` checks final selection and absolute naturalness before filesystem mutation.
`publish-fixed.ts` checks front matter,
contributor identity,
artifact agreement,
and source destinations before creating or writing page.

Defensive completeness errors map to `INCOMPLETE` and scheduler outcome `stopped` in `entry-error-outcome.ts`.
They do not trigger whole-entry resumable failure.
`UnansweredContestSliceError` remains ordinary resumable `ERROR` because it is artifact and tally inconsistency,
not quality verdict.

## Operational behavior

The focused verification log is
`~/temp/agent/test-continuous-repair-invariant-20260829.log`.
It records 17 runner `PASS` lines,
zero `FAIL` lines,
and exit code zero across recovery,
strict publication,
scheduler,
and complete pass-entry boundaries.

The current full-suite log is
`~/temp/agent/buildAndTest-destination-quality-recovery-final-20260829.log`.
It records 871 runner `PASS` lines,
zero `FAIL` lines,
and exit code zero.
The log names both destination-recovery integration cases,
so unchanged suite-line count is not being used alone as evidence.

The handover names 82 GFP logs under `~/temp/agent/`.
A filesystem check found all 82.
They divide into 41 red logs and 41 green logs.
Every red log contains runner `] FAIL ` evidence.
Every green log contains runner `] PASS ` evidence and no runner `] FAIL ` line.

The final-selection recovery test preserves exact caller abort identity and writes no unsafe settlement.
Provider-silent unsafe standing reports `provider-unavailable` and writes nothing.
Exact failed-evidence cycles report stage-specific interruption and write nothing.
Identical unsafe twins share one final safe recovery chain.

## Accepted cycle policy

An exact deterministic cycle is not publishable completion.
It pauses indefinitely until prompt strategy,
roster,
provider,
evidence,
or code changes.
Restarting unchanged work may replay durable payloads and re-derive same pause.
This is accepted fail-closed policy rather than remaining fallback behavior.

## Verification boundary

No stopped generation-11 or generation-12 run is evidence for this audit.
No current real-provider run has traversed every new recovery path.
The next work may define and optimize Carena completion path,
then launch fresh isolated roots under current pipeline digest and strict publication verification.
