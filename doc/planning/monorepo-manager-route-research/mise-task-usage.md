# How this repository actually uses Mise for tasks

Read-only study,
 2026-09-17,
of `/var/home/user/Monochromatic` at `7bb4d1ec5`.
Scope:
 task definition,
 composition,
 naming,
 and execution.
Tool provisioning appears only where it changes how tasks behave.

Nothing here is adopted.
Rule `DRR` applies:
this is a proposal input,
not a decision record,
and no design document was changed.


Merged into "Configuration authoring" in `doc/planning/monorepo-manager-from-scratch-design.md` on 2026-09-17.
Spot-checked then:
the root `test` task spawns only the three root suites (`mise.toml:681-698`),
`doc/handover/lint-fix-2026-06.md` contains no mention of a glob,
and all three stale `mise watch ... -- node ...` tasks are still present.

## Commands and measurement basis

Every count below came from one of these,
run from the repository root unless stated:

```sh
mise --version                                   # 2026.9.5 linux-x64 (2026-09-10)
mise tasks --all --hidden --json                 # 1,886,490 bytes, 1836 entries
mise tasks --all --json                          # 1807 entries
mise config ls
mise task info '//:lint'
mise task info '//package/module/jsonc-edit:lint:oxlint'
mise task info '//package/oxlint-plugin/no-restricted-syntax:build:js:node'
hyperfine --warmup 1 --runs 5 'mise tasks --all --hidden --json'
rg --files . --glob 'mise*.toml'
rg --line-number '^\[' mise.toml
git log --since='90 days ago' --pretty=format:'%H' -- mise.toml
```

Structured counts came from four scratch TypeScript files run with `node`,
kept beside this document:
`analyze.ts` (task-dump field census),
`files.ts` (per-file shape),
`inline.ts` (embedded script volume),
`overrides.ts` (template shadowing and partial overrides).

Claims are marked Verified with the backing command or `path:line`,
or Unverified.

## 1. How tasks are defined today

### Where definitions live

Verified (`rg --files . --glob 'mise*.toml'`,
 plus `wc -l`):

- 182 active Mise configuration files:
   `mise.toml`,
   `mise.no-env.toml`,
   `mise.local.toml`,
   and 179 under `package/`.
- 15 further files under `package-paused/` and `package-deprecated/`,
   excluded from discovery.
- 10,858 newline-terminated lines across the 182 active files.
- `package/**` alone accounts for 8,463 of those lines.

Verified (`rg --line-number '^\[' --glob 'mise*.toml' package/`,
tallied by table kind):
every package file contains only three kinds of table.

- `[tasks.*]`:
   1,788 headers.
- `[vars]`:
   5 files
   (`package/desktop-app/terminal/mise.toml:27`,
   `package/music-player/design/mise.toml:1`,
   `package/cli/nested-wayland-session/mise.toml:10`,
   `package/music-player/android-app/mise.toml:19`,
   `package/music-player/desktop-app/mise.toml:45`).
- `[env]`:
   3 files
   (`package/music-player/desktop-app/mise.toml:32`,
   `package/desktop-app/file-manager-qt/mise.toml:6`,
   `package/desktop-app/terminal/mise.toml:16`).

No package file declares `[tools]`,
`[settings]`,
or `[hooks]`.
This is the single most load-bearing fact for the one-root-file decision,
and section 5 returns to it.

Verified (`mise.toml:1`,
`file-enforcer.config.ts:718-723`):
the root `mise.toml` is generated by concatenating `mise.no-env.toml`
with a dynamic `[env]` block.
`mise.local.toml` is four lines,
gitignored through the `*.local.*` pattern (`.gitignore:11`),
and sets only `[settings] jobs = 16`.

### Discovery, namespacing, and the working directory

Verified (`mise.toml:15-26`):

```toml
monorepo_root = true

[monorepo]
config_roots = [
  "package/*/*",
]
```

Verified (`mise.toml:254`):
`task.monorepo_depth = 3`,
matching the `package/<category>/<name>` layout.

Consequences,
all Verified from `mise tasks --all --hidden --json`:

- Every task carries a `//`-prefixed label.
   1836 of 1836 are namespaced;
   none are bare.
- 171 distinct `source` files contribute tasks;
   54 tasks come from the root `mise.toml`.
- Every task has a non-empty `dir`,
   with 174 distinct values,
   defaulting to the config root that declared it.
   Only 35 lines in the whole tree set `dir` explicitly
   (`rg --count-matches 'dir = ' --glob 'mise*.toml' .`).

Verified (`mise tasks --json` run from `package/module/jsonc-edit`):
standing inside a package lists 56 tasks,
the 41 visible root tasks plus that package's 15,
and nothing from sibling packages.
File placement therefore supplies both the label
and the relevance filter,
for free.

### Composition: `task_templates` and `extends`

Verified (`mise.toml:511-620`):
the root declares 19 `task_templates`.

- Fan-out aggregates whose body is `{{vars.fanout}}`:
   `build`,
   `build:js`,
   `watch:build`,
   `watch:build:js`,
   `test`,
   `watch:test`,
   `lint`,
   `prepare`.
- Bundler leaves:
   `build:js:browser`,
   `build:js:client`,
   `build:js:node`
   and their three `watch:` counterparts,
   each a single `rolldown --configLoader native --config rolldown.<target>.config.ts` line.
- Dispatcher leaves:
   `lint:oxlint` (`mise.toml:590-597`),
   `format:oxlint` (`:598-605`),
   `lint:types` (`:606-612`).
- Runners:
   `test:unit` (`:549-580`),
   `watch:lint:types` (`:614-616`).

Verified (`files.ts`):

- 1,341 `extends = "..."` lines across the active tree.
- 1,898 `[tasks.*]` headers across the active tree,
   so 70.7 percent of every task block written in this repository
   is a bare re-declaration of a template name.
- 52 files contain nothing but bare `extends` blocks.
- The heaviest are 14 bare extends each in
   `package/module/zip-writer/mise.toml`
   and `package/module/logger/mise.toml`.

Verified (`files.ts`,
 extends-target tally):
the distribution is long-tailed toward lint and build.
`lint:oxlint` 143,
`lint:types` 143,
`lint` 141,
`format:oxlint` 140,
`build` 110,
`build:js` 106,
`watch:build` 104,
`watch:build:js` 104,
`test:unit` 103,
`build:js:node` 89,
`watch:build:js:node` 86,
`build:js:browser` 28,
`watch:build:js:browser` 27,
`build:js:client` 9,
`watch:build:js:client` 3,
`watch:test` 2,
`watch:lint:types` 2,
`test` 1.

Verified (`overrides.ts`):

- 21 blocks combine `extends` with another field.
   13 of those add only `hide = true`.
- Local fields win over the template.
   `package/oxlint-plugin/no-restricted-syntax/mise.toml:7-11` extends `build:js:node`
   and then sets its own `run`;
   `mise task info` for that target prints only the local command,
   so the `extends` line is dead.
- 84 package blocks carry a template name with no `extends` at all,
   defining something entirely different under the same name.

### Naming and the implicit dependency graph

Verified (`mise.toml:624-625`):

```text
# Task naming follows `${category}${optional(`--${modifier}`)}:${child}`
# never use depends or post depends, use run only because it's the easiest to understand.
```

Verified (`analyze.ts`):

- 208 distinct leaf names.
   Instances by colon depth:
   478 at depth 1,
   979 at depth 2,
   261 at depth 3,
   118 at depth 4.
- The `--modifier` half of the stated convention is used exactly once,
   by `//:prepareAndBuild--allowFailure` (`mise.toml:1091`).

The colon hierarchy is not decoration.
Verified (`mise.toml:291-307`):
`vars.fanout` reads `MISE_TASK_NAME`,
enumerates `mise tasks --all --hidden --json`,
keeps names one colon level deeper,
and spawns `mise run <child>` for each.
So `build` runs whatever is named `build:*`,
and a task's position in the graph is decided by its name.

### Field census

Verified (`analyze.ts` over `mise tasks --all --hidden --json`,
1836 tasks):

- `run` non-empty: 1835.
   The exception is `//package/module/test:build:js`,
   which is `depends`-only (`package/module/test/mise.toml:4-7`).
- `shell` non-empty:
   1215,
   all of them the single string `node --input-type=module-typescript -e`.
   The other 621 use the platform default.
- `description` non-empty:
   1009;
   827 tasks have none.
- `usage` non-empty:
   135,
   of which 102 are inherited from the `test:unit` template.
   The remaining 33 are spread over `run`,
   `fuzz`,
   `fuzz:coverage`,
   `test:browser`,
   `test:mutation`,
   and a handful of one-offs.
- `depends` non-empty: 41.
- `hide` true: 29.
- `aliases` non-empty:
   5
   (`bt`,
   `pi`,
   `file-enforcer`,
   `t`,
   `tW`).
- `env` non-empty:
   4
   (`//:lint`,
   `//:secrets:edit`,
   `//package/git-policy/cli:pack:npm`,
   `//package/pi-plugin/advisor:verify:host`).
- `depends_post`,
   `wait_for`,
   `sources`,
   `outputs`,
   `tools`,
   `timeout`,
   `raw`,
   `interactive`,
   `quiet`,
   `silent`,
   `global`,
   `args`,
   `file`:
   zero non-empty in all 1836 tasks.

Zero `sources` and zero `outputs` means no task is ever skipped as fresh.
Verified independently at `doc/research/bazel-migration-dx.md:40-44`.
Verified (`mise.toml:243-244`):
`task_timeout` is commented out with the note
"Per task timeout not implemented yet in Mise."

### Run bodies

Verified (`analyze.ts`):

- 1,951 `run` entries across 1836 tasks;
   90 tasks use an array `run` of more than one entry.
- 23 entries are not strings but Mise's native task-list form,
   `{ tasks = [...] }`,
   used by 6 tasks
   (`//:format`,
   `//:lint`,
   `//:prepare:pnpm`,
   `//:prepareAndBuild`,
   `//:watch:buildAndTest`,
   `//package/config/tofu:test`).
- 166 entries invoke `mise run` as a subprocess.
- 112 entries contain `&&`;
   26 contain a `;` separator.
- 5 lines set `run_windows`,
   in `package/oxlint-plugin/no-restricted-syntax/mise.toml:11`
   and `package/oxlint-plugin/prefer-readonly-parameter-type/mise.toml:11,41,46,52`.
   These never appear in a Linux `mise tasks --json` dump.

Verified (`analyze.ts`,
 first word of each run line):
the top entries are `{{vars.fanout}}` 564,
`{{vars.dispatch_workspace_node}}` 432,
`rolldown` 244,
`mise` 172,
`cargo` 119,
`{{vars.parse_usage_args}}` 116,
`node` 97.

Verified:
1,159 of 1,836 tasks (63.1 percent) have a `run` containing `{{vars.`.

### Inline Node scripts

Verified (`mise.toml:280-509`):
the root declares seven `vars`,
six of which are executable TypeScript interpolated into task bodies.

- `fanout` (`:291-307`):
   intra-package child fan-out.
- `fanout_packages` (`:322-347`):
   cross-package fan-out by task-name suffix,
   with a worker pool bounded by `availableParallelism()`
   except for `watch*` suffixes,
   which run unbounded.
- `dispatch_workspace_node` (`:358-420`):
   resolves the monorepo root,
   prefers `dist/final/node/<entry>.mjs` over `src/<entry>.ts`,
   and rebuilds the oxlint config on mtime drift.
- `parse_usage_args` (`:426-451`):
   a hand-written shell-word parser for Mise's `usage_args` string.
- `run_test_files` (`:455-487`):
   bounded parallel `node <file>` runner
   that locates each file's nearest ancestor `mise.toml` for cwd.
- `run_playwright` (`:502-509`):
   Playwright inside podman with `--security-opt label=disable`.

Verified (`inline.ts`,
over 181 active files excluding `mise.no-env.toml`):
118 triple-quoted blocks holding 1,155 lines,
11.8 percent of all configuration lines.
The heaviest are
`mise.toml` at 339 of 1,331 lines,
`package/music-player/desktop-app/mise.toml` at 229 of 541,
`package/desktop-app/terminal/mise.toml` at 116 of 274,
`package/kwin/key-helper/mise.toml` at 71 of 130.

Verified (`mise.toml:259-263`):
the shell choice is a stated policy,
not an accident:
trivial bodies use the platform default,
and bodies needing logic declare the Node shell per task.
Verified (`doc/decision/mise-task-node-invocations.md`):
the shape was measured and deliberately kept,
with the benchmark recorded there.

### The parallel systems beside Mise (rule `EVL`)

Layer 1,
 the tool itself:
covered above.

Layer 2,
 parallel systems.
Verified:

- `package.json` scripts:
   one entry in the whole workspace,
   the placeholder at `package/figma/to-penpot/package.json:16`.
- Shell scripts:
   three `.sh` files,
   only one inside the active package tree
   (`package/dev-script/file-enforcer/generator/entrypoint.sh`),
   the others under `package-paused/` and `doc/audit/`.
- CI:
   15 workflow files,
   9 of which reference Mise.
- `package/dev-script/task-util`:
   31 source files providing `depends`,
   `command`,
   `append`,
   `tsc-filter`,
   `oxlint-wrapper`,
   and `pnpm-filter`.
- 16 active files named `mise.<action>.ts`,
   each invoked as `run = "node src/mise.verify-extension.ts"` or similar.

Layer 3,
 what is inside those parallel systems.
Verified:

- CI does not run the repository task graph.
   `rg --only-matching 'mise run [^\s"]*' .github/workflows/` returns
   only targeted package tasks
   (`//package/config/pnpr:publish`,
   `//package/git-policy/cli:perf:lifecycle-latency`,
   the two fuzz workflows,
   `readonly-semantic-bridge`)
   plus `changeset:version` and `changeset:build`.
   No workflow runs `//:lint`,
   `//:test`,
   or `//:build`.
   Corroborated at `doc/research/bazel-migration-dx.md:50-51`.
- CI otherwise uses `mise install <tool>` plus `mise exec`,
   not `mise run`.
- `package/dev-script/task-util/src/depends*.ts` is a make-style staleness checker,
   the capability Mise's own `sources` and `outputs` would provide.
   It is invoked from exactly one active place,
   `mise.toml:1030`,
   for the Playwright image build.
- One test exercises the task graph itself:
   `package/dev-script/task-util/src/build-and-test.unit.test.ts`,
   described in `package/dev-script/task-util/README.md` as consuming the generated
   `mise.toml` "rather than a bundled TypeScript function:
   the configuration is the artifact under test."

Layer 4,
 inline annotations.
Verified:
two `TODO` markers in all Mise configuration
(`mise.toml:1104` and its source line).
No `FIXME`,
 `HACK`,
 or `XXX`.
The debt is not annotated inline;
it is written up instead.
17 files match `doc/troubleshooting/mise-*.md`.

Layer 5,
 suppressions and exceptions.
Verified:

- `mise.toml:2` carries `# harper:ignore`,
   the only linter suppression in the file.
- `dprint.json` extends `package/config/dprint/index.json`,
   whose excludes do not list any `mise*.toml`,
   and whose plugin list includes `toml-0.7.0.wasm`.
   So the TOML is formatted,
   but the 1,155 lines of TypeScript inside its strings are not:
   the dprint TypeScript plugin is disabled
   (`package/config/dprint/index.json:39-40`),
   and no `.ts` linter sees a `.toml` file.
- `mise.toml:226-241`:
   seven backends are disabled by policy,
   each with a recorded reason
   (`asdf` for Windows support,
   five others for weak validation).
   This is the healthy kind of suppression:
   justified in place.

Layer 6,
 stated policies.
Verified:

- `AGENTS.md:875-882` (`CM3`):
   all builds and tasks via `mise run`,
   never `pnpm exec` or raw tools.
   Practice matches:
   zero `pnpm exec` lines in any task body.
- `AGENTS.md:977-979` (`SCR`):
   inline Mise task logic as Node TypeScript or move it into a package bin,
   and "Never create `mise.<action>.ts` files."
   Practice contradicts the last clause:
   16 such files exist.
- `mise.toml:625`:
   "never use depends or post depends,
   use run only."
   Practice contradicts it 41 times.
- `AGENTS.md:892` (`CM5`):
   run package tasks as `mise run //package/path:task`,
   not `mise run --cd`.
- `AGENTS.md:899-903` (`WC2`):
   root files are generated by file-enforcer;
   edit the source and commit the output as-is.

### Drift against the existing coverage ledger

Verified:
`doc/planning/mise-removal-coverage.md:100-110` records
178 active configuration files,
9,422 lines,
1,811 tasks from 169 source files.
Today the same measurements give
182 files,
10,858 lines,
1,836 tasks from 171 source files.
The surface grew about 15 percent in lines since that ledger was written.

## 2. The good

### Templates plus discovery give one vocabulary across 179 packages

Verified:
`mise run //package/<anything>:lint:types` works
for all 145 packages that declare it,
with one definition at `mise.toml:606-612`.
An agent or a person never has to read a package
to know how to type-check it.
`AGENTS.md:887` depends on exactly this
when it tells agents to run `mise run //package/<path>:test:unit`.

This is the property meow must not lose.
It is worth more than the boilerplate it costs.

### Bazel-shaped labels, already in muscle memory

Verified:
1836 of 1836 tasks carry a `//path:task` label,
and `mise task info '//:lint'` resolves one.
The design's "Follow Bazel" target syntax
is not a new convention for this repository;
it is the one already in use.

### Standing in a package narrows what you see

Verified (`mise tasks --json` from `package/module/jsonc-edit`):
56 tasks,
the 41 visible root tasks plus that package's 15.
No sibling noise.
This is a real affordance,
and it comes entirely from file placement.

### The working directory is derived, not declared

Verified:
174 distinct `dir` values across 1836 tasks,
but only 35 explicit `dir =` lines.
Nearly every task gets the right cwd
because of where its file sits.
`cargo nextest run` in `package/linter/rust/mise.toml`
needs no path at all.

### The Node shell is a deliberate, measured choice

Verified (`doc/decision/mise-task-node-invocations.md`):
the alternative was benchmarked
(spawn 56.9 ms against import 37.3 ms at the node level,
190.0 ms against 173.5 ms at the mise level,
with a real `lint:oxlint` at 466 ms)
and rejected on fragility grounds,
not on taste.
Rule `SCR` then encodes it.
The repository writes no shell scripts,
which removes an entire class of quoting bug.

### Generated root configuration keeps a hand-editable source

Verified (`mise.toml:1`,
 `file-enforcer.config.ts:718-723`):
`mise.no-env.toml` is the file a person edits;
the machine-derived `[env]` block is appended.
The seam is clean,
and `AGENTS.md:899-903` states it.

### Backend disabling is justified in place

Verified (`mise.toml:226-241`).
Each disabled backend carries its reason in a `#region` comment.
This is the layer-5 pattern that signals discipline rather than debt.

## 3. The bad

### Seven of every ten task blocks written here are boilerplate

Verified (`files.ts`):
1,341 of 1,898 task blocks are a bare `extends`.
Counting header plus body,
2,632 of the 8,463 lines under `package/**` exist
only to opt a package into a template it could have been selected for.

Concretely,
`package/module/jsonc-edit/mise.toml` opens with 13 consecutive blocks
of the form:

```toml
[tasks."build:js:browser"]
extends = "build:js:browser"
```

Adding a package means copying that block.
Adding a template means editing up to 143 files.

### `buildAndTest` is the same two lines, 47 times

Verified (`odd.ts` over the task dump):
56 tasks are named `buildAndTest`,
with 6 distinct bodies,
and 47 of them are byte-identical:

```toml
run = ["mise run build", "mise run test:unit"]
```

There is no `task_templates.buildAndTest`
(`rg --line-number '^\[task_templates' mise.toml` lists 19 templates,
 not including it).
The most duplicated task in the repository is the one template nobody wrote.

### Each package repeats its description, or has none

Verified:
827 of 1836 tasks have no description.
The 1009 that do include 143 copies of "Lint with Oxlint"
inherited from one template,
alongside genuinely package-specific text.
`mise tasks` output is therefore a mix of one sentence repeated
and long tails of silence.

### The stated `depends` ban is broken 41 times, quietly

Verified:
`mise.toml:625` forbids `depends`;
41 tasks use it.
The uses are not frivolous:
12 are `//package/pi-plugin/*:verify:extension` depending on `build`,
5 are oxlint plugins whose `test:unit` depends on `build`,
6 are `//package/music-player/android-app:*` depending on `build:native`.
Every one expresses a real build-before-test edge
that `run = ["mise run build", ...]` would express worse
by spawning another `mise` process.
The policy lost,
and no document records that it lost.

### The two ordering mechanisms coexist with no rule

Verified:
6 tasks use Mise's native `{ tasks = [...] }` run entries,
including `//:lint` (`mise.toml:858-861`)
and `//:format`.
Verified (`mise.toml:285`):
the `fanout` comment says
"No `:::` separator or other mise-specific inline sugar."
So the root config both avoids Mise task sugar in its fan-out helper
and uses Mise task sugar two hundred lines later.
Nothing states when each applies.

### `usage` is effectively one template's feature

Verified:
135 tasks declare `usage`,
and 102 of those inherit it from `test:unit`.
Only 33 hand-written usage specs exist across 179 packages.
Verified (`doc/decision/mise-task-node-invocations.md`):
declaring `usage` changes argument delivery,
from an appended argv vector to `usage_*` environment variables,
which is why 116 run lines must interpolate `{{vars.parse_usage_args}}`
to undo the encoding.
The feature is avoided because using it costs a parser.

### Nothing is cached, so the aggregates are slow by construction

Verified:
zero tasks declare `sources` or `outputs`.
Verified (`hyperfine --warmup 1 --runs 5 'mise tasks --all --hidden --json'`):
223.3 ms plus or minus 8.2 ms per enumeration.
Verified (`mise.toml:291-307`,
 `:322-347`):
every fan-out task pays that enumeration once.
Verified (`doc/decision/mise-task-node-invocations.md`):
Mise process startup is about 135 ms.

Inference,
not measured end to end:
`mise run lint` enumerates once,
then spawns one `mise run` per matching package.
Verified:
166 package tasks are named `lint`,
141 of which extend the fan-out template and therefore enumerate again.
So one `mise run lint` performs at least 142 enumerations,
about 32 seconds at the measured 223 ms,
and starts at least 167 `mise` processes,
about 23 seconds at the 135 ms figure in the decision record,
before any linter runs.
Rule `QAB` applies:
this is arithmetic over measured components,
not an observed wall clock,
and running the real aggregate was out of scope for this study.

### Short flags in configuration, against rule `LFF`

Verified:
`mise watch -w src -r -- ...` appears in
`package/kwin/key-helper/mise.toml:40`,
`package/desktop-daemon/hall-monitor/mise.toml:11`,
`package/webapp-productivity/rss/mise.toml:21`,
`package/ssg/aquati.cat/mise.toml:51`.
`AGENTS.md` rule `LFF` requires long form.
Two packages already migrated
(`package/webapp-productivity/done/mise.toml:26`,
`package/webapp-productivity/done-postcss/mise.toml:35`
both use `--watch` and `--restart`),
so the tree is mid-migration with no tracking.

## 4. The ugly

### A task's real body is invisible from both its definition and `mise task info`

Verified:
1,159 of 1,836 tasks (63.1 percent) have a `run` containing `{{vars.`.
Verified (`mise task info '//package/module/jsonc-edit:lint:oxlint'`):

```text
Run:
  {{vars.dispatch_workspace_node}}
  ensureOxlintConfig()
  runWorkspaceNode('package/dev-script/task-util', 'oxlint-wrapper', [])
```

The inspection command prints the template,
unexpanded.
To learn what `//package/module/jsonc-edit:lint:oxlint` does,
a reader must open `mise.toml`,
find `[task_templates."lint:oxlint"]` at line 590,
then read `vars.dispatch_workspace_node` at lines 358 to 420,
a 62-line function that resolves the monorepo root,
chooses built output over source,
walks two package directories,
compares mtimes,
and may spawn `mise run //package/config/oxlint:build:js:node`.
None of that is reachable from the target name.

This is the finding that most directly justifies meow.

### Textual interpolation into one module scope, with no scoping rule

Verified (`doc/troubleshooting/mise-inline-task-import-collision.md`):
`//package/cli/nested-wayland-session:run` failed with
`SyntaxError: Identifier 'execFileSync' has already been declared`
because a `vars` snippet imported `execFileSync`
and the interpolating body imported it again.

Verified (`mise.toml:355-357`):
the fix is a comment:

```text
# Its imported bindings are
# reused by any inline follow-up in those bodies, so the follow-up must not
# reimport them.
```

Verified (`mise.toml:490-494`):
`run_playwright` adds a second such rule:
the interpolating task "must put `{{vars.parse_usage_args}}` BEFORE
`{{vars.run_playwright}}` and must not re-import `execFileSync`".

So correctness across 1,159 tasks rests on ordering and non-redeclaration rules
that live only in TOML comments,
that no tool checks,
and that fail at runtime in whichever task happens to violate them.

### The machinery that replaced Mise's native glob cites a document that does not contain the evidence

Verified (`mise.toml:308-312`):

```text
# node -e cross-package fanout: the JS replacement for the native `mise
# '//package/...:<task>'` monorepo glob, which is unreliable (aborts the
# aggregate while child fanout work is still in flight; see
# doc/handover/lint-fix-2026-06.md).
```

Verified:
`rg --count-matches 'fanout|package/\.\.\.|aggregate' doc/handover/lint-fix-2026-06.md`
returns 2,
both in unrelated passages
(`:126` "The aggregate failed package list from that run was:",
`:199` "because the root fanout is large").
`rg --line-number 'glob' doc/handover/lint-fix-2026-06.md` returns nothing,
sanity-checked by widening to the whole 199-line file.
The claim is restated at `doc/research/bazel-migration-dx.md:47`
but with no primary observation either.

So the justification for 85 lines of hand-written orchestration
(`mise.toml:322-347` plus every `fanout_packages` caller)
is currently unsupported at the path it names.

Worse,
Verified (`doc/decision/desktop-app-podman-build.md:84-87`):
an accepted decision document still instructs the reader to use the native glob:

```bash
mise run '//package/desktop-app/...:verify:container'
```

One document says the mechanism is unreliable,
another tells people to use it.

### `mise run test` at the root does not run 15 packages' tests

Verified (`mise.toml:681-698`):
the root `test` task,
described as "Run all tests,
 or pass args to filter unit tests",
spawns `mise run test:unit`,
 `test:browser`,
 and `test:e2e`,
all root-level.
It does not fan out to packages.

Verified (`mise.toml:701-702`):
the root `test:unit` extends the template,
which globs `**/*.unit.test.ts` from the repository root.

Verified (task dump,
 tasks named exactly `<pkg>:test`):
23 package-level `test` tasks exist,
and 15 of them run a native toolchain the glob cannot reach:

```text
//package/cli/forbidden-strings:test            cargo nextest run --release
//package/cli/nested-wayland-session:test       cargo nextest run
//package/desktop-app/file-manager:test         cargo nextest run --release
//package/desktop-app/file-manager-gtk-sticky:test
//package/desktop-app/file-manager-qt:test
//package/desktop-app/terminal:test
//package/fuzz/forbidden-strings:test           cargo nextest run --lib --release
//package/linter/kotlin:test                    ./gradlew test --no-daemon --console=plain
//package/linter/rust:test
//package/music-player/desktop-app:test
//package/music-player/truepeak-core:test
//package/rust-linter-plugin/builtin:test
//package/rust-module/forbidden-regex:test
//package/rust-module/rust-linter-core:test
//package/rust-module/rust-linter-pattern:test
```

Verified (`mise.toml:855-861`):
`//:lint` does fan out via `{{vars.fanout_packages}}`,
so lint reaches these packages and test does not.
The asymmetry is invisible from either task's name or description.
`//:validate` (`mise.toml:966-968`) chains `format`,
 `build`,
 `test`,
so it inherits the gap,
and `//:buildAndTest` (`mise.toml:792`) calls the same root `test`.

This is the strongest latent-bug candidate found.

### Three active tasks use a `mise watch` form the repository has already proven broken

Verified (`doc/troubleshooting/mise-watch-runs-tasks-not-commands.md`):
`mise watch ... -- node src/server.ts` makes Mise search for a task named `node`
and abort with `no task ... :node found`.
The document traces it to `src/cli/watch.rs:29-42` in mise `v2026.7.0`,
where the trailing values are parsed as task names.

Verified,
still present in active packages:

- `package/kwin/key-helper/mise.toml:40`
   `run = "mise watch -w src -r -- node src/index.ts"`
- `package/desktop-daemon/hall-monitor/mise.toml:11`
   same body.
- `package/webapp-productivity/rss/mise.toml:21`
   same body.

The two packages named in the troubleshooting document were fixed;
these three were not.
Unverified:
whether mise 2026.9.5 still behaves this way.
Running them was out of scope,
since each starts a watcher.

### 96 task blocks silently shadow a template name

Verified (`overrides.ts`):
84 package blocks and 12 root blocks carry a template name with no `extends`.
Nothing marks the divergence in either direction.

The consequence is not theoretical.
Verified (`package/cli/wg-quicker-exempt/mise.toml:21-23`):

```toml
[tasks."test:unit"]
description = "Run instruction and lifecycle unit tests"
run = "cargo test"
```

A reader who knows `test:unit` from the other 103 packages
will expect a Node glob runner
and get `cargo test`.
Verified (`package/music-player/design/mise.toml:387-398`):
that package's `lint` and `test:unit` have identical bodies,
both `{{vars.validate_question}}`,
so "linting" and "testing" are the same command under two names.

### `hide` does not hide from the thing that runs tasks

Verified:
29 tasks set `hide = true`;
`mise tasks --all --json` returns 1807 and `--hidden` returns 1836.
Verified (`mise.toml:301`,
 `:342`):
both fan-out helpers enumerate with `--hidden`,
so hidden tasks are still selected and still run.
`hide` is presentation only.
Verified:
`package/module/logger/mise.toml:30-36` hides `lint:types` and `watch:lint:types`
with no comment,
and six other packages do the same.
Nothing records why those packages hide a task the other 136 show.

### The generated root config drifts with install state and pollutes unrelated commits

Verified (`file-enforcer.config.ts:669-675`):
the `_.path` list is globbed from `node_modules/.bin` directories present on disk,
with the comment
"this list changes with install state:
 a fresh clone,
 a pruned package,
or a pnpm install that adds a dependency all move it."
Verified:
43 `node_modules/.bin` entries in the current `mise.toml`.
Verified:
`git log --since='90 days ago'` shows 76 commits touching `mise.toml`
and 45 touching `mise.no-env.toml`;
40 of the 76 touched the generated file without touching its source.
Verified (`git show 0c43ba6fa`):
a commit titled "docs(*):
 add SGD singular dir and name path rule"
also carries
"chore(*):
 sync generated mise.toml PATH entries" and a one-line `_.path` addition.
Tracked as issue #335 per the comment at `file-enforcer.config.ts:674`.

### `latest` pins turn the lockfile into permanent working-tree noise

Verified:
32 of the root tool entries are `= "latest"`;
`mise.lock` holds 38 tool entries.
Verified (`git diff --stat mise.lock` at session start):
140 insertions and 140 deletions uncommitted,
including `cargo-nextest` 0.9.143 to 0.9.144 and `hcloud` 1.67.0 to 1.68.0.
Verified:
38 commits touched `mise.lock` in 90 days.
Verified (`doc/troubleshooting/mise-lock-pins-repo-tool-version.md`):
the same tool name resolves to two different versions
depending on whether the shell is inside the repository,
because `mise upgrade` at home moves the global pin
while the repository stays on its lock.

### One repository setting forces a workaround into half the CI workflows

Verified (`mise.toml:221-222`):
`auto_install = true`.
Verified (`.github/workflows/npm-release.yml:36-41`):

```text
# The root mise config lists heavy dev tools (slint from a git rev, cmake, and
# more) with auto_install on; every mise run or mise exec would try to build
# them on the runner. Off, each job uses only the tools it installs below.
```

Verified:
7 of the 15 workflow files set `MISE_AUTO_INSTALL: 'false'`.
Verified (`mise.toml:271-278`):
the `enter` hook that `auto_install` was meant to pair with
is commented out,
"to reduce delays in daily development."
So the setting is on,
its intended trigger is off,
and every CI job pays for it in boilerplate.

### The `try` task exists to swallow exit codes

Verified (`mise.toml:629-639`):

```toml
[tasks.try]
description = "Run a task, tolerating failures"
```

Verified (`doc/troubleshooting/mise-build-and-test-failure-masking.md`):
`buildAndTest` once used `try`,
which masked build failures;
the fix was to stop using it and retain errors as causes instead.
The task remains available,
and `//:prepareAndBuild--allowFailure` (`mise.toml:1091`) is a second such surface.

### The buffer ceiling is a symptom nobody has priced

Verified (`doc/troubleshooting/mise-task-fanout-buffer.md`):
`mise tasks --all --hidden --json` exceeded Node's default 1 MiB
`execFileSync` buffer and produced `spawnSync mise ENOBUFS`.
Verified:
the current dump is 1,886,490 bytes,
and four separate places raise the cap to 16 MiB
(`mise.toml:299`,
 `:331`,
 `:816`,
 `:1001`).
Verified:
`mise.toml:817` uses `mise tasks ls --all --json`,
without `--hidden`,
while the three others use `mise tasks --all --hidden --json`.
Nothing explains the difference,
and it means `buildAndTest` cannot find a hidden `build` task
where the fan-out helpers can.

### 1,155 lines of TypeScript that no linter or type checker reads

Verified (`inline.ts`):
118 triple-quoted blocks,
1,155 lines.
Verified:
`package/config/dprint/index.json` formats TOML
but its TypeScript plugin is disabled at line 39,
and no oxlint or `tsc` invocation targets `.toml`.
Rules `TSD`,
 `TY1`,
 `LG1`,
 `PP7` and the rest of the TypeScript section of `AGENTS.md`
apply to none of it.
Verified:
the largest single block,
`vars.dispatch_workspace_node` at `mise.toml:358-420`,
is 62 lines including a recursive `newestMtime` walk,
which rule `ITR` would question in a `.ts` file
and which nothing questions here.

## 5. What meow's decided design fixes, keeps, or breaks

Decisions read from
`doc/planning/monorepo-manager-from-scratch-design.md`
"Configuration authoring" (`:1627-1696`),
"Consequences of one root file" (`:1697-1711`),
"User interface" (`:169-477`),
"Declarative configuration" (`:1466-1626`),
"HCL tooling" (`:1712-1884`).

The design document was edited by concurrent work during this study,
growing from 3,175 to 3,196 lines.
Line citations are against the 3,196-line state read on 2026-09-17.
Rule `EC1`:
nothing was touched.

### Fixed outright

Tags instead of file placement
removes 2,632 lines under `package/**`:
1,316 blocks whose entire content is a header plus one `extends` line.
A package that is a TypeScript library gets `lint:types`
because it is a TypeScript library,
not because someone pasted a block.
This is the largest single measured win available.

Most-specific-tag-wins with explicit override markers
addresses the 96 silent shadows and the 21 partial overrides directly.
Verified (`:1664-1694`):
the markers are the paired meta-arguments
`overridden = true` on the base and `override = true` on the specialization,
"validated as a pair".
`package/cli/wg-quicker-exempt:test:unit` running `cargo test`
becomes a marked override of a marked-overridable base,
or an error.
The dead `extends` at
`package/oxlint-plugin/no-restricted-syntax/mise.toml:8`
becomes detectable,
because a specialization with no marked base is an error.

A real expression language ends the textual interpolation hazard.
`vars.fanout` and friends are not values;
they are source fragments pasted into a module scope,
which is what produced
`doc/troubleshooting/mise-inline-task-import-collision.md`
and the ordering rule at `mise.toml:490-494`.
HCL values compose;
string fragments do not.

`meow watch` keeping everything up to date,
plus a content-addressed cache,
answers the zero-`sources`,
 zero-`outputs`,
 223-ms-per-enumeration finding.
The 142-enumeration arithmetic for one `mise run lint` disappears
when there is one resident process holding the graph.

`meow status` showing failed,
 paused,
 blocked,
 flaky,
 queued,
 and running
replaces `//:lint`'s current behavior of printing one aggregated error string
listing failed package names
(`mise.toml:346`).

Rule `SYB` and the argv-list command shape
remove the 26 `;`-separated and 112 `&&`-chained run entries
and the hand-written shell-word parser at `mise.toml:426-451`.

### Kept, and worth keeping deliberately

Bazel-shaped labels are already the repository's vocabulary:
1836 of 1836 tasks carry one.
`meow run //package/cow:test` will read as familiar,
not as migration cost.

Glob targets with shell-quoting diagnostics
match how people already reach for `//package/...`,
including at `doc/decision/desktop-app-podman-build.md:86`.

Byte-exact output forwarding for a single target
matches today's `stdio: 'inherit'` throughout the fan-out helpers.

The one root file is not the break it looks like for tools or environment.
Verified:
package configuration files contain only `[tasks.*]`,
five `[vars]`,
and three `[env]`.
There is no per-package tool pinning and no per-package settings to relocate.

### Made worse, or left unanswered

#### The one root file removes the relevance filter, and nothing replaces it

Verified:
from `package/module/jsonc-edit`,
`mise tasks` lists 56 tasks:
41 root plus that package's 15.
The design settles target labels
("they come from the package a tag selected",
 `:1698-1701`)
but says nothing about what a person or agent sees
when they are standing in a directory and ask what they can run.
With no per-package file,
directory context no longer narrows anything by construction.

Today this affordance is what makes `AGENTS.md:891` workable:
"Read root + package `mise.toml` for available commands."
That instruction has no meow equivalent.

#### The one root file is large, and the estimate should be on the record

Verified:
removing every bare-extends block leaves 5,831 substantive lines under `package/**`,
plus 1,240 lines of hand-maintained root source.
So a single `meow.hcl` expressing today's content
lands near 7,000 lines before any HCL syntax overhead.
The design acknowledges "The file is large by construction" (`:1708-1711`)
but does not name a number.
7,000 lines is past the point where
`hcl-edit`'s missing formatter (`:1544-1548`)
and the language server's outline become load-bearing rather than nice.

#### Argv-only commands have no home for 1,155 lines of logic

The design states:
"A task's command is an argv list,
spawned directly with no shell:
`command = ["node", "--test", "src"]`" (`:1642-1646`).

Verified:
1,215 tasks currently declare a Node shell,
1,159 interpolate a `vars` fragment,
and 1,155 lines of TypeScript live inside triple-quoted strings.
Much of that logic is not decoration:
`dispatch_workspace_node` chooses built output over source,
`run_test_files` finds each test file's package root,
`fanout_packages` bounds concurrency by `availableParallelism()`.

Under meow those responsibilities move.
Concurrency bounding and fan-out become the daemon's job,
which is decided.
Built-versus-source selection and test discovery do not have an owner yet.
The design's logic inventory (`:1488-1520`) counts
`file-enforcer.config.ts` units,
not these.
Rule `RCO` asks for a coverage ledger before removing an incumbent,
and these lines are not in one.

#### Tag inference must reproduce a graph that is currently encoded in names

Verified (`mise.toml:291-307`):
`fanout` selects children by counting colons in the task name.
Verified (`package/kwin/key-helper/mise.toml:42-43`):

```text
# Named `sea` (not `build:sea`) so the `build` fanout, which runs every `build:*`
# subtask, does not pull it in before the ESM bundle exists.
```

A task was renamed to change what runs.
Verified (`package/linter/kotlin/mise.toml:1-4`) records the mirror case:
the whole-tree scan lives at the root
"so the fanout-discovered package `lint` below stays a local compile check
and does not scan the repo twice."

Tag-based selection is a better model than name-prefix selection,
but these two comments are the only places the current graph's exceptions are written down.
Migration has to find the rest,
and there is no list.

#### Explicit override markers meet a tree with 41 unmarked `depends` edges and a ban on them

Verified:
`mise.toml:625` bans `depends`;
41 tasks use it;
the coverage ledger at
`doc/planning/mise-removal-coverage.md:86-92` records
"no cross-package build dependency is declared."
Verified:
that is not quite right today.
`package/module/test/mise.toml:7` declares
`depends = ["//package/module/test:build:js:node", "//package/module/test:build:js:client"]`,
which is fully qualified but intra-package.
All 41 are intra-package.

The design settles ordering from
"declared reads,
 writes,
 and explicit `depends_on`" (`:1559-1562`).
That is a third model,
neither today's name-prefix fan-out nor today's `depends`.
Nothing yet maps the 41 edges onto it,
and the repository's own stated policy about them is stale.

#### Nothing in the design covers the run-time reality that broke things here

Three findings have no counterpart in the decided design,
and each cost this repository a troubleshooting document:

- Windows command variants.
   Verified:
   5 `run_windows` lines,
   invisible in a Linux task dump.
   The design's platform matrix covers building meow,
   not per-task platform branching.
- Interactive tasks.
   Verified:
   `mise.toml:1134` `secrets:edit`,
   which `doc/planning/mise-removal-coverage.md:91-92` names
   as requiring `mise run --raw`.
   `meow run` forwards output and holds the terminal,
   which may or may not give a child a TTY.
- Trust in disposable worktrees.
   Verified (`doc/troubleshooting/mise-disposable-worktree-trust.md`):
   a fresh worktree needs its own trust before any task runs.
   Rule `IWT` sends agents into `git worktree add` for every decision-verb task,
   so meow's AUR-style full-file trust prompt (`:389-418`)
   will fire on every such worktree.
   An agent cannot answer an interactive prompt.

## 6. Questions for the user

Rule `QGR`:
everything settled requirements already determine has been left out.
Four questions remain,
each turning on preference or authority rather than measurement.

### Question 1: what replaces standing in a directory?

Today,
 `mise tasks` inside `package/module/jsonc-edit` shows 56 tasks
instead of 1807.
One root `meow.hcl` removes the mechanism that produces that.

Option A,
 `meow` infers the package from the process working directory
and narrows listings and relative targets to it.

- Pros:
   preserves today's affordance exactly;
   keeps `AGENTS.md:891` meaningful with a wording change;
   matches the design's existing "relative forms" target syntax (`:288-297`).
- Cons:
   adds hidden state to every command;
   two terminals in different directories behave differently,
   which the JSON stream will not explain.

Option B,
 no directory inference;
narrowing is explicit,
for example `meow status //package/module/...`.

- Pros:
   one command means one thing everywhere;
   simplest to specify and to log.
- Cons:
   every listing starts from 1800-plus targets;
   loses an affordance that costs nothing today.

Option C,
 directory inference for listing and status only,
never for `meow run`.

- Pros:
   the destructive path stays explicit while the exploratory path stays convenient;
   a wrong guess can only mislist,
   never misrun.
- Cons:
   a split rule to learn;
   the design currently describes one target grammar,
   not two.

Ranking:
 C > A > B.
C over A because the only real hazard in directory inference
is running the wrong package's task,
and C removes exactly that while keeping the benefit.
A over B because B discards a measured affordance
(56 versus 1807) to buy consistency that C already buys.

### Question 2: where does per-task logic live when commands are argv lists?

1,155 lines of TypeScript currently sit in task bodies.
Fan-out and concurrency go to the daemon.
Built-versus-source selection,
test-file discovery,
and platform branching do not have an owner.

Option A,
 repository packages with real `bin` entries,
invoked as `command = ["node", "package/dev-script/task-util/src/dispatch.ts", ...]`.

- Pros:
   the code becomes lintable,
   type-checked,
   and testable,
   which today it is not;
   rule `RCI` prefers extending `task-util`,
   which already owns this boundary;
   no meow feature needed.
- Cons:
   pays a Node start per dispatch,
   already measured at 56.9 ms in
   `doc/decision/mise-task-node-invocations.md`;
   keeps a Node dependency on paths meow could own.

Option B,
 meow built-ins for the recurring cases
(prefer-built-output,
 discover-test-files).

- Pros:
   removes the Node hop entirely;
   the daemon can cache the discovery;
   the behavior becomes inspectable from the target.
- Cons:
   meow grows repository-specific features;
   each built-in is a new thing to specify,
   build,
   and document
   before meow can replace Mise at all.

Option C,
 HCL expressions in `meow.hcl`,
using the evaluator to pick paths and expand file lists.

- Pros:
   stays in one language;
   matches the "full language allowed" answer (`:1573`).
- Cons:
   puts filesystem probing in configuration evaluation,
   which the design wants cacheable by read set;
   `hcl-rs` has no built-in functions (issue #484,
   noted at `:1544-1545`).

Ranking:
 A > B > C.
A over B because A needs no meow feature and makes 1,155 currently unchecked lines
subject to the repository's own TypeScript rules,
which is a correctness win available immediately.
B over C because C makes configuration evaluation do I/O,
which conflicts with the cache-key design the same document settles.

### Question 3: does the `depends` ban survive?

`mise.toml:625` bans `depends`;
41 tasks use it;
the design settles ordering from declared reads,
 writes,
 and `depends_on`.

Option A,
 retire the ban.
meow gets `depends_on`,
and the 41 edges migrate as-is.

- Pros:
   matches the design already settled at `:1559-1562`;
   matches what the tree does;
   ends a policy that is provably not enforced.
- Cons:
   loses whatever readability the ban was buying;
   `AGENTS.md` and the root comment both need editing.

Option B,
 keep the ban,
and require every edge to be expressed as reads and writes
so meow infers the order.

- Pros:
   maximal caching,
   since a declared write is also a cache input;
   no author ever states an order that the file set contradicts.
- Cons:
   12 `pi-plugin` `verify:extension` tasks depend on `build`
   for reasons that are about artifact existence,
   not file contents;
   forcing every edge through file declarations may be more ceremony than the ban saved.

Ranking:
 A > B.
A over B because the ban's stated reason,
"it's the easiest to understand",
is already contradicted by 41 counterexamples,
and B asks authors to encode an artifact-existence edge as a file-set edge,
which is the harder thing to read,
not the easier one.

### Question 4: how does an agent in a disposable worktree get past the trust prompt?

Rule `IWT` requires decision-verb work to happen in `git worktree add` forks.
Verified (`doc/troubleshooting/mise-disposable-worktree-trust.md`):
Mise already blocks there today.
meow's trust model prints every evaluated file and waits for an answer (`:389-418`).
An agent cannot answer it.

Option A,
 trust follows repository identity,
so a worktree of an already-trusted repository at the same commit inherits trust.

- Pros:
   the worktree case disappears;
   `cli-git` already reasons about repository identity,
   which rule `WXG` and `doc/planning/git-wrong-repository-identity-defense.md` cover.
- Cons:
   a worktree can be checked out at an untrusted commit;
   identity is a weaker key than exact bytes,
   which the design chose deliberately.

Option B,
 a `meow trust --accept-file <path>` non-interactive form
that a caller may use only when it already holds the bytes.

- Pros:
   keeps exact-bytes trust intact;
   scriptable,
   so `IWT` workflows and CI both work.
- Cons:
   an agent that can call it has effectively self-trusted;
   the security property becomes a convention about who may call it.

Option C,
 leave it interactive and require the human to trust each worktree.

- Pros:
   strongest property,
   no new surface.
- Cons:
   every decision-verb task now needs a human in the loop for setup,
   which rule `CKA` says to bridge rather than hand off.

Ranking:
 B > A > C.
B over A because A silently weakens the exact-bytes rule the design settled,
while B keeps it and moves the decision to an explicit,
 auditable call.
A over C because C makes the repository's own mandated workflow
require a human for every fork,
which is a cost paid constantly for a risk that arises rarely.
